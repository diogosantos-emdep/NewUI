<dxwui:WinUIDialogWindow  x:Class="Emdep.Geos.UI.CustomControls.MessageBoxEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
        DataContext="MessageBoxEditViewModel"
         x:ClassModifier="internal">                      
    <Grid HorizontalAlignment="Stretch" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image Source="{Binding ImagePath}" Height="50" Grid.Column="0" VerticalAlignment="Center" ></Image>

        <WrapPanel  Orientation="Horizontal" Width="Auto" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Stretch" >
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                    HorizontalScrollBarVisibility="Disabled" 
                    CanContentScroll="True">
                <TextBox Text="{Binding Message}" IsReadOnly="True" VerticalAlignment="Stretch" HorizontalAlignment="Center" FontSize="20" Margin="10,0,0,0" TextWrapping="Wrap"/>
            </ScrollViewer>
        </WrapPanel>
    </Grid>
</dxwui:WinUIDialogWindow>
