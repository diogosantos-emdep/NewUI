<dxwui:WinUIDialogWindow 
                x:Class="Emdep.Geos.UI.CustomControls.PickingInfoMessageBoxView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
                xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
                DataContext="PickingInfoMessageBoxViewModel"
                xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                x:ClassModifier="internal">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image Source="{Binding ImagePath}" Height="50" Grid.Column="0" VerticalAlignment="Center" ></Image>
        <WrapPanel  Orientation="Horizontal" Width="Auto" Grid.Column="1" VerticalAlignment="Center" >
            <dxe:ListBoxEdit x:Name="Message"
                                 ItemsSource="{Binding DataContext.CommentList,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxwui:WinUIDialogWindow}}}"
                                 AllowItemHighlighting="False" SelectionMode="Single" Focusable="False" Margin="0,0,0,0" ShowBorder="False" IsReadOnly="True" >
                <dxe:ListBoxEdit.ItemsPanel>
                    <ItemsPanelTemplate>
                            <WrapPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </dxe:ListBoxEdit.ItemsPanel>
                <dxe:ListBoxEdit.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
                            <TextBlock Text="{Binding OTCode}" VerticalAlignment="Stretch" HorizontalAlignment="Left" FontSize="20" Margin="10,0,0,0" TextWrapping="Wrap" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Comment}" VerticalAlignment="Stretch" HorizontalAlignment="Left" FontSize="20" Margin="10,0,0,0" TextWrapping="Wrap" />
                            <TextBlock Text="" VerticalAlignment="Stretch" HorizontalAlignment="Left" FontSize="20" Margin="10,0,0,0" TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </dxe:ListBoxEdit.ItemTemplate>
            </dxe:ListBoxEdit>
        </WrapPanel>
    </Grid>
</dxwui:WinUIDialogWindow>
