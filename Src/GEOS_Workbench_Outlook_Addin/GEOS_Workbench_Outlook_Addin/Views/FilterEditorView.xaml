<dxc:DXWindow x:Class="Emdep.Geos.Modules.Crm.Views.FilterEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Crm.Views"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
             xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             mc:Ignorable="d" 
             Title="Filter Window" Height="300" Width="600"
             d:DesignHeight="300" d:DesignWidth="300"
             >
    <dxc:DXWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding FilterEditorViewCancelCommand}"/>
    </dxc:DXWindow.InputBindings>
    <Grid x:Name="Grid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition MaxHeight="65"/>
            <RowDefinition MaxHeight="50"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"  Grid.Row="1" HorizontalAlignment="Left"  VerticalAlignment="Center">
            <dxe:TextEdit Margin="5" Width="150" Height="25" Text="{Binding FilterName,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">
                <dxe:TextEdit.EditValue>
                    <Binding Path="FilterName" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                        <Binding.ValidationRules>
                            <Validation:CustomFilterEditorValidation FieldName="FilterName" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                        </Binding.ValidationRules>
                    </Binding>
                </dxe:TextEdit.EditValue>
            </dxe:TextEdit>
            <CheckBox Margin="5" Content="Save for future use" IsChecked="{Binding IsSave,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"></CheckBox>
        </StackPanel> 
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Button Content="{DynamicResource FiterEditorViewAccept}" Margin="5,5,5,5" Command="{Binding FilterEditorViewAcceptCommand}" Height="30" Width="100"/>
            <Button Content="{DynamicResource FiterEditorViewDelete}" Margin="5,5,5,5" Command="{Binding FilterEditorViewDeleteButtonCommand}" Height="30" Width="100"/>
            <Button Content="{DynamicResource FiterEditorViewCancel}" Margin="5,5,5,5" Command="{Binding FilterEditorViewCancelCommand}" Height="30" Width="100"/>
        </StackPanel>
    </Grid>
    <dxmvvm:Interaction.Triggers>
        <dxmvvm:EventToCommand EventName="Closing" Command="{Binding DXWindowClosingCommand}"
                                               PassEventArgsToCommand="True"/>
    </dxmvvm:Interaction.Triggers> 
</dxc:DXWindow> 
