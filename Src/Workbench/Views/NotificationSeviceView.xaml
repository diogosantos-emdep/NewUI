<UserControl  
            x:Class="Workbench.Views.NotificationSeviceView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
            xmlns:ViewModel="clr-namespace:Workbench.ViewModels"         
            mc:Ignorable="d" 
            d:DesignHeight="300" d:DesignWidth="300">
   
    <!--DataContext="{dxmvvm:ViewModelSource Type={x:Type ViewModel:CustomNotificationViewModel}}"-->
    <UserControl.Resources>
        <DataTemplate x:Key="CustomNotificationTemplate" DataType='{x:Type ViewModel:CustomNotificationViewModel}'>
            <Border Background="White" BorderThickness="1" BorderBrush="Black">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock HorizontalAlignment="Left" Text="test" Foreground="Blue" Style="{DynamicResource TextBlockCustomNotificationViewModel}" Margin="5"/>
                    <TextBlock HorizontalAlignment="Center" Text="{Binding Content}" Foreground="Black" Style="{DynamicResource TextBlockCustomNotificationViewModel1}" Margin="3"/>
                    <TextBlock HorizontalAlignment="Left" Text="test" Foreground="Blue" Style="{DynamicResource TextBlockCustomNotificationViewModel}" Margin="5"/>
                    <TextBlock HorizontalAlignment="Center" Text="test1" Foreground="Black" Style="{DynamicResource TextBlockCustomNotificationViewModel1}" Margin="3"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:NotificationService x:Name="ServiceWithDefaultNotifications" 
                                    ApplicationId="sample_notification_app"
                                    PredefinedNotificationTemplate="LongHeaderAndShortText"/>    
    </dxmvvm:Interaction.Behaviors>
    
</UserControl>
