<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:ViewModels="clr-namespace:Workbench.ViewModels" x:Class="Workbench.Views.AuthenticationView"
             Foreground="White"  KeyboardNavigation.TabNavigation="Cycle"
             Padding="0,0,0,0" Margin="0,0,0,0" Style="{DynamicResource UserControlBackgroundStyle}"
             mc:Ignorable="d"  
             d:DesignHeight="350" d:DesignWidth="380">

    <!--<UserControl.Resources>
        <ResourceDictionary Source="/GeosWorkbench;component/Resources/Language.xaml" />
        </UserControl.Resources>-->
    <!--Your window content-->

    <UserControl.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding DataContext.LoginImpersonateEscapeActionCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
    </UserControl.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"  Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Stretch" HorizontalAlignment="Center" >
            <Ellipse  Style="{DynamicResource EllipseImpersonateUserStyle}"
				 StrokeThickness="1" Height="100" Width="100"   HorizontalAlignment="Center">
            </Ellipse>
        </StackPanel>
        <dxlc:LayoutItem  Style="{DynamicResource LayoutItemForgroundStyle}" Label="{DynamicResource AuthenticationViewIdentity}" Margin="40,0,65,0" Padding="0,0,0,0" Grid.Row="1" VerticalAlignment="Center" Height="30" HorizontalAlignment="Center">
            <dxe:ComboBoxEdit   Grid.Column="1"  Grid.Row="1"  Height="30" Style="{DynamicResource ComboBoxEditStyle}"
				SelectionStart="0"                  
				Width="246"
				Text="{Binding LoginImpersonateName,UpdateSourceTrigger=PropertyChanged}"
				SelectedIndex="{Binding SelectedIndexImpersonateUser ,UpdateSourceTrigger=PropertyChanged}"
				ApplyItemTemplateToSelectedItem="True" 
				AllowDefaultButton="False"
				ItemsSource="{Binding ImpersonateUserList,UpdateSourceTrigger=PropertyChanged}"
				AutoComplete="True" 
				IsTextEditable="True"
				ImmediatePopup="True"
				IncrementalFiltering="True" 
				HorizontalAlignment="Center" 
				VerticalAlignment="Center" TabIndex="0" 
                x:Name="UserControlImpersonate" 
                FocusManager.FocusedElement="{Binding ElementName=UserControlImpersonate}"/>
        </dxlc:LayoutItem>
        <DockPanel Grid.Row="2" Width="300" Height="35" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Width="130" Height="30" HorizontalAlignment="Left" Content="{DynamicResource NewPasswordViewAccept}"  Command="{Binding LoginImpersonateAcceptButtonCommand}" TabIndex="1" IsDefault="True" Style="{DynamicResource ButtonStyle}" />
            <Button Width="130" Height="30" HorizontalAlignment="Right" Content="{DynamicResource NewPasswordViewCancel}" Command="{Binding LoginImpersonateCancelButtonCommand}" TabIndex="2" Style="{DynamicResource ButtonStyle}" />
        </DockPanel>
    </Grid>
</UserControl>
