<UserControl x:Class="Emdep.Geos.Modules.Hrm.Views.EmployeeBacklogHolidaysView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Hrm.Views"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:Coverter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxsch="http://schemas.devexpress.com/winfx/2008/xaml/scheduling"
             xmlns:dxscht="http://schemas.devexpress.com/winfx/2008/xaml/scheduling/themekeys"
             xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys"
             xmlns:dxschcore="http://schemas.devexpress.com/winfx/2008/xaml/scheduling/core"
             xmlns:dxschc="http://schemas.devexpress.com/winfx/2008/xaml/scheduling/common"
             xmlns:dxschv="http://schemas.devexpress.com/winfx/2008/xaml/scheduling/visual"
             xmlns:dxr="http://schemas.devexpress.com/winfx/2008/xaml/ribbon"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
             xmlns:dxa="http://schemas.devexpress.com/winfx/2008/xaml/accordion" 
             xmlns:dxschd="clr-namespace:DevExpress.Xpf.Scheduler.Drawing;assembly=DevExpress.Xpf.Scheduler.v19.2"
             xmlns:HrmApplication="clr-namespace:Emdep.Geos.Modules.Hrm"
             xmlns:ViewModel="clr-namespace:Emdep.Geos.Modules.Hrm.ViewModels"
             xmlns:behaviors="clr-namespace:Emdep.Geos.UI.Behaviors;assembly=Emdep.Geos.UI.Common"
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             xmlns:dxschp="clr-namespace:DevExpress.Xpf.Scheduling.Panels;assembly=DevExpress.Xpf.Scheduling.v19.2" 
             xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:dxei="http://schemas.devexpress.com/winfx/2008/xaml/editors/internal"
             xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="EmployeeLeavesViewInstance">

    <UserControl.Resources>
        <Coverter:HoursToDayConverter x:Key="HoursToDayConverter" />
    </UserControl.Resources>

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:EventToCommand EventName="Loaded" PassEventArgsToCommand="True" 
                               Command="{Binding LoadedEmployeeLeavesViewInstanceCommand,Mode=TwoWay}" 
                               CommandParameter="{Binding ElementName=EmployeeLeavesViewInstance}">
        </dxmvvm:EventToCommand>
    </dxmvvm:Interaction.Behaviors>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="AUTO" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" Margin="20,0,0,0" >
                <Label x:Name="LabelLeaves" Content="{DynamicResource Automaticbacklog_Calculatebacklogdays}" 
            Style="{StaticResource LabelTitleStyle}"
            HorizontalAlignment="Left" VerticalAlignment="Center" />
            </StackPanel>
           

        </Grid>

        <GroupBox Grid.Column="0" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!--Region -->
                <Label Grid.Row="0" Grid.Column="0" Margin="1,0,0,0" Content="{DynamicResource Automaticbacklog_Region}" />
                <dxe:ComboBoxEdit Grid.Row="0" Grid.Column="3" Height="30"
                    Name="CbeRegion"
                    Style="{DynamicResource ComboBoxEditStyle}" 
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    IsTextEditable="False"
                    DisplayMember="RegionName" 
                    ItemsSource="{Binding RegionList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                    EditValue="{Binding SelectedRegion, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                    IsEnabled ="{Binding EnableFilters, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                    <dxe:ComboBoxEdit.StyleSettings>
                        <dxe:CheckedComboBoxStyleSettings />
                    </dxe:ComboBoxEdit.StyleSettings>
                    <dxmvvm:Interaction.Triggers>
                        <dxmvvm:EventToCommand EventName="PopupClosed" Command="{Binding ChangeRegionCommand}" PassEventArgsToCommand="True"/>
                    </dxmvvm:Interaction.Triggers>
                </dxe:ComboBoxEdit>

                <!--Country-->

                <Label Grid.Row="2" Grid.Column="0" Margin="1,0,0,0" Content="{DynamicResource Automaticbacklog_Country}" />
                <dxe:ComboBoxEdit Grid.Row="2" Grid.Column="3" 
                    Name="CbeCountry"
                    Style="{DynamicResource ComboBoxEditStyle}" 
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    IsTextEditable="False"
                    DisplayMember="CountryName" 
                    ItemsSource="{Binding CountryList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                    EditValue="{Binding SelectedCountry, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                    IsEnabled ="{Binding EnableFilters, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">

                    <dxe:ComboBoxEdit.StyleSettings>
                        <dxe:CheckedComboBoxStyleSettings />
                    </dxe:ComboBoxEdit.StyleSettings>
                    <dxmvvm:Interaction.Triggers>
                        <dxmvvm:EventToCommand EventName="PopupClosed" Command="{Binding ChangeCountryCommand}" PassEventArgsToCommand="True"/>
                    </dxmvvm:Interaction.Triggers>
                </dxe:ComboBoxEdit>

                <!--Plant-->

                <Label Grid.Row="4" Grid.Column="0" Margin="1,0,0,0" Content="{DynamicResource Automaticbacklog_Plant}" />
                <dxe:ComboBoxEdit Grid.Row="4" Grid.Column="3" 
                    Name="CbePlant"
                    Style="{DynamicResource ComboBoxEditStyle}" 
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    IsTextEditable="False"
                    DisplayMember="SiteName" 
                    ItemsSource="{Binding PlantList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                    EditValue="{Binding SelectedPlant, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                    IsEnabled ="{Binding EnableFilters, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">

                    <dxe:ComboBoxEdit.StyleSettings>
                        <dxe:CheckedComboBoxStyleSettings />
                    </dxe:ComboBoxEdit.StyleSettings>
                    <dxmvvm:Interaction.Triggers>
                        <dxmvvm:EventToCommand EventName="PopupClosed" Command="{Binding ChangePlantCommand}" PassEventArgsToCommand="True"/>
                    </dxmvvm:Interaction.Triggers>
                </dxe:ComboBoxEdit>


                <!--Department-->
                <Label Grid.Row="6" Grid.Column="0" Margin="1,0,0,0" Content="{DynamicResource Automaticbacklog_Department}" />
                <dxe:ComboBoxEdit Grid.Row="6" Grid.Column="3" 
                    Name="cbDepartment"
                    Style="{DynamicResource ComboBoxEditStyle}" 
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    IsTextEditable="False"
                    DisplayMember="DepartmentName" 
                    ItemsSource="{Binding DepartmentList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                    EditValue="{Binding SelectedDepartment, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                    IsEnabled ="{Binding EnableFilters, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                    <dxe:ComboBoxEdit.StyleSettings>
                        <dxe:CheckedComboBoxStyleSettings />
                    </dxe:ComboBoxEdit.StyleSettings>
                    <dxmvvm:Interaction.Triggers>
                        <dxmvvm:EventToCommand EventName="PopupClosed" Command="{Binding ChangePlantCommand}" PassEventArgsToCommand="True"/>
                    </dxmvvm:Interaction.Triggers>
                </dxe:ComboBoxEdit>



            </Grid>
            
        </GroupBox>
        <dxg:GridControl x:Name="dxgGridControl1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Column="1" Grid.Row="1"  Grid.ColumnSpan="2"
                         ItemsSource="{Binding Path=EmployeeListFiltered,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" SelectionMode="Row"
                          SelectedItem="{Binding SelectedObject, Mode=TwoWay}" ImplyNullLikeEmptyStringWhenFiltering="True"
                         AllowLiveDataShaping="True" Visibility="{Binding DataContext.ShowNextButton, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}">

            <dxg:GridColumn Header="" ShowCheckBoxInHeader="True" FieldName="IsSelected" Width="50" FixedWidth="True" 
                     AllowBestFit="true" 
                     AllowCellMerge="False"
                     AllowMoving="false"
                      AllowSorting="False"
                     AllowAutoFilter="False"
                     ShowInColumnChooser="True" 
                     FilterPopupMode="CheckedList">
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <CheckBox x:Name="CheckedBox_Editor" IsChecked="{Binding RowData.Row.IsSelected,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
            </dxg:GridColumn>
            
            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Code}" FieldName="EmployeeCode" ColumnFilterMode="DisplayText" Width="120" FixedWidth="True" ReadOnly="True" />
            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Firstname}" FieldName="FirstName" ColumnFilterMode="DisplayText" Width="50*" FixedWidth="False" ReadOnly="True" />
            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Lastname}" FieldName="LastName" ColumnFilterMode="DisplayText" FilterPopupMode="CheckedList" Width="50*" FixedWidth="False" ReadOnly="True" />
            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Department}" FieldName="EmployeeDepartments" ColumnFilterMode="DisplayText" Width="250" FixedWidth="True" ReadOnly="True" />
            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Organization}" FieldName="Company" ColumnFilterMode="DisplayText" Width="150" FixedWidth="True" ReadOnly="True" />           

            <dxmvvm:Interaction.Behaviors>
                <UIHelper:GridSelectingBehavior />
               
                <dxmvvm:EventToCommand EventName="CustomRowFilter"
                                       Command="{Binding CustomRowFilterCommand}"
                                       PassEventArgsToCommand="True" />

            </dxmvvm:Interaction.Behaviors>

            <dxg:GridControl.TotalSummary>
                <dxg:GridSummaryItem x:Name="summary" FieldName="DepartmentName" SummaryType="Count" DisplayFormat="Total Count: {0}" Visible="True" Alignment="Left" />
            </dxg:GridControl.TotalSummary>

            <dxg:GridControl.View>
                <dxg:TableView x:Name="EmployeeTableView"
                                   NavigationStyle="Row"
                                   AllowEditing="True"
                                   RowDetailsVisibilityMode="Visible"
                                   ShowHorizontalLines="True"
                                   ShowSearchPanelMode="Always"
                                   ShowColumnHeaders="True"
                                   VerticalScrollbarVisibility="Auto" 
                                   HorizontalScrollbarVisibility="Auto"
                                   AllowFixedColumnMenu="False"
                                   ShowFixedTotalSummary="True" 
                                   SearchPanelHorizontalAlignment="Stretch"
                                   ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                   AllowGrouping="True"
                                   ShowGroupPanel="False"
                                   ShowIndicator="False" AutoWidth="True"
                                   SearchString="{Binding MyFilterString,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">

                    <dxg:TableView.Resources>
                        <DataTemplate x:Key="EmployeeLeaveReportPrintHeaderTemplate">
                            <Grid Width="{Binding UsablePageWidth}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="130" />
                                </Grid.ColumnDefinitions>
                                <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" Height="50" />
                                <dxe:TextEdit Text="Employee Leaves Report" IsPrintingMode="True" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" />
                                <Image Source="/Emdep.Geos.Modules.Hrm;component/Assets/Images/GeosReportLogo.png" Height="50" Width="80" dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                            </Grid>
                        </DataTemplate>

                        <DataTemplate x:Key="EmployeeLeaveReportPrintFooterTemplate">
                            <dxe:TextEdit HorizontalContentAlignment="Center" 
                                                  Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                                  dxp:ExportSettings.TargetType="PageNumber" 
                                                  dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                                  dxp:PageNumberExportSettings.Format="Page {0} of {1}" />
                        </DataTemplate>

                    </dxg:TableView.Resources>

                    <dxmvvm:Interaction.Behaviors>
                        <dxmvvm:EventToCommand Command="{Binding EditLeaveGridDoubleClickCommand}" EventName="MouseDoubleClick" CommandParameter="{Binding ElementName=LeaveTableView}">
                            <dxmvvm:EventToCommand.EventArgsConverter>
                                <dx:EventArgsToDataRowConverter />
                            </dxmvvm:EventToCommand.EventArgsConverter>
                        </dxmvvm:EventToCommand>
                       
                        <dxmvvm:EventToCommand EventName="ShowFilterPopup"
                                               Command="{Binding CustomShowFilterPopupCommand}"
                                               PassEventArgsToCommand="True" />


                    </dxmvvm:Interaction.Behaviors>

                </dxg:TableView>
               
            </dxg:GridControl.View>
            
        </dxg:GridControl>

        <dxg:GridControl x:Name="dxgGridControl2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Column="1" Grid.Row="1"  Grid.ColumnSpan="2"
                 ItemsSource="{Binding Path=EmployeeListFiltered,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" SelectionMode="Row"
                  SelectedItem="{Binding SelectedObject, Mode=TwoWay}" ImplyNullLikeEmptyStringWhenFiltering="True"
                 AllowLiveDataShaping="True" Visibility="{Binding DataContext.ShowSaveButton, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}">

            <dxg:GridColumn Header="" ShowCheckBoxInHeader="True" FieldName="IsSelected" Width="50" FixedWidth="True" 
             AllowBestFit="true" 
             AllowCellMerge="False"
             AllowMoving="false"
              AllowSorting="False"
             AllowAutoFilter="False"
             ShowInColumnChooser="True" 
             FilterPopupMode="CheckedList">
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <CheckBox x:Name="CheckedBox_Editor" IsChecked="{Binding RowData.Row.IsSelected,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
               HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
            </dxg:GridColumn>

            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Code}" FieldName="EmployeeCode" ColumnFilterMode="DisplayText" Width="120" FixedWidth="True" ReadOnly="True" />
            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Firstname}" FieldName="FirstName" ColumnFilterMode="DisplayText" Width="50*" FixedWidth="False" ReadOnly="True" />
            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Lastname}" FieldName="LastName" ColumnFilterMode="DisplayText" FilterPopupMode="CheckedList" Width="50*" FixedWidth="False" ReadOnly="True" />
            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Department}" FieldName="EmployeeDepartments" ColumnFilterMode="DisplayText" Width="250" FixedWidth="True" ReadOnly="True" />
            <dxg:GridColumn Header="{DynamicResource Automaticbacklog_Organization}" FieldName="Company" ColumnFilterMode="DisplayText" Width="150" FixedWidth="True" ReadOnly="True" />
            <!--<dxg:GridColumn Header="{DynamicResource Automaticbacklog_Backlog_days}" FieldName="BacklogHours" ColumnFilterMode="DisplayText" Width="150" FixedWidth="True" ReadOnly="True"
                    Visible="{Binding DataContext.ShowBacklogDays, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"/>-->
            <dxg:GridColumn FieldName="BacklogHoursCount" Header="{DynamicResource Automaticbacklog_Backlog_days}" 
     Width="150" FixedWidth="True" ReadOnly="True" FilterPopupMode="CheckedList"
     Visible="{Binding DataContext.ShowBacklogDays, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
     AllowResizing="True">
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock >
                            <TextBlock.Text >
                                <MultiBinding Converter="{Coverter:HoursToDayConverter}">
                                    <Binding Path="RowData.Row.BacklogHours"  />
                                    <Binding Path="RowData.Row.DailyWorkingHours" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
            </dxg:GridColumn>



            <dxmvvm:Interaction.Behaviors>
                <UIHelper:GridSelectingBehavior />

                <dxmvvm:EventToCommand EventName="CustomRowFilter"
                               Command="{Binding CustomRowFilterCommand}"
                               PassEventArgsToCommand="True" />

            </dxmvvm:Interaction.Behaviors>

            <dxg:GridControl.TotalSummary>
                <dxg:GridSummaryItem x:Name="summary2" FieldName="DepartmentName" SummaryType="Count" DisplayFormat="Total Count: {0}" Visible="True" Alignment="Left" />
            </dxg:GridControl.TotalSummary>

            <dxg:GridControl.View>
                <dxg:TableView x:Name="EmployeeTableView2"
                           NavigationStyle="Row"
                           AllowEditing="True"
                           RowDetailsVisibilityMode="Visible"
                           ShowHorizontalLines="True"
                           ShowSearchPanelMode="Always"
                           ShowColumnHeaders="True"
                           VerticalScrollbarVisibility="Auto" 
                           HorizontalScrollbarVisibility="Auto"
                           AllowFixedColumnMenu="False"
                           ShowFixedTotalSummary="True" 
                           SearchPanelHorizontalAlignment="Stretch"
                           ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                           AllowGrouping="True"
                           ShowGroupPanel="False"
                           ShowIndicator="False" AutoWidth="True"
                           SearchString="{Binding MyFilterString,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">

                    <dxg:TableView.Resources>
                        <DataTemplate x:Key="EmployeeLeaveReportPrintHeaderTemplate">
                            <Grid Width="{Binding UsablePageWidth}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="130" />
                                </Grid.ColumnDefinitions>
                                <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E" HorizontalAlignment="Stretch" 
                                              VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" Height="50" />
                                <dxe:TextEdit Text="Employee Leaves Report" IsPrintingMode="True" HorizontalAlignment="Stretch" 
                                              VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" />
                                <Image Source="/Emdep.Geos.Modules.Hrm;component/Assets/Images/GeosReportLogo.png" Height="50" Width="80" dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                            </Grid>
                        </DataTemplate>

                        <DataTemplate x:Key="EmployeeLeaveReportPrintFooterTemplate">
                            <dxe:TextEdit HorizontalContentAlignment="Center" 
                                          Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                          dxp:ExportSettings.TargetType="PageNumber" 
                                          dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                          dxp:PageNumberExportSettings.Format="Page {0} of {1}" />
                        </DataTemplate>

                    </dxg:TableView.Resources>

                    <dxmvvm:Interaction.Behaviors>
                        <dxmvvm:EventToCommand Command="{Binding EditLeaveGridDoubleClickCommand}" EventName="MouseDoubleClick" CommandParameter="{Binding ElementName=LeaveTableView}">
                            <dxmvvm:EventToCommand.EventArgsConverter>
                                <dx:EventArgsToDataRowConverter />
                            </dxmvvm:EventToCommand.EventArgsConverter>
                        </dxmvvm:EventToCommand>

                        <dxmvvm:EventToCommand EventName="ShowFilterPopup"
                                       Command="{Binding CustomShowFilterPopupCommand}"
                                       PassEventArgsToCommand="True" />


                    </dxmvvm:Interaction.Behaviors>

                </dxg:TableView>

            </dxg:GridControl.View>

        </dxg:GridControl>

        <Grid Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="3" Visibility="{Binding DataContext.ShowNextButton, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Center">
            <Button Content="{DynamicResource Automaticbacklog_Next}" 
         Command="{Binding NextButtonCommand}" 
         Height="30"
         Width="100" IsEnabled="{Binding IsAcceptEnable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
         Margin="0,0,0,0"
         Style="{DynamicResource Button16Style}"
         VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>

        <Grid Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="3" Visibility="{Binding DataContext.ShowSaveButton, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <Button Content="{DynamicResource Automaticbacklog_Reset}" 
                    Command="{Binding ResetButtonCommand}" 
                  Height="30"
                  Width="100" IsEnabled="{Binding IsAcceptEnable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Margin="0,0,40,0"
                  Style="{DynamicResource Button16Style}"
                  VerticalAlignment="Center" HorizontalAlignment="Center"/>

                <Button Content="{DynamicResource Automaticbacklog_Save}" 
                    Command="{Binding SaveButtonCommand}" 
                  Height="30"
                  Width="100" IsEnabled="{Binding IsAcceptEnable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Margin="40,0,0,0"
                  Style="{DynamicResource Button16Style}"
                  VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>


    </Grid>
</UserControl>
