<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.Hrm.Views.AddEditSkillsView"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                         xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                         xmlns:local="clr-namespace:Emdep.Geos.Modules.Hrm.Views"
                         xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
                         xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
                         xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                         xmlns:Interactive="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                         xmlns:Converter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
                         xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
                         xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"                         
                         mc:Ignorable="d" 
                         d:DesignHeight="300" d:DesignWidth="300"
                         Style="{DynamicResource WinUIDialogWindowStyle}"
                         Title="{Binding WindowHeader}"
                         TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}">

    <dxwui:WinUIDialogWindow.Resources>
        <dxmvvm:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Converter:GroupboxBackroundColorConvertor x:Key="GroupboxBackroundColorConvertor" />
        <Converter:ForegroundColorConverter x:Key="ForegroundColorConverter" />
    </dxwui:WinUIDialogWindow.Resources>
    
    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding EscapeButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <!--<ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="270"/>-->
            <ColumnDefinition Width="155"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>
        <!--Code-->
        <TextBlock Grid.Row="0" Grid.Column="0"  Text="{DynamicResource SkillsCode}" Style="{StaticResource TextBlock16Style}" VerticalAlignment="Center"  />
        <dxe:TextEdit Grid.Row="0" Grid.Column="2" IsReadOnly="True"
                      Text="{Binding Code}" VerticalContentAlignment="Center">
        </dxe:TextEdit>
        
        <!--Name-->
        <TextBlock Grid.Row="2" Grid.Column="0"  Text="{DynamicResource SkillsName}" Style="{StaticResource TextBlock16Style}" VerticalAlignment="Center" />
        <dxe:TextEdit Text="{Binding Name}" Grid.Row="2" Grid.Column="2" MaxLength="50" NullText="{DynamicResource SkillsName}"
                      AllowNullInput="True" InvalidValueBehavior="AllowLeaveEditor">
            <dxe:TextEdit.EditValue>
                <Binding Path="Name" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <Binding.ValidationRules>
                        <Validation:DepartmentValidation FieldName="Name" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </dxe:TextEdit.EditValue>
        </dxe:TextEdit>

        <!--Description-->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="{DynamicResource SkillsDescription}" Style="{StaticResource TextBlock16Style}" VerticalAlignment="Center" />
        <dxe:TextEdit Text="{Binding Description}" NullText="{DynamicResource SkillsDescription}" Grid.Row="4" Grid.Column="2" VerticalContentAlignment="Top" VerticalAlignment="Top"
                      HorizontalAlignment="Left" TextWrapping="Wrap" MaxLength="300" Height="60" Width="350"
                      AllowNullInput="True" InvalidValueBehavior="AllowLeaveEditor" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AcceptsReturn="True">
            <dxe:TextEdit.EditValue>
                <Binding Path="Description" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <Binding.ValidationRules>
                        <Validation:DepartmentValidation FieldName="Description" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </dxe:TextEdit.EditValue>
        </dxe:TextEdit>

        <!--Type-->
        <TextBlock Grid.Row="6" Grid.Column="0" Text="{DynamicResource SkillsType}" Style="{StaticResource TextBlock16Style}" VerticalAlignment="Center" />
        <dxe:ComboBoxEdit Grid.Row="6" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left" Width="350" Height="30"
                          AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True"
                          ApplyItemTemplateToSelectedItem="True"
                          IsSynchronizedWithCurrentItem="True"
                          AllowUpdateTwoWayBoundPropertiesOnSynchronization="True"
                          IsReadOnly="False"
                          IsTextEditable="False"
                          AllowItemHighlighting="True"
                          ItemsSource="{Binding SkillsTypeList,Mode=TwoWay}" 
                          DisplayMember="Value" ValueMember="IdLookupValue">

            <dxe:ComboBoxEdit.SelectedItem>
                <Binding Path="DataContext.SelectedType" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" ValidatesOnDataErrors="True"  UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <Binding.ValidationRules>
                        <Validation:SkillValidation FieldName="SelectedType" ValidationStep="UpdatedValue"  ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </dxe:ComboBoxEdit.SelectedItem>

            <dxe:ComboBoxEdit.ItemContainerStyle>
                <!--<Style TargetType="dxe:ComboBoxEditItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>-->
                <Style TargetType="dxe:ComboBoxEditItem">
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Data.IdLookupValue}" Value="0" />
                                <Condition Binding="{Binding Path=(dx:ThemeManager.TreeWalker).ThemeName, RelativeSource={RelativeSource Self}}" Value="BlackAndBlue" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </MultiDataTrigger>
                    </Style.Triggers>
                </Style>


            </dxe:ComboBoxEdit.ItemContainerStyle>

            <dxe:ComboBoxEdit.ItemTemplate>
                <DataTemplate>
                    <Border Margin="2,0,0,0"
                            Background="{Binding HtmlColor}"
                            BorderBrush="{Binding ColorName}"
                            Padding="2">
                        <TextBlock Margin="2,0,0,0"
                            HorizontalAlignment="Left"
                            Foreground="Black"
                            Padding="2"
                            Text="{Binding Value}" />
                    </Border>
                </DataTemplate>
            </dxe:ComboBoxEdit.ItemTemplate>

            <dxe:ComboBoxEdit.EditNonEditableTemplate>
                <ControlTemplate>
                    <Border Margin="2,2,2,2"
                            Background="{Binding SelectedItem.HtmlColor}"
                            BorderBrush="{Binding SelectedItem.ColorName}">
                        <TextBlock HorizontalAlignment="Left" Foreground="Black" Text="{Binding SelectedItem.Value}" />
                    </Border>
                </ControlTemplate>
            </dxe:ComboBoxEdit.EditNonEditableTemplate>

        </dxe:ComboBoxEdit>

        <!--InUse-->
        <TextBlock Grid.Row="8" Grid.Column="0" Grid.RowSpan="2" Text="{DynamicResource SkillsInUse}" Style="{StaticResource TextBlock16Style}" VerticalAlignment="Center" TextWrapping="Wrap"/>
        <dxe:ToggleSwitch x:Name="InUse" 
                          Grid.Row="8" Grid.Column="2"  
                          HorizontalAlignment="Left" VerticalAlignment="Center" ContentPlacement="Inside"
                          CheckedStateContent="Yes" UncheckedStateContent="No" 
                          ToggleSwitchWidth="50" ToggleSwitchHeight="22" 
                          IsThreeState="False"
                          IsChecked="{Binding InUse,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
        </dxe:ToggleSwitch>

        <!--Accept and Cancel Buttons-->
        <Grid Grid.Row="10" ShowGridLines="False" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--[GEOS2-3943][rdixit][22.11.2022] Added IsEnabled Property-->
            <Button  HorizontalAlignment="Right"
                     Style="{DynamicResource Button16Style}"
                     VerticalAlignment="Top"
                     Content="{DynamicResource AddEditSkillsAcceptButton}"
                     Command="{Binding AcceptFileActionCommand}"
                     IsEnabled="{Binding Source={x:Static GeosApplication:GeosApplication.Instance}, Path=IsAdminPermissionForHRM, Mode=TwoWay ,UpdateSourceTrigger=PropertyChanged}"
                     Height="30"
                     Width="100" />

            <Button Grid.Row="10" Grid.Column="2"
                    HorizontalAlignment="Left"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Top"
                    Content="{DynamicResource AddEditSkillsCancelButton}"
                    Command="{Binding CancelButtonCommand }"
                    Height="30"
                    Width="100" />
        </Grid>
    </Grid>
</dxwui:WinUIDialogWindow>
