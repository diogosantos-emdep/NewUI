<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.Hrm.Views.AddNewJobDescriptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Hrm.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             xmlns:HrmApplication="clr-namespace:Emdep.Geos.Modules.Hrm" 
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
              xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:behaviors="clr-namespace:Emdep.Geos.UI.Behaviors;assembly=Emdep.Geos.UI.Common"
             xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
             xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxeh="http://schemas.devexpress.com/winfx/2008/xaml/editors/internal"
              xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
                          xmlns:sys="clr-namespace:System;assembly=mscorlib"
               xmlns:ve="http://schemas.devexpress.com/winfx/2008/xaml/docking/visualelements"
                  
             mc:Ignorable="d"
            Height="{Binding SystemParameters.PrimaryScreenHeight}" 
              Width="{Binding SystemParameters.PrimaryScreenWidth}"
                         Template="{StaticResource Maximized_DXDialogWindow}"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch"
                         HorizontalContentAlignment="Stretch"
                         VerticalContentAlignment="Stretch"
                        Title="{Binding WindowHeader}"
             TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
             Style="{DynamicResource WinUIDialogWindowStyle}">

    <dxwui:WinUIDialogWindow.Resources>

        <DataTemplate x:Key="TemplateDiamond">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition>
                        <ColumnDefinition.Style>
                            <Style TargetType="{x:Type ColumnDefinition}">
                                <Setter Property="Width" Value="105"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IdImage}" Value="0">
                                        <Setter Property="Width" Value="0" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ColumnDefinition.Style>
                    </ColumnDefinition>
                    <ColumnDefinition />

                </Grid.ColumnDefinitions>
                <dxe:RatingEdit IsReadOnly="True" ItemsCount="{Binding IdImage}"  Grid.Column="0" HorizontalContentAlignment="Center">
                    <dxe:RatingEdit.ItemStyle>
                        <Style TargetType="dxe:RatingItem">
                            <Setter Property="Background" Value="Gold" />
                            <Setter Property="VerticalAlignment" Value="Stretch" />
                            <Setter Property="HorizontalAlignment" Value="Left" />
                            <Setter Property="BorderBrush" Value="Gold" />
                            <Setter Property="Geometry">
                                <Setter.Value>
                                    <RectangleGeometry Rect="0,0,25,25">
                                        <RectangleGeometry.Transform>
                                            <RotateTransform CenterX="0" CenterY="0" Angle="45"/>
                                        </RectangleGeometry.Transform>
                                    </RectangleGeometry>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </dxe:RatingEdit.ItemStyle>
                </dxe:RatingEdit>
                <TextBlock Text="{Binding Value}" VerticalAlignment="Center" Grid.Column="1"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="TemplateDiamondWithCircle">
            <StackPanel Orientation="Horizontal">
                <ItemsControl>
                    <dxe:RatingEdit IsReadOnly="True" ItemsCount="{Binding IdImage}" Width="105" Grid.Column="0" HorizontalContentAlignment="Center" >
                        <dxe:RatingEdit.ItemStyle>
                            <Style TargetType="dxe:RatingItem">
                                <Setter Property="Background" Value="Gold" />
                                <Setter Property="VerticalAlignment" Value="Stretch" />
                                <Setter Property="BorderBrush" Value="Gold" />
                                <Setter Property="Geometry">
                                    <Setter.Value>
                                        <GeometryGroup FillRule="EvenOdd">
                                            <EllipseGeometry Center="0,0" RadiusX="40" RadiusY="40" />
                                            <EllipseGeometry Center="0,0" RadiusX="40" RadiusY="40" />
                                            <RectangleGeometry Rect="-20,-20,40,40">
                                                <RectangleGeometry.Transform>
                                                    <RotateTransform CenterX="0" CenterY="0" Angle="45" />
                                                </RectangleGeometry.Transform>
                                            </RectangleGeometry>
                                        </GeometryGroup>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </dxe:RatingEdit.ItemStyle>
                    </dxe:RatingEdit>
                </ItemsControl>
                <TextBlock Text="{Binding Value}" VerticalAlignment="Center" HorizontalAlignment="Left" />
            </StackPanel>
        </DataTemplate>

        <Style x:Key="myGroupBoxStyle" TargetType="{x:Type dxlc:GroupBox}">
            <Setter Property="MaximizeElementVisibility" Value="Visible" />
            <Setter Property="Width" Value="280" />
            <Setter Property="Height" Value="145" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding MaximizedElementPosition}" Value="Bottom">
                    <Setter Property="Width" Value="280" />
                    <Setter Property="Height" Value="27" />
                </DataTrigger>
                <DataTrigger Binding="{Binding MaximizedElementPosition}" Value="Top">
                    <Setter Property="Width" Value="280" />
                    <Setter Property="Height" Value="27" />
                </DataTrigger>
            </Style.Triggers>

        </Style>

        <Style x:Key="HealthGroupBoxStyle" TargetType="{x:Type dxlc:GroupBox}">
            <Setter Property="MaximizeElementVisibility" Value="Visible" />
            <Setter Property="Width" Value="280" />
            <Setter Property="Height" Value="27" />
           

        </Style>

        <Style TargetType="{x:Type ve:AutoHideTray}">
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <ve:psvStackPanel HorizontalAlignment="Right" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </dxwui:WinUIDialogWindow.Resources>

  

    <!--Sprint 41-HRM	M041-08	New configuration section Job Descriptions-To add new Job Descriptions -sdesai-->
    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding AddJobDescriptionsViewCancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <!--<ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" >-->
    <Grid Height="{Binding DialogHeight}" Width="{Binding DialogWidth}" >

        <!--Information and changlog section -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>


        
                    <dxlc:FlowLayoutControl x:Name="flowLayoutControl"
                                            Grid.Row="0"
                                            Orientation="Horizontal"
                                            ItemSpace="2" 
                                            LayerSpace="2" 
                                            Background="Transparent" 
                                            AllowMaximizedElementMoving="False" 
                                            VerticalAlignment="Stretch"
                                            HorizontalAlignment="Stretch" Padding="5"
                                            BreakFlowToFit="False" MaximizedElementPosition="Right">
                        <dxmvvm:Interaction.Behaviors>
                            <UIHelper:FlowLayoutControlForMinimiseMaximise/>
                        </dxmvvm:Interaction.Behaviors>



                        <!--INFORMATION-->
                        <dxlc:GroupBox x:Name="Info" MaximizeElementVisibility="Visible" Style="{StaticResource myGroupBoxStyle}">
                            <dxlc:GroupBox.HeaderTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="-5,0,0,0">
                                        <TextBlock VerticalAlignment="Center"
                                                   FontWeight="Bold"
                                                   Text="{DynamicResource JobInformation}" />

                            <dxe:TextEdit Text="{DynamicResource JobInformation}" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"  ShowBorder="False" IsReadOnly="True">
                                            <Binding Path="DataContext.InformationError" RelativeSource="{RelativeSource AncestorType=dxlc:GroupBox}" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" ValidatesOnDataErrors="True">
                                                <Binding.ValidationRules>
                                                    <Validation:EditPCMArticleValidation FieldName="InformationError" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True" />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </dxe:TextEdit>
                                    </DockPanel>
                                </DataTemplate>
                            </dxlc:GroupBox.HeaderTemplate>

                            <dxlc:GroupBox.ContentTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Margin="2" VerticalAlignment="Stretch" HorizontalAlignment="Left"
                                       Text="{DynamicResource JobDescriptionJobCode1}"
                                       FontWeight="Bold"
                                       Padding="2" />
                                        <TextBlock Margin="2" Padding="1" Text="{Binding DataContext.JobDescriptionAbbreviation, RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}" VerticalAlignment="Stretch" HorizontalAlignment="Left" Grid.Row="0" Grid.Column="1" TextWrapping="WrapWithOverflow" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Row="1"
                                       Grid.Column="0"
                                       Margin="2" VerticalAlignment="Stretch" HorizontalAlignment="Left"
                                       Text="{DynamicResource JobDescriptionTitle1}"
                                       FontWeight="Bold" 
                                       Padding="2" TextWrapping="WrapWithOverflow" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Margin="2" Padding="1" Text="{Binding DataContext.JobDescriptionTitle, RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}" VerticalAlignment="Stretch" HorizontalAlignment="Left"   Grid.Row="1" Grid.Column="1" TextWrapping="WrapWithOverflow" TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding DataContext.JobDescriptionTitle, RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}"/>
                                    </Grid>
                                </DataTemplate>
                            </dxlc:GroupBox.ContentTemplate>

                            <dxlc:GroupBox.MaximizedContentTemplate>
                                <DataTemplate >
                                    <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Auto" Width="2000">
                                        <dxlc:LayoutControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0" Padding="0" Orientation="Vertical" Style="{DynamicResource LayoutControlStyle}"
                                            DragScrolling="False">
                                            <dxlc:LayoutGroup Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                                <!--<dxlc:LayoutGroup Header="{DynamicResource PCMArticleWarehouse}" Margin="0" Padding="0" VerticalAlignment="Top" View="GroupBox" Orientation="Vertical" HorizontalAlignment="Stretch">-->
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                        <RowDefinition Height="40"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="155"/>
                                                        <ColumnDefinition Width="320"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Label Grid.Row="0" Grid.Column="0" Content="{DynamicResource JobDescriptionJDCode}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>
                                                    <!--<TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding DataContext.JobDescriptionCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"  VerticalAlignment="Center" HorizontalAlignment="Left" />-->
                                                    <dxe:TextEdit 
                                                      Text="{Binding DataContext.JobDescriptionCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                                                        Margin="0,0,0,0" Grid.Row="0" Grid.Column="1"  Width="300" Height="30" VerticalAlignment="Center" HorizontalAlignment="Left" 
                      IsEnabled="True" 
                      IsReadOnly="{Binding DataContext.IsReadOnlyField, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}">
                                                        <dxe:TextEdit.EditValue>
                                                            <Binding Path="DataContext.JobDescriptionCode" RelativeSource="{RelativeSource AncestorType=dxlc:GroupBox}" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                                <Binding.ValidationRules>
                                                                    <Validation:JobDescriptionValidation FieldName="DataContext.JobDescriptionCode" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                                                </Binding.ValidationRules>
                                                            </Binding>
                                                        </dxe:TextEdit.EditValue>
                                                    </dxe:TextEdit>

                                                    <Label Grid.Row="1" Grid.Column="0" Content="{DynamicResource JobDescriptionTitle}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>
                                                    <dxe:TextEdit Margin="0,0,0,0" Grid.Row="1" Grid.Column="1" Width="300" Height="30" VerticalAlignment="Center" HorizontalAlignment="Left"
     IsReadOnly="{Binding DataContext.IsReadOnlyField, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
    IsEnabled="True" Text="{Binding DataContext.JobDescriptionTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}">
                                                        <dxe:TextEdit.EditValue>
                                                            <Binding Path="DataContext.JobDescriptionTitle" RelativeSource="{RelativeSource AncestorType=dxlc:GroupBox}" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                                <Binding.ValidationRules>
                                                                    <Validation:JobDescriptionValidation FieldName="DataContext.JobDescriptionTitle" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                                                </Binding.ValidationRules>
                                                            </Binding>
                                                        </dxe:TextEdit.EditValue>

                                                        <!--<dxe:TextEdit Text="{Binding DataContext.JobDescriptionTitle, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" 
                                                      Margin="0,0,0,0" Grid.Row="1" Grid.Column="1" 
                                                   Width="300" Height="30" VerticalAlignment="Center" HorizontalAlignment="Left"  ToolTip="{Binding DataContext.JobDescriptionTitle, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                      IsReadOnly="{Binding DataContext.IsReadOnlyField,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}">-->
                                                       
                                                        
                                                        <!-- here it is not allowing null <dxe:TextEdit Text="{Binding DataContext.JobDescriptionTitle, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" 
                                                      Margin="0,0,0,0" Grid.Row="1" Grid.Column="1" 
                                                   Width="300" Height="30" VerticalAlignment="Center" HorizontalAlignment="Left" 
                      AllowNullInput="True" EditValue="{Binding DataContext.JobDescriptionTitle,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" 
                      IsReadOnly="{Binding IsReadOnlyField,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">-->
                                                        
                                                        
                                                        
                                                        <!--<dxe:TextEdit.EditValue>
                                                            <Binding Path="DataContext.JobDescriptionTitle" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                                <Binding.ValidationRules>
                                                                    <Validation:JobDescriptionValidation FieldName="JobDescriptionTitle" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                                                </Binding.ValidationRules>
                                                            </Binding>
                                                        </dxe:TextEdit.EditValue>-->
                                                    </dxe:TextEdit>

                                                    <Label Grid.Row="2" Grid.Column="0" Content="{DynamicResource JobDescriptionJobCode}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>

                                                    <dxe:TextEdit Text="{Binding DataContext.JobDescriptionAbbreviation, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                                                                  Margin="0,0,0,0" Grid.Row="2" Grid.Column="1" Width="300" Height="30" VerticalAlignment="Center" HorizontalAlignment="Left" 
                    IsEnabled="True" 
                      IsReadOnly="{Binding DataContext.IsReadOnlyField, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}">
                                                        <dxe:TextEdit.EditValue>
                                                            <Binding Path="DataContext.JobDescriptionAbbreviation" RelativeSource="{RelativeSource AncestorType=dxlc:GroupBox}" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                                <Binding.ValidationRules>
                                                                    <Validation:JobDescriptionValidation FieldName="DataContext.JobDescriptionAbbreviation" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                                                </Binding.ValidationRules>
                                                            </Binding>
                                                        </dxe:TextEdit.EditValue>
                                                    </dxe:TextEdit>

                                                    <Label Grid.Row="3" Grid.Column="0" Content="{DynamicResource JobDescriptionParent}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>
                          <dxe:ComboBoxEdit Grid.Row="3" Grid.Column="1" HorizontalAlignment="Left" 
                         Width="300" Height="30" VerticalAlignment="Center"  ItemsSource="{Binding DataContext.JobDescriptionListParent,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged ,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"                         
                          DisplayMember="JobDescriptionTitle" ValueMember="IdJobDescription" 
                          AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True"
                          SelectedIndex="{Binding DataContext.SelectedParentIndex,Mode=TwoWay ,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox} }">
                                                        <!--[rdixit][GEOS2-4515][09.06.2023]-->
                                                        <dxe:ComboBoxEdit.ItemContainerStyle>
                                                            <Style TargetType="dxe:ComboBoxEditItem">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding JobDescriptionInUse}" Value="0">
                                                                        <Setter Property="IsEnabled" Value="False" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                                <Setter Property="IsEnabled" Value="True" />
                                                            </Style>
                                                        </dxe:ComboBoxEdit.ItemContainerStyle>
                                                    </dxe:ComboBoxEdit>

                                                    <Label Grid.Row="4" Grid.Column="0" Content="{DynamicResource JobDescriptionsDepartment}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>
                                                    <dxe:ComboBoxEdit Grid.Row="4" Grid.Column="1" HorizontalAlignment="Left" 
                         Width="300" Height="30" VerticalAlignment="Center"  ItemsSource="{Binding DataContext.DepartmentList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                          DisplayMember="DepartmentName" ValueMember="IdDepartment" 
                          AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True">
                                                        <dxe:ComboBoxEdit.SelectedIndex>
                                                            <Binding Path="DataContext.SelectedIndexDepartment" RelativeSource="{RelativeSource AncestorType=dxlc:GroupBox}" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                                <Binding.ValidationRules>
                                                                    <Validation:JobDescriptionValidation FieldName="SelectedIndexDepartment" ValidationStep="UpdatedValue" />
                                                                </Binding.ValidationRules>
                                                            </Binding>
                                                        </dxe:ComboBoxEdit.SelectedIndex>
                                                        <!--[rdixit][11.08.2022][GEOS2-2396]-->
                                                        <dxe:ComboBoxEdit.ItemContainerStyle>
                                                            <Style TargetType="dxe:ComboBoxEditItem">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding DataContext.DepartmentInUse}" Value="0">
                                                                        <Setter Property="IsEnabled" Value="False" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                                <Setter Property="IsEnabled" Value="True" />
                                                            </Style>
                                                        </dxe:ComboBoxEdit.ItemContainerStyle>
                                                    </dxe:ComboBoxEdit>

                                                    <Label Grid.Row="5" Grid.Column="0" Content="{DynamicResource JobDescriptionsLevel}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>

                                                    <!--<dxe:ComboBoxEdit Grid.Row="6" Grid.Column="1" HorizontalAlignment="Left" 
                          Height="30"  Width="250" ValueMember="IdLookupValue" VerticalAlignment="Center"
                          ItemsSource="{Binding JDLevelList, Mode=TwoWay}"
                          AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True" 
                          ApplyItemTemplateToSelectedItem="True" IsTextEditable="False" DisplayMember="Level" >

            <dxe:ComboBoxEdit.ItemTemplateSelector>
                <UIHelper:JDLevelTemplateSelector/>
            </dxe:ComboBoxEdit.ItemTemplateSelector>

            <dxe:ComboBoxEdit.SelectedIndex>
                <Binding Path="SelectedIndexLevel" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay"/>
            </dxe:ComboBoxEdit.SelectedIndex>
        </dxe:ComboBoxEdit>-->

                                                    <dxe:ComboBoxEdit Grid.Row="5" Grid.Column="1" HorizontalAlignment="Left" 
                          Height="30"  Width="300" ValueMember="IdLookupValue" VerticalAlignment="Center"   
                          ItemsSource="{Binding DataContext.JDLevelList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                          AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True" ApplyItemTemplateToSelectedItem="True" IsTextEditable="False" >
                                                        <dxe:ComboBoxEdit.ItemTemplateSelector>
                                                            <UIHelper:JDLevelTemplateSelector/>
                                                        </dxe:ComboBoxEdit.ItemTemplateSelector>

                                                        <dxe:ComboBoxEdit.SelectedIndex>
                                                            <Binding Path="DataContext.SelectedIndexLevel" RelativeSource="{RelativeSource AncestorType=dxlc:GroupBox}" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                                <Binding.ValidationRules>
                                                                    <Validation:JobDescriptionValidation FieldName="SelectedIndexLevel" ValidationStep="UpdatedValue" />
                                                                </Binding.ValidationRules>
                                                            </Binding>
                                                        </dxe:ComboBoxEdit.SelectedIndex>
                                                    </dxe:ComboBoxEdit>

                                                    <Label Grid.Row="6" Grid.Column="0" Content="{DynamicResource JobDescriptionsScope}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>
                                                    <dxe:ComboBoxEdit Grid.Row="6" Grid.Column="1" HorizontalAlignment="Left" 
                          Height="30" Width="300" VerticalAlignment="Center" 
                          ItemsSource="{Binding DataContext.JDScopeList, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" DisplayMember="Value" ValueMember="IdLookupValue"
                          AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True" IsTextEditable="False" >
                                                        <!--<dxe:ComboBoxEdit.ItemTemplateSelector>
                <UIHelper:JDLevelTemplateSelector/>
            </dxe:ComboBoxEdit.ItemTemplateSelector>-->

                                                        <dxe:ComboBoxEdit.SelectedIndex>
                                                            <Binding Path="DataContext.SelectedIndexScope" RelativeSource="{RelativeSource AncestorType=dxlc:GroupBox}" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                                <Binding.ValidationRules>
                                                                    <Validation:JobDescriptionValidation FieldName="SelectedIndexScope" ValidationStep="UpdatedValue" />
                                                                </Binding.ValidationRules>
                                                            </Binding>
                                                        </dxe:ComboBoxEdit.SelectedIndex>
                                                    </dxe:ComboBoxEdit>

                                                    <Label Grid.Row="7" Grid.Column="0" Content="{DynamicResource JobDescriptionFileName}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>

                                                    <dxe:ComboBoxEdit x:Name="jobDescriptipnFile" Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2" Height="30" Width="300"
                          EditValue="{Binding DataContext.AttachmentList, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                          ItemsSource="{Binding DataContext.AttachmentList,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          DisplayMember="OriginalFileName"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Center"
                          AllowDefaultButton="False"
                          AutoComplete="True"
                          ImmediatePopup="True"  Margin="0 0 0 0"
                          ValidateOnEnterKeyPressed="True" 
                          ValidateOnTextInput="True"
                          IsTextEditable="False" Style="{DynamicResource ComboBoxEditStyle}" 
                          
                          IsEnabled="{Binding IsAcceptEnabled}">

                                                        <dxe:ComboBoxEdit.StyleSettings>
                                                            <dxe:TokenComboBoxStyleSettings  NewTokenPosition="Far" EnableTokenWrapping="True"  />
                                                        </dxe:ComboBoxEdit.StyleSettings>
                                                    </dxe:ComboBoxEdit>

                                                    <Button Content= "{DynamicResource ChooseJobDescriptionFile}"
                    Height="30"  
                    Grid.Row="8" Grid.Column="1"
                    Width="140" 
                    Margin="5,5,20,5"
                    FontSize="16"
                    HorizontalAlignment="Right"   
                   VerticalAlignment="Center" 
                    Command="{Binding DataContext.ChooseFileCommand,RelativeSource={RelativeSource AncestorType={x:Type dxwui:WinUIDialogWindow}}}"                    IsEnabled="{Binding IsAcceptEnabled}"
                   Grid.ColumnSpan="2"/>

                                                    <Label Grid.Row="9" Grid.Column="0" Content="{DynamicResource JobDescriptionInUse}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>
                                                    <dxe:ToggleSwitch x:Name="chkInUse" Grid.Row="9" Grid.Column="1" Height="30" HorizontalAlignment="Left" VerticalAlignment="Center" ContentPlacement="Inside"
                                                       CheckedStateContent="Yes" UncheckedStateContent="No" ToggleSwitchWidth="50" ToggleSwitchHeight="22" IsThreeState="False"                                
                                               IsChecked="{Binding DataContext.InUse,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                          IsReadOnly="{Binding IsReadOnlyField,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">
                                                    </dxe:ToggleSwitch>

                                                    <Label Grid.Row="10" Grid.Column="0" Content="{DynamicResource JobDescriptionIsMandatory}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>
                                                    <StackPanel Orientation="Horizontal" Grid.Row="10" Grid.Column="1">
                                                        <dxe:ToggleSwitch x:Name="chkIsMandatory" Height="30" HorizontalAlignment="Left" VerticalAlignment="Center" ContentPlacement="Inside"
                                                       CheckedStateContent="Yes" UncheckedStateContent="No" ToggleSwitchWidth="50" ToggleSwitchHeight="22" IsThreeState="False"                                
                                               IsChecked="{Binding DataContext.IsMandatory,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                          IsReadOnly="{Binding Source={x:Static HrmApplication:HrmCommon.Instance}, Path=IsPermissionReadOnly,Mode=OneTime,UpdateSourceTrigger=PropertyChanged}">
                                                        </dxe:ToggleSwitch>
                                                        <Image  Source="/Emdep.Geos.Modules.Hrm;component/Assets/Images/INFO1.png" ToolTip= "{DynamicResource JD_IsMandatoryToolTip}"  Margin="10,0,10,5"  VerticalAlignment="Center" Height="20" Width="20" />
                                                    </StackPanel>
                                                    <Label Grid.Row="11" Grid.Column="0" Content="{DynamicResource JobDescriptionIsRemote}" Height="30" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,0,10"/>
                                                    <StackPanel Orientation="Horizontal" Grid.Row="11" Grid.Column="1">
                                                        <dxe:ToggleSwitch x:Name="chkIsRemote" Height="30" HorizontalAlignment="Left" VerticalAlignment="Center" ContentPlacement="Inside"
                                                       CheckedStateContent="Yes" UncheckedStateContent="No" ToggleSwitchWidth="50" ToggleSwitchHeight="22" IsThreeState="False"                                
                                               IsChecked="{Binding DataContext.IsRemote,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}"
                          IsReadOnly="{Binding Source={x:Static HrmApplication:HrmCommon.Instance}, Path=IsPermissionReadOnly,Mode=OneTime,UpdateSourceTrigger=PropertyChanged}">
                                                        </dxe:ToggleSwitch>
                                                        <Image  Source="/Emdep.Geos.Modules.Hrm;component/Assets/Images/INFO1.png" Margin="10,0,10,5" ToolTip= "{DynamicResource JD_IsRemoteToolTip}" VerticalAlignment="Center" Height="20" Width="20" />
                                                    </StackPanel>
                                                </Grid>
                                                <!--</dxlc:LayoutGroup>-->


                                            </dxlc:LayoutGroup>
                                        </dxlc:LayoutControl>

                                    </ScrollViewer>
                                </DataTemplate>
                            </dxlc:GroupBox.MaximizedContentTemplate>
                        </dxlc:GroupBox>

                        <!-- end Information-->

                        
                        <!--Health and Safety-->
            <dxlc:GroupBox x:Name="HealthAndSafety" MaximizeElementVisibility="Visible" Visibility="{Binding DataContext.IsHealthAndSafetyVisible, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}"   Style="{StaticResource HealthGroupBoxStyle}" >
                <dxlc:GroupBox.HeaderTemplate>
                    <DataTemplate>
                        <DockPanel Margin="-5,0,0,0">
                            <TextBlock VerticalAlignment="Center"
                                                   FontWeight="Bold"
                                                   Text="{Binding DataContext.HealthAndSafetyHeader, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" />
                            <TextBlock Width="50"
                                                   Margin="5,1,0,0"
                                                   HorizontalAlignment="Left"
                                                   VerticalAlignment="Center"
                                                   FontWeight="Bold"
                                                   Name="ArticleChangeLogListCount"
                                                   Text="{Binding DataContext.HealthAndSafetyFilesList.Count, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" />
                        </DockPanel>
                    </DataTemplate>
                </dxlc:GroupBox.HeaderTemplate>

                <dxlc:GroupBox.ContentTemplate>
                    <DataTemplate>
                        <Grid Height="0" Margin="0">
                          

                        </Grid>
                    </DataTemplate>
                </dxlc:GroupBox.ContentTemplate>

                <dxlc:GroupBox.MaximizedContentTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="5"/>
                                <RowDefinition Height="40"/>
                            </Grid.RowDefinitions> 
                      
                        <dxg:GridControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="0" 
                                                     ItemsSource="{Binding DataContext.HealthAndSafetyFilesList,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}" SelectionMode="Row"
                                                    SelectedItem="{Binding DataContext.SelectedHealthAndSafetyFile,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}"
                                         ImplyNullLikeEmptyStringWhenFiltering="True" MaxHeight="1500" MaxWidth="2000"
                                                     >

                            <dxg:GridColumn Header="{DynamicResource HealthAndSafetyPlantHeader}" FieldName="Plant" MinWidth="100" Width="180" ReadOnly="True" FixedWidth="True"  UnboundType="String" FilterPopupMode="CheckedList"/>

                            <dxg:GridColumn Header="{DynamicResource HealthAndSafetyTitleHeader}" FieldName="Title" MinWidth="100" Width="200" ReadOnly="True" FixedWidth="True"  UnboundType="String" FilterPopupMode="CheckedList"/>

                            <dxg:GridColumn Header="{DynamicResource HealthAndSafetyRemarksHeader}" FieldName="Remarks" MinWidth="100" Width="*" ReadOnly="True" FixedWidth="True"  UnboundType="String" FilterPopupMode="CheckedList"/>

                            <dxg:GridColumn Header="{DynamicResource HealthAndSafetyFileHeader}" FieldName="File" MinWidth="30" Width="40"  ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button x:Name="btnViewFileHealthAndSafety" ToolTip="View Document" 
                                                                    Command="{Binding DataContext.OpenPDFDocumentCommand,RelativeSource={RelativeSource AncestorType={x:Type dxlc:LayoutGroup}}}"
                                                                    CommandParameter="{Binding RowData.Row}">
                                            <Button.ContentTemplate>
                                                <DataTemplate>
                                                    <Image Height="16" Width="16" Source="/Emdep.Geos.Modules.PCM;component/Assets/Images/pdf-icon.png" />
                                                </DataTemplate>
                                            </Button.ContentTemplate>
                                            <Button.Style>
                                                    <Style TargetType="{x:Type Button}">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Cursor" Value="Hand"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                        </Button>
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding RowData.Row.SavedFileName}" Value="{x:Null}">
                                                    <Setter TargetName="btnViewFileHealthAndSafety" Property="Visibility" Value="Hidden"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RowData.Row.SavedFileName}" Value="{x:Static sys:String.Empty}">
                                                    <Setter TargetName="btnViewFileHealthAndSafety" Property="Visibility" Value="Hidden"/>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>

                            <dxg:GridColumn MinWidth="30" Width="40" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Name="deleteRowItem" ToolTip="{DynamicResource Delete}" 
                                                                
                                                                     Command="{Binding DataContext.DeleteHealthFileCommand,RelativeSource={RelativeSource AncestorType={x:Type dxlc:LayoutGroup}}}">
                                            <Button.ContentTemplate>
                                                <DataTemplate>
                                                    <Image Height="16" Width="16"  Style="{ DynamicResource DeleteStyle}" />
                                                </DataTemplate>
                                            </Button.ContentTemplate>
                                            <Button.Style>
                                                <Style TargetType="{x:Type Button}">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Cursor" Value="Hand"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>


                            <dxg:GridControl.View>
                                <dxg:TableView x:Name="HealthAndSafetyTableView"
                                                           NavigationStyle="Row"
                                                           AllowHorizontalScrollingVirtualization="True" 
                                                           RowDetailsVisibilityMode="Visible"
                                                           ShowHorizontalLines="True"
                                                           ShowColumnHeaders="True"
                                                           VerticalScrollbarVisibility="Auto" 
                                                           HorizontalScrollbarVisibility="Auto"
                                                           AllowFixedColumnMenu="False"
                                                           AutoWidth="False"
                                             
                                                           ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                                           AllowGrouping="True"
                                                           ShowGroupPanel="False"
                                                           ShowIndicator="False" 
                                                           AllowColumnMoving="False"
                                                           >
                                        <dxmvvm:Interaction.Behaviors>
                                            <dxmvvm:EventToCommand Command="{Binding DataContext.EditFileCommand, RelativeSource={RelativeSource AncestorType={x:Type dxlc:LayoutGroup}}}"
                                                                               EventName="MouseDoubleClick"
                                                                               CommandParameter="{Binding ElementName=HealthAndSafetyTableView}">
                                                <dxmvvm:EventToCommand.EventArgsConverter>
                                                    <dx:EventArgsToDataRowConverter />
                                                </dxmvvm:EventToCommand.EventArgsConverter>
                                            </dxmvvm:EventToCommand>

                                        </dxmvvm:Interaction.Behaviors>
                                    </dxg:TableView>
                            </dxg:GridControl.View>
                        </dxg:GridControl>

                            <!--<Add button>-->
                            <Button Content=" " Grid.Row="2" Height="40" Width="40" HorizontalAlignment="Right"
                                   
                                                Command="{Binding DataContext.AddHealthAndSafetyFileCommand, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}"
                                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Grid}}"
                                                
                                                                                               
                                                >
                                <Button.ToolTip>
                                    <TextBlock Text="{DynamicResource ButtonAdd}"/>
                                </Button.ToolTip>
                                <Button.ContentTemplate>
                                    <DataTemplate>
                                        <Image Style="{ DynamicResource AddNewButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                                    </DataTemplate>
                                </Button.ContentTemplate>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </dxlc:GroupBox.MaximizedContentTemplate>
            </dxlc:GroupBox>

            <!--EquipmentAndTools-->
            <dxlc:GroupBox x:Name="EquipmentAndTools" MaximizeElementVisibility="Visible" Visibility="{Binding DataContext.IsEquipmentAndToolsVisible, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}" Style="{StaticResource HealthGroupBoxStyle}" >
                <dxlc:GroupBox.HeaderTemplate>
                    <DataTemplate>
                        <DockPanel Margin="-5,0,0,0">
                            <TextBlock VerticalAlignment="Center"
                                                   FontWeight="Bold"
                                                   Text="{Binding DataContext.EquipmentAndToolsHeader, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" />
                            <TextBlock Width="50"
                                                   Margin="5,1,0,0"
                                                   HorizontalAlignment="Left"
                                                   VerticalAlignment="Center"
                                                   FontWeight="Bold"
                                                   Name="ArticleChangeLogListCount"
                                                   Text="{Binding DataContext.EquipmentAndToolsFilesList.Count, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" />
                        </DockPanel>
                    </DataTemplate>
                </dxlc:GroupBox.HeaderTemplate>

                <dxlc:GroupBox.ContentTemplate>
                    <DataTemplate>
                        <Grid Height="0" Margin="0">


                        </Grid>
                    </DataTemplate>
                </dxlc:GroupBox.ContentTemplate>

                <dxlc:GroupBox.MaximizedContentTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="5"/>
                                <RowDefinition Height="40"/>
                            </Grid.RowDefinitions>
                            <dxg:GridControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="0" 
                                                     ItemsSource="{Binding DataContext.EquipmentAndToolsFilesList,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}" SelectionMode="Row"
                                                    SelectedItem="{Binding DataContext.SelectedEquipmentAndToolsFile,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}"
                                         ImplyNullLikeEmptyStringWhenFiltering="True" MaxHeight="1500" MaxWidth="2000"
                                                     >

                                <dxg:GridColumn Header="{DynamicResource EquipmentAndToolsEquipmentHeader}" FieldName="EquipmentType" MinWidth="100" Width="180" ReadOnly="True" FixedWidth="True"  UnboundType="String" FilterPopupMode="CheckedList"/>

                                <dxg:GridColumn Header="{DynamicResource AddEquipmentAndToolsCategoryHeader}" FieldName="CategoryType" MinWidth="100" Width="250" ReadOnly="True" FixedWidth="True"  UnboundType="String" FilterPopupMode="CheckedList"/>

                                <dxg:GridColumn Header="{DynamicResource EquipmentAndToolsRemarksHeader}" FieldName="Remarks" MinWidth="100" Width="*" ReadOnly="True" FixedWidth="True"  UnboundType="String" FilterPopupMode="CheckedList"/>

                                <dxg:GridColumn Header="{DynamicResource EquipmentAndToolsStartDateHeader}" FieldName="StartDate" MinWidth="100" Width="150" ReadOnly="True" FixedWidth="True"  UnboundType="String" FilterPopupMode="CheckedList"/>

                                <dxg:GridColumn Header="{DynamicResource EquipmentAndToolsEndDateHeader}" FieldName="EndDate" MinWidth="100" Width="150" ReadOnly="True" FixedWidth="True"  UnboundType="String" FilterPopupMode="CheckedList"/>

                                <dxg:GridColumn Header="{DynamicResource EquipmentAndToolsIsMandatoryHeader}" FieldName="IsMandatory" MinWidth="50" Width="100" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList">
                                    <dxg:GridColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,5,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="{x:Type TextBlock}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Path=RowData.Row.IsMandatory}" Value="True">
                                                                <Setter Property="TextBlock.Text" Value="Yes"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Path=RowData.Row.IsMandatory}" Value="False">
                                                                <Setter Property="TextBlock.Text" Value="No"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </dxg:GridColumn.CellTemplate>
                                    <dxg:GridColumn.PrintCellStyle>
                                        <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                            <Setter Property="dxp:ExportSettings.TargetType" Value="Text"  />
                                            <Setter Property="dxp:TextExportSettings.Text" Value="{Binding Path=RowData.Row.IsMandatory}" >
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Path=RowData.Row.IsMandatory}" Value="True">
                                                    <Setter Property="dxp:TextExportSettings.Text" Value="Yes"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=RowData.Row.IsMandatory}" Value="False">
                                                    <Setter Property="dxp:TextExportSettings.Text" Value="No"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </dxg:GridColumn.PrintCellStyle>
                                    <dxg:GridColumn.EditSettings>
                                        <dxe:TextEditSettings>
                                            <dxe:TextEditSettings.DisplayTextConverter>
                                                <dxmvvm:ObjectToObjectConverter>
                                                    <dxmvvm:MapItem Source="True" Target="Yes"/>
                                                    <dxmvvm:MapItem Source="False" Target="No"/>
                                                </dxmvvm:ObjectToObjectConverter>
                                            </dxe:TextEditSettings.DisplayTextConverter>
                                        </dxe:TextEditSettings>
                                    </dxg:GridColumn.EditSettings>
                                </dxg:GridColumn>


                                <dxg:GridColumn Header="{DynamicResource HealthAndSafetyFileHeader}" FieldName="SavedFileName" MinWidth="30" Width="40"  ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList">
                                    <dxg:GridColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button x:Name="btnViewFile" ToolTip="View Document" 
                                                                    Command="{Binding DataContext.OpenEquipmentPDFDocumentCommand,RelativeSource={RelativeSource AncestorType={x:Type dxlc:LayoutGroup}}}"
                                                                    CommandParameter="{Binding RowData.Row}">
                                                <Button.ContentTemplate>
                                                    <DataTemplate>
                                                        <Image Height="16" Width="16" Source="/Emdep.Geos.Modules.PCM;component/Assets/Images/pdf-icon.png" />
                                                    </DataTemplate>
                                                </Button.ContentTemplate>
                                                <Button.Style>
                                                    <Style TargetType="{x:Type Button}">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Cursor" Value="Hand"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding RowData.Row.SavedFileName}" Value="{x:Null}">
                                                    <Setter TargetName="btnViewFile" Property="Visibility" Value="Hidden"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RowData.Row.SavedFileName}" Value="{x:Static sys:String.Empty}">
                                                    <Setter TargetName="btnViewFile" Property="Visibility" Value="Hidden"/>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </dxg:GridColumn.CellTemplate>
                                </dxg:GridColumn>

                                <dxg:GridColumn MinWidth="30" Width="40" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList">
                                    <dxg:GridColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Name="deleteRowItem" ToolTip="{DynamicResource Delete}" 
                                                                     Command="{Binding DataContext.DeleteEquipmentFileCommand,RelativeSource={RelativeSource AncestorType={x:Type dxlc:LayoutGroup}}}">
                                                <Button.ContentTemplate>
                                                    <DataTemplate>
                                                        <Image Height="16" Width="16"  Style="{ DynamicResource DeleteStyle}" />
                                                    </DataTemplate>
                                                </Button.ContentTemplate>
                                                <Button.Style>
                                                    <Style TargetType="{x:Type Button}">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Cursor" Value="Hand"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </DataTemplate>
                                    </dxg:GridColumn.CellTemplate>
                                </dxg:GridColumn>


                                <dxg:GridControl.View>
                                    <dxg:TableView x:Name="HealthAndSafetyTableView"
                                                           NavigationStyle="Row"
                                                           AllowHorizontalScrollingVirtualization="True" 
                                                           RowDetailsVisibilityMode="Visible"
                                                           ShowHorizontalLines="True"
                                                           ShowColumnHeaders="True"
                                               
                                                           VerticalScrollbarVisibility="Auto" 
                                                           HorizontalScrollbarVisibility="Auto"
                                                           AllowFixedColumnMenu="False"
                                                           AutoWidth="False"
                                                           ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                                           AllowGrouping="True"
                                                           ShowGroupPanel="False"
                                                           ShowIndicator="False" 
                                                           AllowColumnMoving="False"
                                                           >
                                        <dxmvvm:Interaction.Behaviors>
                                            <dxmvvm:EventToCommand Command="{Binding DataContext.EditEquipmentFileCommand, RelativeSource={RelativeSource AncestorType={x:Type dxlc:LayoutGroup}}}"
                                                                               EventName="MouseDoubleClick"
                                                                               CommandParameter="{Binding ElementName=HealthAndSafetyTableView}">
                                                <dxmvvm:EventToCommand.EventArgsConverter>
                                                    <dx:EventArgsToDataRowConverter />
                                                </dxmvvm:EventToCommand.EventArgsConverter>
                                            </dxmvvm:EventToCommand>

                                        </dxmvvm:Interaction.Behaviors>
                                    </dxg:TableView>
                                </dxg:GridControl.View>
                            </dxg:GridControl>


                            <!--<Add button>-->
                            <Button Content=" " Grid.Row="2" Height="40" Width="40" HorizontalAlignment="Right"
                                    
                                                Command="{Binding DataContext.AddEquipmentAndToolsFileCommand, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}"
                                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Grid}}">
                                <Button.ToolTip>
                                    <TextBlock Text="{DynamicResource ButtonAdd}"/>
                                </Button.ToolTip>
                                <Button.ContentTemplate>
                                    <DataTemplate>
                                        <Image Style="{ DynamicResource AddNewButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                                    </DataTemplate>
                                </Button.ContentTemplate>
                            </Button>
                        </Grid>
                        
                    </DataTemplate>
                </dxlc:GroupBox.MaximizedContentTemplate>
            </dxlc:GroupBox>


            <!--Change Log Information-->

                        <dxlc:GroupBox x:Name="ChangLog" MaximizeElementVisibility="Visible" Style="{StaticResource myGroupBoxStyle}" >
                            <dxlc:GroupBox.HeaderTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="-5,0,0,0">
                                        <TextBlock VerticalAlignment="Center"
                                                   FontWeight="Bold"
                                                   Text="{DynamicResource ChangeLog}" />
                                        <TextBlock Width="50"
                                                   Margin="5,1,0,0"
                                                   HorizontalAlignment="Left"
                                                   VerticalAlignment="Center"
                                                   FontWeight="Bold"
                                                   Name="ArticleChangeLogListCount"
                                                   Text="{Binding DataContext.ListJobChangeLog.Count, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" />
                                    </DockPanel>
                                </DataTemplate>
                            </dxlc:GroupBox.HeaderTemplate>

                            <dxlc:GroupBox.ContentTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Label Content="{DynamicResource SAMOrdersItemsDateUser}" Grid.Row="0" Grid.Column="0" Margin="3,0,0,0" Padding="2"  FontWeight="Bold" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" Margin="3,0,0,0" Padding="2"  Text="{Binding DataContext.ListJobChangeLog[0].Datetime,
                                        RelativeSource={RelativeSource AncestorType=dxlc:GroupBox},StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}"/>
                                        <Label Content="{DynamicResource SAMOrdersItemsUser}" Grid.Row="1" Grid.Column="0" Margin="3,0,0,0" Padding="2"  FontWeight="Bold"/>
                                        <TextBlock Grid.Row="1" Grid.Column="1" Margin="3,0,0,0" Padding="2" 
                                         Text="{Binding DataContext.ListJobChangeLog[0].User, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" TextWrapping="Wrap"/>
                                        <Label Content="{DynamicResource SAMOrdersItemsAction}" Grid.Row="2" Grid.Column="0" Margin="3,0,0,0" Padding="2"  FontWeight="Bold"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1" Margin="3,0,0,0" Padding="2"
                                         Text="{Binding DataContext.ListJobChangeLog[0].Change, RelativeSource={RelativeSource AncestorType=dxlc:GroupBox}}" 
                                         ToolTip="{Binding DataContext.ListJobChangeLog[0].Change, RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}" 
                                         TextWrapping="WrapWithOverflow" Width="Auto" TextTrimming="CharacterEllipsis" />
                                    </Grid>
                                </DataTemplate>

                            </dxlc:GroupBox.ContentTemplate>

                            <dxlc:GroupBox.MaximizedContentTemplate>
                                <DataTemplate>
                                    <dxg:GridControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                                                     ItemsSource="{Binding DataContext.ListJobChangeLog,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxlc:GroupBox}}}" SelectionMode="Row"
                                                     ImplyNullLikeEmptyStringWhenFiltering="True" MaxHeight="1500" MaxWidth="2000"
                                                     >

                                        <dxg:GridControl.Resources>
                                            <dx:ListOfObjectToIEnumerableOfStringConverter x:Key="listConverter"/>
                                            <dx:EditorStringIdConverter x:Key="EditorStringIdConverter" />
                                            <dx:BoolInverseConverter x:Key="boolInverseConverter"/>
                                            <dx:VisibilityToBoolInverseConverter x:Key="visibilityToBoolInverseConverter"/>
                                            <dx:StringToVisibilityConverter x:Key="stringToVisibilityConverter"/>
                                            <Style x:Key="{dxet:SearchControlThemeKey ResourceKey=FindButtonStyle}" TargetType="{x:Type Button}">
                                                <Setter Property="Margin" Value="0,0,6,0"/>
                                                <Setter Property="dx:FocusHelper2.Focusable" Value="False" />
                                                <Setter Property="Content" Value="{Binding Path=IsTabStop, RelativeSource={RelativeSource Self}, ConverterParameter=LookUpFind, Converter={StaticResource EditorStringIdConverter}}" />
                                            </Style>
                                            <Style x:Key="{dxet:SearchControlThemeKey ResourceKey=CloseButtonStyle}" TargetType="{x:Type Button}">
                                                <Setter Property="dx:FocusHelper2.Focusable" Value="False" />
                                                <Setter Property="Content" Value="{Binding Path=IsTabStop, RelativeSource={RelativeSource Self}, ConverterParameter=LookUpClose, Converter={StaticResource EditorStringIdConverter}}" />
                                            </Style>
                                            <dx:BoolToObjectConverter x:Key="NullValueButtonPlacementConverter">
                                                <dx:BoolToObjectConverter.TrueValue>EditBox</dx:BoolToObjectConverter.TrueValue>
                                                <dx:BoolToObjectConverter.FalseValue>None</dx:BoolToObjectConverter.FalseValue>
                                            </dx:BoolToObjectConverter>
                                            <dx:BooleanToVisibilityConverter x:Key="boolToVisibilityConverter" />

                                            <ControlTemplate x:Key="{dxet:SearchControlThemeKey ResourceKey=Template, IsThemeIndependent=true}">
                                                <ContentControl dx:FocusHelper2.Focusable="False" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="2" />
                                                            <ColumnDefinition Width="45" />
                                                        </Grid.ColumnDefinitions>

                                                        <dxe:ComboBoxEdit MinHeight="22" Grid.Column="0" HorizontalAlignment="Stretch" 
                                                            ImmediatePopup="{Binding Path= SearchControlPropertyProvider.ActualImmediateMRUPopup, RelativeSource={RelativeSource TemplatedParent}}"
                                                            x:Name="editor" AllowDefaultButton="False"
                                                            ShowNullText="{Binding Path=SearchControlPropertyProvider.ActualShowFindButton, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource boolInverseConverter}}"
                                                            dxeh:MVVMFocusBehavior.IsFocused="{Binding Path=IsKeyboardFocusWithin, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}"
                                                            ItemsSource="{Binding Path=MRU, RelativeSource={RelativeSource TemplatedParent}}"
                                                            Margin="0"
                                                            ValidateOnEnterKeyPressed="True"
                                                            IncrementalFiltering="False"
                                                            IsCaseSensitiveSearch="True"
                                                            EditValuePostDelay="{Binding Path=SearchTextPostDelay, RelativeSource={RelativeSource TemplatedParent}}"
                                                            EditValuePostMode="{Binding Path=SearchControlPropertyProvider.ActualPostMode, RelativeSource={RelativeSource TemplatedParent}}"
                                                            EditValue="{Binding Path=SearchText, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                            IsTabStop="{Binding IsEditorTabStop, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}">
                                                            <dxe:ComboBoxEdit.NullText>
                                                                <MultiBinding>
                                                                    <Binding Path="NullText" RelativeSource="{RelativeSource TemplatedParent}" />
                                                                    <Binding Path="IsTabStop" RelativeSource="{RelativeSource Self}" Converter="{StaticResource EditorStringIdConverter}" ConverterParameter="LookUpSearch" />
                                                                    <MultiBinding.Converter>
                                                                        <dx:SearchControlNullTextConverter />
                                                                    </MultiBinding.Converter>
                                                                </MultiBinding>
                                                            </dxe:ComboBoxEdit.NullText>

                                                            <dxe:ComboBoxEdit.StyleSettings>
                                                                <dxe:SearchControlStyleSettings/>
                                                            </dxe:ComboBoxEdit.StyleSettings>

                                                            <dxe:ComboBoxEdit.Buttons>
                                                                <dxe:ButtonInfo GlyphKind="Custom" x:Name="buttonClear"
                                                                                Command="{Binding Path=SearchControlPropertyProvider.ClearSearchTextCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                                                                Visibility="{Binding Path=SearchControlPropertyProvider.ActualShowClearButton, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource boolToVisibilityConverter}}">
                                                                    <dxe:ButtonInfo.Template>
                                                                        <DataTemplate>
                                                                            <dxe:ButtonClose Command="{Binding Command}" Visibility="{Binding Visibility}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0" />
                                                                        </DataTemplate>
                                                                    </dxe:ButtonInfo.Template>
                                                                </dxe:ButtonInfo>
                                                                <dxe:ButtonInfo GlyphKind="DropDown"
                                                                                Command="{Binding Path=OpenPopupCommand, ElementName=editor}"
                                                                                Visibility="{Binding Path=ShowMRUButton, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource boolToVisibilityConverter}}" />
                                                            </dxe:ComboBoxEdit.Buttons>
                                                        </dxe:ComboBoxEdit>

                                                        <Button Name="ExporttoExcel" ToolTip="{DynamicResource ExporttoExcel}"  Grid.Column="3"
                                                                Command="{Binding DataContext.ExportToExcelCommand,RelativeSource={RelativeSource AncestorType={x:Type dxwui:WinUIDialogWindow}}}"
                                                                CommandParameter="{Binding ElementName=ChangeLogTableView}" Height="30" Width="30" >
                                                            <Button.ContentTemplate>
                                                                <DataTemplate>
                                                                    <Image Style="{DynamicResource PCMExportToExcelButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                                                                </DataTemplate>

                                                            </Button.ContentTemplate>
                                                            <Button.Style>
                                                                <Style TargetType="{x:Type Button}">
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Cursor" Value="Hand"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                    </Grid>
                                                </ContentControl>
                                            </ControlTemplate>
                                        </dxg:GridControl.Resources>

                                        <dxg:GridColumn Header="{DynamicResource UserL}" FieldName="User" MinWidth="100" Width="180" ReadOnly="True" FixedWidth="True"  UnboundType="String" FilterPopupMode="CheckedList">
                                        </dxg:GridColumn>
                                        <dxg:GridColumn Header="{DynamicResource DateUserL}" FieldName="Datetime"  MinWidth="100" Width="150" MaxWidth="150"  ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList">
                                            <dxg:GridColumn.EditSettings>
                                                <dxe:DateEditSettings DisplayFormat="g" Name="changeLogTime" />
                                            </dxg:GridColumn.EditSettings>
                                        </dxg:GridColumn>
                                        <dxg:GridColumn Header="{DynamicResource ActionL}" FieldName="Change"  MinWidth="100" Width="*" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList">
                                            <dxg:GridColumn.CellToolTipBinding>
                                                <Binding Path="RowData.Row.change" Mode="OneWay">
                                                </Binding>
                                            </dxg:GridColumn.CellToolTipBinding>

                                            <dxg:GridColumn.CellToolTipTemplate>
                                                <DataTemplate>
                                                    <dxe:TextEdit BorderThickness="0" Text="{Binding Path=., Mode=OneWay}" 
                                                                      HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                                                      IsReadOnly="True" Width="auto" MaxWidth="100000">
                                                    </dxe:TextEdit>
                                                </DataTemplate>
                                            </dxg:GridColumn.CellToolTipTemplate>

                                        </dxg:GridColumn>

                                        <dxg:GridControl.View>
                                            <dxg:TableView x:Name="ChangeLogTableView"
                                                           NavigationStyle="Row"
                                                           AllowHorizontalScrollingVirtualization="True" 
                                                           RowDetailsVisibilityMode="Visible"
                                                           ShowHorizontalLines="True"
                                                           ShowColumnHeaders="True"
                                                           VerticalScrollbarVisibility="Auto" 
                                                           HorizontalScrollbarVisibility="Auto"
                                                           AllowFixedColumnMenu="False"
                                                           AutoWidth="False"
                                                           ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                                           AllowGrouping="True"
                                                           ShowGroupPanel="False"
                                                           ShowIndicator="False" 
                                                           AllowColumnMoving="False"
                                                           >
                                            </dxg:TableView>
                                        </dxg:GridControl.View>
                                    </dxg:GridControl>
                                </DataTemplate>
                            </dxlc:GroupBox.MaximizedContentTemplate>
                        </dxlc:GroupBox>
                        <!--  end changelog-->
                    </dxlc:FlowLayoutControl>
              

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                    HorizontalAlignment="Center" 
                    Orientation="Horizontal"
                    Margin="15,9,0,0"
                    Width="250">

            <Button Content="{DynamicResource JobDescriptionAcceptButton}"
                    Command="{Binding AddJobDescriptionButtonCommand}"
                    Height="30"
                    Width="100"
                    Margin="0,0,10,0"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Bottom" 
                    IsEnabled="{Binding IsAcceptEnabled}"/>


            <Button Content="{DynamicResource JobDescriptionCancelButton}"
                    Command="{Binding AddJobDescriptionsViewCancelButtonCommand}" 
                    Height="30"
                    Width="100"
                    Margin="10,0,0,0"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Bottom" />
        </StackPanel>

        <i:Interaction.Triggers>
            <i:EventTrigger EventName="KeyDown">
                <behaviors:InvokeDelegateCommandAction Command="{Binding CommandTextInput}" CommandParameter="{Binding Path=InvokeParameter, RelativeSource={RelativeSource Self}}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
    </Grid>
    <!--</ScrollViewer>-->

</dxwui:WinUIDialogWindow>