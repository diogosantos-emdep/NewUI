<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.Hrm.Views.AddHealthAndSafetyFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:HrmApplication="clr-namespace:Emdep.Geos.Modules.Hrm"
             xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Hrm.Views"
             mc:Ignorable="d" 
                          Style="{DynamicResource WinUIDialogWindowStyle}"
             Title="{Binding WindowHeader}"
             TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
             d:DesignHeight="300" d:DesignWidth="300">

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding CancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="90"/>
            </Grid.RowDefinitions>
            
            <!--Plant-->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource AddHealthAndSafetyPlantHeader}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center" />

            <dxe:ComboBoxEdit Style="{DynamicResource ComboBoxEditStyle}" Grid.Column="2"   Grid.Row="0"                 
                                      ItemsSource="{Binding PlantList}"
                                      DisplayMember="ShortName"
                                      EditValue="{Binding SelectedPlantList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <dxe:ComboBoxEdit.StyleSettings>
                    <dxe:CheckedComboBoxStyleSettings />
                </dxe:ComboBoxEdit.StyleSettings>
                <dxmvvm:Interaction.Triggers>
                    <dxmvvm:EventToCommand EventName="PopupClosed"
                                               Command="{Binding DataContext.PlantOwnerPopupClosedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                               PassEventArgsToCommand="True"/>
                </dxmvvm:Interaction.Triggers>
            </dxe:ComboBoxEdit>

            <!--Title-->

            <TextBlock Grid.Row="2" Grid.Column="0"  Text="{DynamicResource AddHealthAndSafetyTitleHeader}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center"  />
            <dxe:TextEdit Grid.Row="2" Grid.Column="2"
                      Text="{Binding HealthAndSafetyTitle,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" >
                <dxe:TextEdit.EditValue>
                    <Binding Path="HealthAndSafetyTitle" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" ValidatesOnDataErrors="True">
                        <Binding.ValidationRules>
                            <Validation:AddEditHealthAndSafetyValidation FieldName="HealthAndSafetyTitle" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                        </Binding.ValidationRules>
                    </Binding>
                </dxe:TextEdit.EditValue>
            </dxe:TextEdit>
            
            <!--Choose File-->
            <dxe:ComboBoxEdit Grid.Row="4" Grid.Column="2"
                          VerticalContentAlignment="Center"
                          EditValue="{Binding DataContext.AttachmentObjectList,RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow,Mode=FindAncestor},Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                          ItemsSource="{Binding DataContext.HealthAndSafetyAttachmentList,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow,Mode=FindAncestor}}"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                          DisplayMember="SavedFileName"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          AllowDefaultButton="False"
                          AutoComplete="True"
                          AllowDrop="True"
                         PreviewDragOver="ProductFileComboBoxEdit_PreviewDragOver"  PreviewDrop="ProductFileComboBoxEdit_PreviewDrop"
                          ImmediatePopup="True"
                          ValidateOnEnterKeyPressed="True" 
                          ValidateOnTextInput="True"
                          IsTextEditable="False" Style="{DynamicResource ComboBoxEditStyle}" 
                         >
                <!--SelectedItem="{Binding SelectedProductTypeFile,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"-->

                <dxe:ComboBoxEdit.StyleSettings>
                    <dxe:TokenComboBoxStyleSettings  NewTokenPosition="Far" EnableTokenWrapping="True"  />
                </dxe:ComboBoxEdit.StyleSettings>

                <!--<dxe:ComboBoxEdit.EditValue>
                <Binding Path="DataContext.ImageName" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" ValidatesOnDataErrors="True">
                    <Binding.ValidationRules>
                        <Validation:AddEditCategoryValidation FieldName="ImageName" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </dxe:ComboBoxEdit.EditValue>-->


                <!--<dxe:ComboBoxEdit.SelectedItem>
                    <Binding Path="SelectedHealthAndSafetyFile" ValidatesOnDataErrors="True"  UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                        <Binding.ValidationRules>
                            <Validation:AddEditHealthAndSafetyValidation FieldName="SelectedHealthAndSafetyFile" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                        </Binding.ValidationRules>
                    </Binding>
                </dxe:ComboBoxEdit.SelectedItem>-->

            </dxe:ComboBoxEdit>


            <!--ChooseFile-->
            <Button Grid.Row="6" Grid.Column="2" Content="{DynamicResource ChooseFile}" Width="115" HorizontalAlignment="Right" Command="{Binding ChooseFileActionCommand}"/>


            <!--Remarks-->
            <TextBlock Grid.Row="8" Grid.Column="0" Text="{DynamicResource AddHealthAndSafetyRemarksHeader}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Top" Margin="0,5,0,0" />
            <dxe:TextEdit Grid.Row="8" Grid.Column="2"  Text="{Binding HealthAndSafetyRemarks}"  VerticalContentAlignment="Top" TextWrapping="Wrap" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AcceptsReturn="True"/>


        </Grid>

        <Grid Grid.Row="2" ShowGridLines="False" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button  HorizontalAlignment="Right"
                     Style="{DynamicResource Button16Style}"
                     VerticalAlignment="Top"
                     Content="{DynamicResource AddCatalogueAcceptButton}"
                     Command="{Binding AcceptFileActionCommand}"
                     Height="30"
                     Width="100" />

            <Button Grid.Row="10" Grid.Column="2"
                                                HorizontalAlignment="Left"
                                                Style="{DynamicResource Button16Style}"
                                                VerticalAlignment="Top"
                                                Content="{DynamicResource AddCatalogueCancelButton}"
                                                Command="{Binding CancelButtonCommand }"
                                                Height="30"
                                                Width="100" />
        </Grid>
    </Grid>
</dxwui:WinUIDialogWindow>
