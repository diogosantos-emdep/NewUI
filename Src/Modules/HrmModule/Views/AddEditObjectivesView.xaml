<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.Hrm.Views.AddEditObjectivesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Hrm.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:Interactive="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:Converter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"                          
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Style="{DynamicResource WinUIDialogWindowStyle}"
             Title="{Binding WindowHeader}"
             TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}">

    <dxwui:WinUIDialogWindow.Resources>
        <dxmvvm:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Converter:GroupboxBackroundColorConvertor x:Key="GroupboxBackroundColorConvertor" />
        <Converter:ForegroundColorConverter x:Key="ForegroundColorConverter" />
    </dxwui:WinUIDialogWindow.Resources>

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding EscapeButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="155"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!--Code-->
        <TextBlock Grid.Row="0" Grid.Column="0"  Text="{DynamicResource ObjectivesCode}" Style="{StaticResource TextBlock16Style}" VerticalAlignment="Center"  />
        <dxe:TextEdit Grid.Row="0" Grid.Column="2" IsReadOnly="True" TabIndex="0"
                      Text="{Binding Code}" VerticalContentAlignment="Center">
        </dxe:TextEdit>

        <!--Description-->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource ObjectivesDescription}" Style="{StaticResource TextBlock16Style}" VerticalAlignment="Center" />
        <dxe:TextEdit Text="{Binding Description}" NullText="{DynamicResource ObjectivesDescription}" Grid.Row="2" Grid.Column="2" VerticalContentAlignment="Top" VerticalAlignment="Top"
                      HorizontalAlignment="Left" TextWrapping="Wrap" MaxLength="400" Height="60" Width="350" TabIndex="1"
                      AllowNullInput="True" InvalidValueBehavior="AllowLeaveEditor" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AcceptsReturn="True">
            <dxe:TextEdit.EditValue>
                <Binding Path="Description" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <Binding.ValidationRules>
                        <Validation:ObjectiveValidation FieldName="Description" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </dxe:TextEdit.EditValue>
        </dxe:TextEdit>

        <!--JD Code-->
        <TextBlock Grid.Row="4" Grid.Column="0"  Text="{DynamicResource ObjectivesJDcode}" Style="{StaticResource TextBlock16Style}" VerticalAlignment="Center" />
        <dxe:ComboBoxEdit  Name="PART_Editor1" Grid.Row="4" Grid.Column="2" HorizontalAlignment="Left" TabIndex="2"
                           Width="350" Height="30" VerticalAlignment="Center"  
                           ApplyItemTemplateToSelectedItem="True"
                           IsSynchronizedWithCurrentItem="True"
                           AllowUpdateTwoWayBoundPropertiesOnSynchronization="True"
                           IsReadOnly="False"
                           ItemsSource="{Binding JDCodeList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"                         
                           ValueMember="IdJobDescription"  DisplayMember="JobDescriptionTitleAndCode" AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True">
            <!--<dxe:ComboBoxEdit.Style>
                <Style TargetType="{x:Type dxe:ComboBoxEdit}" >
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Row.JobDescriptionInUse}" Value="No">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Foreground" Value="Gray"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Row.JobDescriptionInUse}" Value="Yes">
                            <Setter Property="IsEnabled" Value="True"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </dxe:ComboBoxEdit.Style>-->

            <dxe:ComboBoxEdit.ItemContainerStyle>
                <Style TargetType="{x:Type dxe:ComboBoxEditItem}" >
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding JobDescriptionInUse}" Value="0">
                            <Setter Property="Background" Value="Gray"/>
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </dxe:ComboBoxEdit.ItemContainerStyle>
            <dxe:ComboBoxEdit.SelectedItem>
                <Binding Path="DataContext.SelectedJobDescription" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <Binding.ValidationRules>
                        <Validation:ObjectiveValidation FieldName="SelectedJobDescription" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </dxe:ComboBoxEdit.SelectedItem>

        </dxe:ComboBoxEdit>

        <!--InUse-->
        <TextBlock Grid.Row="6" Grid.Column="0" Grid.RowSpan="2" Text="{DynamicResource ObjectivesInUse}" Style="{StaticResource TextBlock16Style}" VerticalAlignment="Center" TextWrapping="Wrap"/>
        <dxe:ToggleSwitch x:Name="InUse" 
                          Grid.Row="6" Grid.Column="2"  
                          HorizontalAlignment="Left" VerticalAlignment="Center" ContentPlacement="Inside" TabIndex="3"
                          CheckedStateContent="Yes" UncheckedStateContent="No" 
                          ToggleSwitchWidth="50" ToggleSwitchHeight="22" 
                          IsThreeState="False"
                          IsChecked="{Binding InUse,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
        </dxe:ToggleSwitch>

        <!--Accept and Cancel Buttons-->
        <Grid Grid.Row="8" ShowGridLines="False" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--[GEOS2-3943][rdixit][22.11.2022] Added IsEnabled Property-->
            <Button  HorizontalAlignment="Right"
                     Style="{DynamicResource Button16Style}"
                     VerticalAlignment="Top"
                     Content="{DynamicResource AddEditObjectivesAcceptButton}"
                     Command="{Binding AcceptObjectiveActionCommand}"
                     IsEnabled="{Binding Source={x:Static GeosApplication:GeosApplication.Instance}, Path=IsAdminPermissionForHRM, Mode=TwoWay ,UpdateSourceTrigger=PropertyChanged}"
                     TabIndex="4"
                     Height="30"
                     Width="100" />

            <Button Grid.Row="10" Grid.Column="2"
                    HorizontalAlignment="Left"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Top"
                    Content="{DynamicResource AddEditObjectivesCancelButton}"
                    Command="{Binding CancelObjectiveButtonCommand }"
                    TabIndex="5"
                    Height="30"
                    Width="100" />
        </Grid>
    </Grid>
</dxwui:WinUIDialogWindow>
