<UserControl x:Class="Emdep.Geos.Modules.Hrm.Views.ContractSituationSplitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:Validation="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:HrmApplication="clr-namespace:Emdep.Geos.Modules.Hrm" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:UIConverter ="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <DataTemplate x:Key="headerTemplate" >
            <TextBlock Text="{Binding TaskOfferCode,UpdateSourceTrigger=PropertyChanged}" />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="400"/>
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <dxg:GridControl 
            Name="ContractSituationGrid" 
            Grid.Row="1"
            ItemsSource="{Binding EmployeeContractSituationList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            VerticalAlignment="Stretch" 
            HorizontalAlignment="Stretch"
            Height="400"
            SelectionMode="Row"
            ImplyNullLikeEmptyStringWhenFiltering="True">
            <dxg:GridControl.Columns>

                <!--File Image="-->
                <dxg:GridColumn FieldName="ContractSituationFileName"
                                Header="{DynamicResource EmpEducationFileName}" 
                                FixedWidth="True" 
                                Width="40" 
                                AllowMoving="False" 
                                AllowColumnFiltering="false" >
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <Button Name="btncontractViewDoc" 
                                    ToolTip="View Document" 
                                    Command="{Binding DataContext.EmployeeDocumentViewCommand,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding RowData.Row}">
                                <Button.ContentTemplate>
                                    <DataTemplate>
                                        <Image Height="16" Width="16" Source="/Emdep.Geos.Modules.Hrm;component/Assets/Images/pdf-icon.png" />
                                    </DataTemplate>
                                </Button.ContentTemplate>
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Cursor" Value="Hand"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding RowData.Row.ContractSituationFileName}" Value="{x:Null}">
                                    <Setter TargetName="btncontractViewDoc" Property="Visibility" Value="Hidden" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding RowData.Row.ContractSituationFileName}" Value="{x:Static sys:String.Empty}">
                                    <Setter TargetName="btncontractViewDoc" Property="Visibility" Value="Hidden" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>

                <!-- File name and file path-->
                <dxg:GridColumn 
                    FieldName="ContractSituationFileName"
                    Header=" File Name"
                    ReadOnly="True" 
                    Width="320" 
                    FilterPopupMode="CheckedList"/>

                <!-- File Delete button image-->
                <dxg:GridColumn 
                    Width="40" 
                    FieldName="" 
                    FixedWidth="True" 
                    AllowMoving="False" 
                    AllowColumnFiltering="false" >
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <Button Name="deleteContactRow" ToolTip="Delete" Command="{Binding DataContext.DeleteContractSituationInformationCommand,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding RowData.Row}"  IsEnabled="{Binding DataContext.IsAcceptEnabled,RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <Button.ContentTemplate>
                                    <DataTemplate>
                                        <Image Height="16" Width="16" Source="/Emdep.Geos.Modules.Hrm;component/Assets/Images/Delete.png" />
                                    </DataTemplate>
                                </Button.ContentTemplate>
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Cursor" Value="Hand"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
            </dxg:GridControl.Columns>

            <!-- table view-->
            <dxg:GridControl.View>
                <dxg:TableView Name="EmpContarctSituationView"  
                               VerticalScrollbarVisibility="Auto" 
                               HorizontalScrollbarVisibility="Auto"
                               AllowRowFixing="Top"
                               AllowDragDrop="True"
                               ShowFixRowButton="Never"
                               FixedTopRows="{Binding SelectedRowEmployeeContractSituationList}"
                               ShowDragDropHint = "True"
                               ShowTargetInfoInDragDropHint = "True"
                               NavigationStyle="Row"
                               AutoWidth="False"
                               ShowGroupPanel="False" 
                               ShowIndicator="False">

                    <dxmvvm:Interaction.Behaviors>
                        <dxmvvm:EventToCommand 
                                Command="{Binding OnDragRecordOverCommand}"
                                EventName="DragRecordOver"
                                PassEventArgsToCommand="True" />
                        <dxmvvm:EventToCommand 
                                Command="{Binding OnDropRecordCommand}"
                                EventName="DropRecord"
                                PassEventArgsToCommand="True" />
                        
                    </dxmvvm:Interaction.Behaviors>
                    
                </dxg:TableView>
            </dxg:GridControl.View>
        </dxg:GridControl>
        
        <StackPanel Grid.Row="2"
                    Margin="0,0,0,0"
                    Width="250"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">

            <Button Height="30"
                    Width="100"
                    Margin="20,0,0,0"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Right"
                    Command="{Binding MergeAcceptButtonCommand}"
                    Content="{DynamicResource EditTargetAndForecastViewAccept}"
                    Style="{DynamicResource Button16Style}" />
            <Button Height="30"
                    Width="100"
                    Margin="20,0,0,0"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Right"
                    Command="{Binding MergeCancelButtonCommand}"
                    Content="{DynamicResource EditTargetAndForecastViewCancel}"
                    Style="{DynamicResource Button16Style}" />
        </StackPanel>
    </Grid>

</UserControl>
