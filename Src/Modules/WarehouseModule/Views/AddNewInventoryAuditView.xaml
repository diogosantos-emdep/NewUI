<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.Warehouse.Views.AddNewInventoryAuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Warehouse.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:WarehouseCommon ="clr-namespace:Emdep.Geos.Modules.Warehouse"
             xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"  
                         Title="{Binding WindowHeader,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                         TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
                         Style="{DynamicResource WinUIDialogWindowStyle}">

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding AddNewInventoryAuditViewCancelCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="20"/>
            <!--<RowDefinition Height="Auto"/>--> <!--40-->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/> <!--120-->
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/> <!--300-->
            <ColumnDefinition Width="Auto"/> <!--120-->
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/> <!--300-->
        </Grid.ColumnDefinitions>

        <!--Name-->
        <Label Grid.Row="0" Grid.Column="0" Content="{DynamicResource EditInventoryAuditName}" Height="Auto" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2"/>
        
        <Label Grid.Row="0" Grid.Column="1" Content="{DynamicResource ColonSymbol}" Height="Auto" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2"/>

        <!--<TextBox  Grid.Row="0" Grid.Column="1" Width="300" Height="30" TextWrapping="Wrap" 
                 VerticalAlignment="Center" HorizontalAlignment="Left" 
                 Text="{Binding Name,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" >
        </TextBox>-->

        <dxe:TextEdit Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Margin="10,2,2,2" Width="320" 
                      Text="{Binding Name,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" 
                      TextWrapping="Wrap" 
                      VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Left" VerticalContentAlignment="Top">
            <dxe:TextEdit.EditValue>
                <Binding Path="Name"  ValidatesOnDataErrors="True">
                    <Binding.ValidationRules>
                        <Validation:WarehouseInventoryAuditValidation FieldName="Name" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </dxe:TextEdit.EditValue>
        </dxe:TextEdit>
        
        <!--Start Date-->

        <Label Grid.Row="0" Grid.Column="3" Content="{DynamicResource EditInventoryAuditStartDate}" Height="Auto" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,2,2,2"/>

        <Label Grid.Row="0" Grid.Column="4" Content="{DynamicResource ColonSymbol}" Height="Auto" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2"/>

        <dxe:DateEdit Margin="10,2,2,2" Grid.Row="0" Grid.Column="5" Width="120" Height="Auto" 
                      HorizontalAlignment="Left" VerticalAlignment="Center" 
                      Mask="dd/MM/yyyy" MaskType="DateTimeAdvancingCaret" MaskUseAsDisplayFormat="True">
            <dxe:DateEdit.EditValue>
                <Binding Path="StartDate" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" ValidatesOnDataErrors="True">
                    <Binding.ValidationRules>
                        <Validation:WarehouseInventoryAuditValidation FieldName="StartDate" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </dxe:DateEdit.EditValue>
            <dxmvvm:Interaction.Behaviors>
                <dxmvvm:EventToCommand EventName="EditValueChanging" Command="{Binding DataContext.OnDateEditValueChangingCommand,RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}" PassEventArgsToCommand="True"/>
            </dxmvvm:Interaction.Behaviors>
        </dxe:DateEdit>

        <Label Grid.Row="1" Grid.Column="3" Content="{DynamicResource EditInventoryAuditEndDate}" Height="Auto" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,2,2,2"  />
        
        <Label Grid.Row="1" Grid.Column="4" Content="{DynamicResource ColonSymbol}" Height="Auto" FontSize="16"
               HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2"/>

        <dxe:DateEdit Margin="10,2,2,2" Grid.Row="1" Grid.Column="5" Width="120" Height="Auto" 
                      HorizontalAlignment="Left" VerticalAlignment="Center" MinValue="{Binding MinEndDate}"
                      Mask="dd/MM/yyyy" MaskType="DateTimeAdvancingCaret" MaskUseAsDisplayFormat="True">
            <dxe:DateEdit.EditValue>
                <Binding Path="EndDate" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" ValidatesOnDataErrors="True">
                </Binding>
            </dxe:DateEdit.EditValue>
          
        </dxe:DateEdit>

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="6"
                    HorizontalAlignment="Center" VerticalAlignment="Stretch"
                    Orientation="Horizontal"
                    Margin="0,0,0,0"
                    Width="Auto">

            <Button Content="Accept"
                    Command="{Binding AddNewInventoryAuditViewAcceptButtonCommand}"
                    Height="30"
                    Width="100"
                    Margin="0,0,10,0"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Center" 
                    />

            <Button Content="Cancel"
                    Command="{Binding AddNewInventoryAuditViewCancelCommand}" 
                    Height="30"
                    Width="100"
                    Margin="10,0,0,0"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Center"  />
        </StackPanel>

    </Grid>
</dxwui:WinUIDialogWindow>
