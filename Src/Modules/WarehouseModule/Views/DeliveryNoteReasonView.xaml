<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.Warehouse.Views.DeliveryNoteReasonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Warehouse.Views"
                         Title="{Binding WindowHeader}"
                           Style="{DynamicResource WinUIDialogWindowStyle}"
                         
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CancelButtonCommand}" />
    </dxwui:WinUIDialogWindow.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <dxlc:LayoutGroup Orientation="Vertical">
                <RadioButton GroupName="Reason" IsChecked="{Binding IsQualityReasonSelected,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="5">
                    <TextBlock Margin="5,-2,0,0" Text="Quality"/>
                </RadioButton>
                <RadioButton GroupName="Reason" IsChecked="{Binding IsMissingReasonSelected,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="5">
                    <TextBlock Margin="5,-2,0,0" Text="Missing material from supplier"/>
                   </RadioButton>
                <RadioButton GroupName="Reason" IsChecked="{Binding IsPendingReasonSelected,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="5">
                    <TextBlock Margin="5,-2,0,0" Text="Pending of adjustments"/>
                </RadioButton>
                <RadioButton GroupName="Reason" IsChecked="{Binding IsOtherReasonSelected,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="5">
                    <TextBlock Margin="5,-2,0,0" Text="Others"/>
                    </RadioButton>
                <dxe:TextEdit Width="300" Text="{Binding OtherReasonComment,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="5" TextWrapping="Wrap" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"  Visibility="{Binding IsOtherSelected,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                    <dxe:TextEdit.Style>
                        <Style TargetType="dxe:TextEdit">
                            <Setter Property="Height" Value="60"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsOtherReasonSelected,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Value="False">
                                    <Setter Property="Height" Value="0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </dxe:TextEdit.Style>
                </dxe:TextEdit>
            </dxlc:LayoutGroup>
           
        </Grid>
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center" 
                    Orientation="Horizontal"
                    Margin="20,5,0,0"
                    Width="290">

            <Button Content="{DynamicResource Accept}"
                            Command="{Binding AcceptButtonCommand}" CommandParameter="{Binding ElementName=DeliveryNoteTableView}"
                            Height="30"
                            Width="100"
                            Margin="0,0,10,0"
                            VerticalAlignment="Bottom"                   
                             />

            <Button Content="{DynamicResource Cancel}"
                            Command="{Binding CancelButtonCommand}" 
                            Height="30"
                            Width="100"
                            Margin="10,0,0,0"
                            VerticalAlignment="Bottom"
                            />
        </StackPanel>
    </Grid>
</dxwui:WinUIDialogWindow>
