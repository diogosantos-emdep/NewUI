<UserControl x:Class="Emdep.Geos.Modules.Warehouse.Views.WorkOrdersPreparationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Warehouse.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxwuin="http://schemas.devexpress.com/winfx/2008/xaml/windowsui/navigation" 
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dxnav="http://schemas.devexpress.com/winfx/2008/xaml/navigation"
             xmlns:Converters="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:ViewModels="clr-namespace:Emdep.Geos.Modules.Warehouse.ViewModels"
             xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:WarehouseCommon ="clr-namespace:Emdep.Geos.Modules.Warehouse"
             xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             xmlns:WmsApplication="clr-namespace:Emdep.Geos.Modules.Warehouse"
             xmlns:Interactive="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:Behaviors="clr-namespace:Emdep.Geos.UI.Behaviors;assembly=Emdep.Geos.UI.Common"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/charts"
             Style="{DynamicResource UserControlStyle}" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">

    <UserControl.Resources>

        <Converters:ImageConverter x:Key="ImageConverter"/>
        <Converters:CellColorConverterForPoDate x:Key="CellColorConverterForPoDate"/>
        <Converters:CellBackroundColorOtTypeConvertor x:Key="CellBackroundColorOtTypeConvertor"/>

        <Style x:Key="HideButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border BorderThickness="0" Background="{TemplateBinding Background}">
                            <Grid>
                                <Image Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Source="/Emdep.Geos.Modules.Warehouse;component/Assets/Images/ArrowRight.png"
                                   Visibility="{TemplateBinding IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter Inverse=True}}"/>
                                <Image Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Source="/Emdep.Geos.Modules.Warehouse;component/Assets/Images/ArrowLeft.png"
                                   Visibility="{TemplateBinding IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter}}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LeftPanelTileItemStyle" TargetType="{x:Type dxnav:TileBarItem}">
            <Setter Property="Foreground" Value="{Binding BackColor,Converter={Converters:ForegroundColorConverter}}"/>
            <Setter Property="BorderBrush" Value="#FFD6D6D6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{Binding BackColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Height" Value="{Binding Height}"/>
            <Setter Property="Width" Value="{Binding Width}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="SelectionPadding" Value="0" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <TextBlock Text="{Binding EntitiesCount,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" FontSize="36" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-5,8,0" Opacity="0.6" Visibility="{Binding EntitiesCountVisibility}"/>
                            <TextBlock HorizontalAlignment="Left" VerticalAlignment="Bottom" Text="{Binding Caption}" FontSize="13" Margin="6,0,0,3"/>
                            <Image HorizontalAlignment="Left" VerticalAlignment="Top" Source="{Binding ImageUri, Converter={StaticResource ImageConverter}}" dxb:ImageColorizer.IsEnabled="True"
                               dxb:ImageColorizer.Color="{Binding Path=(TextElement.Foreground).Color, RelativeSource={RelativeSource Self}}"
                               Stretch="None" Margin="6,6,0,0"/>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderThickness" Value="5"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource TileBarItemColorStyle}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Content="{DynamicResource WorkOrdersPreparationHeader}" Style="{StaticResource LabelTitleStyle}" Grid.Row="0" />

            <DockPanel Grid.Row="0" Grid.Column="1">

                <Grid HorizontalAlignment="Right" Width="Auto" Height="45" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="45"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Warehouse combobox section -->
                    <DockPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" >
                        <Image Style="{DynamicResource WarehouseImageStyle}" Width="24" Height="24" Margin="7,0,3,0" />
                        <TextBlock Text="{DynamicResource Warehouse}"  FontWeight="Bold" Margin="5,2,5,0" VerticalAlignment="Bottom" />
                    </DockPanel>

                    <dxe:ComboBoxEdit Name="CbeWareHouse" Style="{DynamicResource ComboBoxEditStyle}" Grid.Column="1" Width="150" Height="30"
                                      Margin="5,0,5,5" VerticalAlignment="Bottom" IsTextEditable="False"
                                      ItemsSource ="{Binding Source={x:Static WarehouseCommon:WarehouseCommon.Instance}, Path=WarehouseList ,UpdateSourceTrigger=PropertyChanged , Mode=TwoWay}"
                                      SelectedItem="{Binding Source={x:Static WarehouseCommon:WarehouseCommon.Instance}, Path=Selectedwarehouse,UpdateSourceTrigger=PropertyChanged}"
                                      DisplayMember="Name" >
                        <dxmvvm:Interaction.Triggers>
                            <dxmvvm:EventToCommand EventName="EditValueChanged"
                                                   Command="{Binding CommandWarehouseEditValueChanged}"
                                                   CommandParameter="{Binding ElementName=CbeWareHouse,Path=SelectedItem}"/>
                        </dxmvvm:Interaction.Triggers>
                    </dxe:ComboBoxEdit>

                    <!-- Refresh Button -->
                    <Button Content=" " Grid.Row="0" Grid.Column="5" Height="40" Width="40" HorizontalAlignment="Right"
                            Command="{Binding RefreshWorkOrderViewCommand}"
                            CommandParameter="{Binding ElementName=PendingWorkOrderTableView}" Margin="0,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource ButtonRefresh}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{ DynamicResource WareHouseRefreshButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!-- Period -->
                    <DockPanel Grid.Column="2" Grid.Row="0" HorizontalAlignment="Right"   VerticalAlignment="Center" 
                                    Margin="15,0,0,0" >
                        <TextBlock Text="{Binding FromDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" 
                                   Margin="0,0,5,0"/>
                        <TextBlock Text=" - " HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" 
                                   Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding ToDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" 
                                   Margin="0,0,5,0"/>
                    </DockPanel>

                    <!--Calender Icon-->
                    <dxwui:AppBar x:Name="Appbar" HideMode="AlwaysVisible" Background="Transparent" Grid.Column="3"
                                  IsSticky="True" HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  IsOpen="True" IsExitButtonEnabled="False" Margin="0,-15,5,0">

                        <dxwui:AppBarButton x:Name="appbarbutton" ToolTip="{DynamicResource PeriodSelect}"
                                        GlyphHeight="24" GlyphWidth="24" AllowGlyphTheming="False"
                                        Style="{DynamicResource CalendarImageGlyphStyle}"
                                        GlyphStretch="Fill" IsEllipseEnabled="False" >
                            <dxwui:AppBarButton.Flyout>
                                <dxwui:MenuFlyout ShowIndicator="True" x:Name="MenuLayout">
                                    <dxwui:MenuFlyoutItem StaysOpenOnClick="True">
                                        <Grid Width="Auto" Height="Auto">
                                            <DockPanel>
                                                <StackPanel Orientation="Vertical" x:Name="PeriodCategories">
                                                    <!--This Year-->
                                                    <Button x:Name="ThisYear" Content="This Year" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=ThisYear}">
                                                    </Button>

                                                    <!--Last Year-->
                                                    <Button x:Name="LastYear" Content="Last Year" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastYear}">
                                                    </Button>


                                                    <!--This Month-->
                                                    <Button x:Name="ThisMonth" Content="{DynamicResource SAMThisMonth}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=ThisMonth}">
                                                    </Button>

                                                    <!--Last one month-->
                                                    <!--<Button x:Name="LastOneMonth" Content="{DynamicResource SAMLastOneMonth}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastOneMonth}">
                                                    </Button>-->

                                                    <!--Last month-->
                                                    <Button x:Name="LastMonth" Content="{DynamicResource SAMLastMonth}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastMonth}">
                                                    </Button>

                                                    <!--This week-->
                                                    <Button x:Name="ThisWeek" Content="{DynamicResource SAMThisWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=ThisWeek}">
                                                    </Button>

                                                    <!--Last one week-->
                                                    <!--<Button x:Name="LastOneWeek" Content="{DynamicResource SAMLastOneWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastOneWeek}">
                                                    </Button>-->

                                                    <!--Last week-->
                                                    <Button x:Name="LastWeek" Content="{DynamicResource SAMLastWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastWeek}">
                                                    </Button>

                                                    <!--Last 12 month-->
                                                    <Button x:Name="Last12Months" Content="Last 12 Months" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=Last12Months}">
                                                    </Button>

                                                    <!--Custom Range-->
                                                    <Button x:Name="CustomRange" Content="{DynamicResource PeriodCustomRange}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCustomRangeCommand}"
                                                            CommandParameter="{Binding ElementName=CustomRange}">
                                                    </Button>
                                                    <StackPanel Orientation="Horizontal" Margin="0,50,0,0">
                                                        <Button Content="{DynamicResource SAMApply}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="60" Background="Yellow" 
                                                            Command="{Binding ApplyCommand}" CommandParameter="{Binding ElementName=MenuLayout}">
                                                        </Button>
                                                        <Button Content="{DynamicResource SAMCancel}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="60" 
                                                            Command="{Binding CancelCommand}" CommandParameter="{Binding ElementName=MenuLayout}">
                                                        </Button>
                                                    </StackPanel>
                                                </StackPanel>

                                                <StackPanel Visibility="{Binding DataContext.IsCalendarVisible, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Orientation="Horizontal">
                                                    <StackPanel Orientation="Vertical">
                                                        <dxe:TextEdit Text="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                        </dxe:TextEdit>
                                                        <!--Calendar-->
                                                        <dxe:DateEditCalendar DateTime="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Vertical">
                                                        <dxe:TextEdit Text="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                        </dxe:TextEdit>
                                                        <!--Calendar-->
                                                        <dxe:DateEditCalendar DateTime="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </DockPanel>
                                        </Grid>
                                    </dxwui:MenuFlyoutItem>
                                </dxwui:MenuFlyout>
                            </dxwui:AppBarButton.Flyout>
                        </dxwui:AppBarButton>
                    </dxwui:AppBar>

                </Grid>
            </DockPanel>
        </Grid>

        <dxlc:LayoutControl Grid.Row="1" Grid.Column="0" Name="layoutControl1" Padding="0" Orientation="Vertical" Margin="0" DragScrolling="False">
            <dxlc:LayoutGroup Orientation="Vertical">
                <dxlc:LayoutGroup dxlc:LayoutControl.AllowHorizontalSizing="True" View="GroupBox"  Margin="0" Padding="0"
                                  IsCollapsible="True" IsCollapsed="False" HorizontalOffset="0" VerticalOffset="0" >
                    <!--[rdixit][GEOS2-4269][28.06.2023]-->
                    <dxc:ChartControl x:Name="ChartItemsForecast" CustomDrawCrosshair="Changed_Event" Margin="0" Padding="0" DataSource="{Binding MainOtsList , Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <dxmvvm:Interaction.Triggers>
                            <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding ChartItemsForecastLoadCommand}" CommandParameter="{Binding ElementName=ChartItemsForecast}"/>
                        </dxmvvm:Interaction.Triggers>
                    </dxc:ChartControl>
                </dxlc:LayoutGroup>

                <dxlc:LayoutGroup Name="layoutGroup12" View="GroupBox"  Margin="0" Padding="0" dxlc:LayoutControl.AllowHorizontalSizing="True" MinHeight="350" IsCollapsible="True" IsCollapsed="False">
                    <dxg:GridControl Name="gridControl"  Padding="0" FilterString="{Binding FilterString,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             Margin="0" ShowBorder="True" VerticalAlignment="Stretch" CustomSummary="GridControl_CustomSummary"
                             ItemsSource="{Binding MainOtsList,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"  LayoutUpdated="GridControl_LayoutUpdated"
                             SelectionMode="Row" ImplyNullLikeEmptyStringWhenFiltering="True" AllowLiveDataShaping="True">

                        <dxg:GridColumn Header="{DynamicResource GridColCode}" FieldName="MergeCode" MinWidth="150" FilterPopupMode="CheckedList" AllowResizing="True">

                            <dxg:GridColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock VerticalAlignment="Center" Margin="10 0 0 0">
                             <Hyperlink x:Name="hyperlink" 
                                               TargetName="_blank"
                                               Command="{Binding DataContext.WorkOrderHyperlinkClickCommand,RelativeSource={RelativeSource AncestorType=dxg:GridControl}}"
                                               CommandParameter="{Binding ElementName=PendingWorkOrderTableView}" Style="{DynamicResource hyperlinkGridStyle}">
                                        <InlineUIContainer>
                                            <TextBlock Text="{Binding Data.MergeCode}"  Style="{DynamicResource HyperlinkTextBlock}" TextDecorations="Underline"/>
                                        </InlineUIContainer>
                                    </Hyperlink>
                        </TextBlock>
                                </DataTemplate>
                            </dxg:GridColumn.CellTemplate>

                        </dxg:GridColumn>
                        <dxg:GridColumn Header="{DynamicResource ManagementGridOffer}" FieldName="Quotation.Offer.Code" FilterPopupMode="CheckedList" MinWidth="100" ReadOnly="True"  AllowResizing="True"/>
                        <dxg:GridColumn Header="{DynamicResource EditActivityGroup}" FieldName="Quotation.Site.Customer.CustomerName" MinWidth="80" FilterPopupMode="CheckedList"  AllowResizing="True"/>
                        <dxg:GridColumn Header="{DynamicResource EditActivityPlant}" FieldName="Quotation.Site.Name" MinWidth="100" FilterPopupMode="CheckedList"  AllowResizing="True"/>
                        <dxg:GridColumn Header="{DynamicResource WMSUrgent}"  Binding ="{Binding RowData.Row.Quotation.Offer.IsCritical}" MinWidth="70" Width="35" MaxWidth="40"
                                FilterPopupMode="CheckedList" FixedWidth="False" AllowResizing="False">
                            <dxg:GridColumn.DisplayTemplate>
                                <ControlTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Width="15" Height="15" Margin="5 0 0 0" HorizontalAlignment="Center">
                                            <Image.Style>
                                                <Style>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RowData.Row.Quotation.Offer.IsCritical}" Value="1">
                                                            <Setter Property="Image.Source" Value="/Emdep.Geos.Modules.Warehouse;component/Assets/Images/Critical.png"/>
                                                            <Setter Property="Image.ToolTip" Value="IsCritical"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>
                                    </StackPanel>
                                </ControlTemplate>
                            </dxg:GridColumn.DisplayTemplate>
                            <dxg:GridColumn.EditSettings>
                                <dxe:TextEditSettings MaxWidth="100">
                                    <dxe:TextEditSettings.DisplayTextConverter>
                                        <dxmvvm:ObjectToObjectConverter>
                                            <dxmvvm:MapItem Source="1" Target="Yes" />
                                            <dxmvvm:MapItem Source="0" Target="No" />
                                        </dxmvvm:ObjectToObjectConverter>
                                    </dxe:TextEditSettings.DisplayTextConverter>
                                </dxe:TextEditSettings>
                            </dxg:GridColumn.EditSettings>
                            <dxg:GridColumn.PrintCellStyle>
                                <Style TargetType="dxe:BaseEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                    <Setter Property="dxp:ExportSettings.TargetType" Value="1" />
                                    <Setter Property="dxp:ImageExportSettings.ForceCenterImageMode" Value="True" />
                                    <Setter Property="dxp:ImageExportSettings.ImageRenderMode" Value="UseImageSource" />
                                    <Setter Property="dxp:ImageExportSettings.ImageSource">
                                        <Setter.Value>
                                            <Image Source="{dx:DXImage Image={x:Null}}" Stretch="None" />
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Value}" Value="1">
                                            <Setter Property="dxp:ImageExportSettings.ImageSource">
                                                <Setter.Value>
                                                    <Image Source="{DynamicResource WareHousePendingWorkOrderIsCriticalImage}" Stretch="Fill" />
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </dxg:GridColumn.PrintCellStyle>
                        </dxg:GridColumn>
                        <dxg:GridColumn Header="{DynamicResource WorkOrderItemsGridCol}" FieldName="OtItemCount" MinWidth="70" MaxWidth="80" FilterPopupMode="CheckedList" AllowResizing="True"/>

                        <dxg:GridColumn Header="{DynamicResource ManagementGridPOCode}" FieldName="PoCode" FilterPopupMode="CheckedList" MinWidth="100" Width="100" AllowResizing="True"/>

                        <dxg:GridColumn Header="{DynamicResource CarriageMethod}" FieldName="Quotation.Offer.CarriageMethod.Value" Width="200" MinWidth="200" FilterPopupMode="CheckedList"  AllowResizing="True"/>

                        <dxg:GridColumn Header="{DynamicResource ManagementGridAssignee}" FieldName="Quotation.Offer.AssignedToPerson.FullName" FilterPopupMode="CheckedList"  Width="200" MinWidth="200" ReadOnly="True" AllowResizing="True"/>

                        <dxg:GridColumn Header="{DynamicResource ManagementGridOperators}" FieldName="OperatorUserName" ColumnFilterMode="DisplayText" FilterPopupMode="CheckedList"  Width="200" MinWidth="200" IsSmart="True" ReadOnly="True" AllowResizing="True"/>

                        <dxg:GridColumn Header="{DynamicResource WorkOrdersPreparationPoDate}" FieldName="PoDate" 
                                MinWidth="100" Width="100" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>

                        <!--[GEOS2-4268][rdixit][22.06.2023] updated time format from dd hh mm to hh mm-->
                        <dxg:GridColumn Name="TotalTimeForPickingOT" 
                                Header="{DynamicResource WorkOrdersPreparationPickingTime}" 
                                FieldName="TotalTimeForPickingOT" 
                                FilterPopupMode="CheckedList"  Width="auto" MinWidth="100"  
                                ReadOnly="True"
                                EditSettings="{dxe:TextSettings Mask='[hH ] mM',
                                MaskUseAsDisplayFormat=True, MaskType=TimeSpan,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>

                        <dxg:GridColumn Header="{DynamicResource WorkOrdersPreparationPackingDate}" 
                                FieldName="FirstShipment.CreatedIn" 
                                MinWidth="100" Width="100" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>
                        <!--[GEOS2-4268][rdixit][22.06.2023] updated time format from dd hh mm to hh mm-->
                        <dxg:GridColumn Header="{DynamicResource WorkOrdersPreparationPackingTime}" 
                                Name="TotalTimeForShipmentCreatedIn" 
                                FieldName="TotalTimeForShipmentCreatedIn" 
                                FilterPopupMode="CheckedList"  Width="auto" MinWidth="140"  
                                ReadOnly="True" 
                                EditSettings="{dxe:TextSettings Mask='[hH ] mM',
                                MaskUseAsDisplayFormat=True, MaskType=TimeSpan,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>

                        <dxg:GridColumn Header="{DynamicResource WorkOrdersPreparationExpectedDeliveryDate}" FieldName="DeliveryDate" 
                                MinWidth="160" Width="160" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>

                        <dxg:GridColumn Header="{DynamicResource WorkOrdersPreparationShippingDate}" 
                                FieldName="FirstShipment.DeliveryDate" 
                                MinWidth="100" Width="100" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>
                        <!--[GEOS2-4268][rdixit][22.06.2023] updated time format from dd hh mm to hh mm-->
                        <dxg:GridColumn Header="{DynamicResource WorkOrdersPreparationShippingTime}" 
                                FieldName="TotalTimeForShipmentDelivery" 
                                Name="TotalTimeForShipmentDelivery" 
                                FilterPopupMode="CheckedList"  Width="auto" MinWidth="140"  
                                ReadOnly="True"
                                EditSettings="{dxe:TextSettings Mask='[hH ] mM',
                                MaskUseAsDisplayFormat=True, MaskType=TimeSpan,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>
                        <!--[GEOS2-4268][rdixit][22.06.2023] Updated summary type to custome-->
                        <dxg:GridControl.TotalSummary>
                            <dxg:GridSummaryItem x:Name="summary" FieldName="MergeCode" SummaryType="Count" DisplayFormat="{DynamicResource GridSummary}" Visible="True"   Alignment="Left"/>
                            <dxg:GridSummaryItem FieldName="TotalTimeForPickingOT" ShowInColumn="TotalTimeForPickingOT" SummaryType="Custom"/>
                            <dxg:GridSummaryItem FieldName="TotalTimeForShipmentCreatedIn" ShowInColumn="TotalTimeForShipmentCreatedIn" SummaryType="Custom"/>
                            <dxg:GridSummaryItem FieldName="TotalTimeForShipmentDelivery" ShowInColumn="TotalTimeForShipmentDelivery" SummaryType="Custom"/>
                        </dxg:GridControl.TotalSummary>

                        <dxg:GridControl.View>
                            <dxg:TableView x:Name="PendingWorkOrderTableView"
                                   NavigationStyle="Row"
                                   RowDetailsVisibilityMode="Visible"
                                   ShowHorizontalLines="True"
                                   ShowSearchPanelMode="Always" 
                                   ShowColumnHeaders="True"                                  
                                   VerticalScrollbarVisibility="Visible"
                                   AllowFixedColumnMenu="False"
                                   ShowFixedTotalSummary="True" ShowTotalSummary="True"
                                   SearchPanelHorizontalAlignment="Stretch"
                                   ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                   IsColumnChooserVisible="False"
                                   FocusedRow="{Binding SelectedObject, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                   AllowGrouping="True"
                                   ShowGroupPanel="False"
                                   ShowIndicator="False" AutoWidth="True"
                                   FilterEditorShowOperandTypeIcon="True"
                                   AllowColumnFiltering="True"
                                   Margin="0" Padding="0"
                                   >
                                <!--IsColumnChooserVisible="{Binding IsWorkOrderColumnChooserVisible,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"-->
                                <dxg:TableView.Resources>
                                    <!--<DataTemplate x:Key="PendingWorkOrderListReportPrintHeaderTemplate">
                                <Grid Width="{Binding UsablePageWidth}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="50" />
                                        <ColumnDefinition Width="130" />
                                    </Grid.ColumnDefinitions>
                                    <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" Height="50" />
                                    <dxe:TextEdit Text="Pending Work Orders Report" IsPrintingMode="True" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" />
                                    <Image Source="/Emdep.Geos.Modules.Warehouse;component/Assets/Images/GeosReportLogo.png" Height="50" Width="80"  dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                                </Grid>
                            </DataTemplate>-->

                                    <!--<DataTemplate x:Key="PendingWorkOrderListReportPrintFooterTemplate">
                                <dxe:TextEdit HorizontalContentAlignment="Center" 
                                                  Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                                  dxp:ExportSettings.TargetType="PageNumber" 
                                                  dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                                  dxp:PageNumberExportSettings.Format="Page {0} of {1}"/>
                            </DataTemplate>-->
                                </dxg:TableView.Resources>

                                <!--<Interactive:Interaction.Triggers>
                            <Interactive:EventTrigger EventName="Loaded">
                                <Behaviors:InvokeDelegateCommandAction Command="{Binding CustomCellAppearanceCommand}" 
                                                                       CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=InvokeParameter}" />
                            </Interactive:EventTrigger>
                        </Interactive:Interaction.Triggers>-->

                                <dxmvvm:Interaction.Behaviors>
                                    <dxmvvm:EventToCommand Command="{Binding TableViewLoadedCommand}"
                                                   EventName="Loaded"
                                                   PassEventArgsToCommand="True" />
                                    <!--[rdixit][GEOS2-3150][05.08.2022]-->
                                    <dxmvvm:EventToCommand EventName="ShowFilterPopup"
                                               Command="{Binding CustomShowFilterPopupCommand}"
                                               PassEventArgsToCommand="True"/>
                                    <dxmvvm:EventToCommand Command="{Binding FilterEditorCreatedCommand}"
                                                   EventName="FilterEditorCreated"
                                                   PassEventArgsToCommand="True" />

                                    <!--<dxmvvm:EventToCommand Command="{Binding CommandGridDoubleClick}"
                                                   EventName="MouseDoubleClick"
                                                   CommandParameter="{Binding ElementName=PendingWorkOrderTableView}" >
                                <dxmvvm:EventToCommand.EventArgsConverter>
                                    <dx:EventArgsToDataRowConverter/>
                                </dxmvvm:EventToCommand.EventArgsConverter>
                            </dxmvvm:EventToCommand>-->

                                    <dxmvvm:EventToCommand Command="{Binding CustomCellAppearanceCommand}"
                                                   EventName="CustomCellAppearance"
                                                   PassEventArgsToCommand="True" />

                                </dxmvvm:Interaction.Behaviors>
                            </dxg:TableView>
                        </dxg:GridControl.View>
                    </dxg:GridControl>
                </dxlc:LayoutGroup>
            </dxlc:LayoutGroup>
        </dxlc:LayoutControl>
        <!--</DockPanel>-->
    </Grid>
</UserControl>
