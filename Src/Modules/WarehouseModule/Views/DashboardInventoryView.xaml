<UserControl x:Class="Emdep.Geos.Modules.Warehouse.Views.DashboardInventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
              xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/charts" 
              xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
              xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
             xmlns:WarehouseCommon ="clr-namespace:Emdep.Geos.Modules.Warehouse"
             xmlns:UIConverter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Warehouse.Views"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    
    <Grid Margin="0,5,0,0" ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Content="{DynamicResource DashBoardInventoryViewHeader}" VerticalAlignment="Top" Margin="10,-5,0,0" Style="{DynamicResource LabelTitleStyle}" HorizontalAlignment="Left" Grid.Column="0" Grid.Row="0" />

        <DockPanel Grid.Column="2" Grid.Row="0" HorizontalAlignment="Stretch" Margin="0,0,5,0" Height="45">
            <Grid HorizontalAlignment="Right" Width="Auto" Height="40" Grid.Column="1" ShowGridLines="False" Grid.ColumnSpan="2" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70*" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="30" />

                    <ColumnDefinition Width="70*" />
                    <ColumnDefinition Width="50*" />
                    <ColumnDefinition Width="10" />

                    <ColumnDefinition Width="50*" />
                    <ColumnDefinition Width="5" /> <!--Auto to 5-->
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <DockPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" >
                    <Image Style="{DynamicResource WarehouseImageStyle}" Width="24" Height="24" Margin="7,0,3,7" />
                    <TextBlock Text="{DynamicResource Warehouse}"  FontWeight="Bold" Margin="5,2,5,9" VerticalAlignment="Bottom" />
                </DockPanel>

                <dxe:ComboBoxEdit Name="CbeWareHouse" Style="{DynamicResource ComboBoxEditStyle}" Grid.Column="1" Width="140" Height="22"
                                       Margin="5,5,5,10" VerticalAlignment="Bottom" IsTextEditable="False"
                                      ItemsSource ="{Binding Source={x:Static WarehouseCommon:WarehouseCommon.Instance}, Path=WarehouseList ,UpdateSourceTrigger=PropertyChanged , Mode=TwoWay}"
                                    SelectedItem="{Binding Source={x:Static WarehouseCommon:WarehouseCommon.Instance}, Path=Selectedwarehouse,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                                      DisplayMember="Name" ShowError="False" ShowErrorToolTip="False"  >

                    <dxmvvm:Interaction.Triggers>
                        <dxmvvm:EventToCommand EventName="EditValueChanged"
                                               Command="{Binding WarehouseChangeCommand}" CommandParameter="{Binding ElementName=CbeWareHouse,Path=SelectedItem}"
                                              />
                    </dxmvvm:Interaction.Triggers>
                </dxe:ComboBoxEdit>

                <!-- Period -->
                <!--Calender Icon-->
                <dxwui:AppBar x:Name="Appbar" HideMode="AlwaysVisible" Background="Transparent" Grid.Column="3"
                                  IsSticky="True" HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  IsOpen="True" IsExitButtonEnabled="False" >

                    <dxwui:AppBarButton x:Name="appbarbutton" ToolTip="{DynamicResource PeriodSelect}"
                                        GlyphHeight="24" GlyphWidth="24" AllowGlyphTheming="False"
                                        Style="{DynamicResource CalendarImageGlyphStyle}"
                                        GlyphStretch="Fill"
                                        Width="50" IsEllipseEnabled="False" Margin="0 -20 0 0">
                        <dxwui:AppBarButton.Flyout>
                            <dxwui:MenuFlyout ShowIndicator="True" x:Name="MenuLayout">
                                <dxwui:MenuFlyoutItem>
                                    <Grid Width="Auto" Height="Auto">
                                        <DockPanel>
                                            <StackPanel Orientation="Vertical" x:Name="PeriodCategories">
                                                <!--This Year-->
                                                <Button x:Uid="ThisYear" Content="{DynamicResource PeriodThisYear}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodThisYearCommand}">
                                                </Button>

                                                <!--Last Year-->
                                                <Button x:Uid="LastYear" Content="{DynamicResource PeriodLastYear}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodLastYearCommand}">
                                                </Button>

                                                <!--Last 12 months-->
                                                <Button x:Uid="Last12Months" Content="{DynamicResource PeriodLast12Months}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodLast12MonthsCommand}">
                                                </Button>

                                                <!--Custom Range-->
                                                <Button Content="{DynamicResource PeriodCustomRange}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCustomRangeCommand}">
                                                </Button>

                                                <StackPanel Orientation="Horizontal" Margin="0,50,0,0">
                                                    <Button Content="Apply" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="60" Background="Yellow" 
                                                            Command="{Binding ApplyCommand}" CommandParameter="{Binding ElementName=MenuLayout}">
                                                    </Button>
                                                    <Button Content="Cancel" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="60" 
                                                            Command="{Binding CancelCommand}" CommandParameter="{Binding ElementName=MenuLayout}">
                                                    </Button>
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Visibility="{Binding DataContext.IsCalendarVisible, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Orientation="Horizontal">
                                                <StackPanel Orientation="Vertical">
                                                    <dxe:TextEdit Text="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                    </dxe:TextEdit>
                                                    <!--Calendar-->
                                                    <dxe:DateEditCalendar DateTime="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Vertical">
                                                    <dxe:TextEdit Text="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                    </dxe:TextEdit>
                                                    <!--Calendar-->
                                                    <dxe:DateEditCalendar DateTime="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </DockPanel>
                                    </Grid>
                                </dxwui:MenuFlyoutItem>
                            </dxwui:MenuFlyout>
                        </dxwui:AppBarButton.Flyout>
                    </dxwui:AppBarButton>
                </dxwui:AppBar>
                <TextBlock Text="{Binding FromDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="4" Margin="3,0,0,0" />
                <TextBlock Text=" - " HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="5" FontWeight="Bold" />
                <TextBlock Text="{Binding ToDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="6" />

                <!--refresh button-->
                <!--Margin="0,-7,0,0"-->
                <!--Grid.Column="10"-->
                <Button Content=" " Height="38" Width="38" Grid.Column="8" Command="{Binding RefreshDashboard2ViewCommand}" HorizontalAlignment="Right" Margin="0,-7,0,0" >
                    <Button.ToolTip>
                        <TextBlock Text="{DynamicResource ButtonRefresh}" />
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Style="{ DynamicResource RefreshButtonImageStyle}" Grid.Column="0" Height="24" Width="24" Margin="-10" />
                            </Grid>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <!-- Switch View Margin="0,-7,0,0" -->
                <!--[pramod.misal][GEOS2-5989][23.09.2024]-->                               
                <dx:DropDownButton x:Name="dropdownButton" Content="Dropdown button" 
                                   HorizontalAlignment="Right" Height="38" Width="38"  Grid.Column="10" Margin="0,-7,0,0" VerticalAlignment="Center">
                    <dx:DropDownButton.Style>
                        <Style>
                            <Setter Property="dx:DropDownButtonBase.ArrowAlignment" Value="Right" />
                            <Setter Property="dx:DropDownButtonBase.ArrowPadding" Value="0" />
                        </Style>
                    </dx:DropDownButton.Style>

                    <Button.ToolTip>
                        <TextBlock Text="Switch View"></TextBlock>
                    </Button.ToolTip>

                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <!--Margin="0,-7,0,0"  -16-->
                                <Image Style="{ DynamicResource WMSSwitchViewButtonImageStyle}" Grid.Column="0" Height="24" Width="22"  Margin="-16" />
                            </Grid>
                        </DataTemplate>
                    </Button.ContentTemplate>

                    <dxb:PopupMenu>
                        <!-- Line View Button -->
                        <dxb:BarButtonItem Name="GridView" GlyphSize="Medium" Content="Line View"  Glyph="{DynamicResource SwitchInventoryLineGraph}" Command="{Binding ShowLineGraphViewCommand}" CommandParameter="{Binding ElementName=ChartInventoryAmountByWeek}">
                            <dxb:BarButtonItem.Style>
                                <Style>
                                    <Style.Triggers>
                                        <Trigger Property="dxb:BarButtonItem.IsMouseOver" Value="True">
                                            <Setter Property="dxb:BarButtonItem.Glyph" Value="{DynamicResource SwitchInventoryLineGraphMouseOver}" />
                                        </Trigger>
                                        <Trigger Property="dxb:BarButtonItem.IsMouseOver" Value="False">
                                            <Setter Property="dxb:BarButtonItem.Glyph" Value="{DynamicResource SwitchInventoryLineGraph}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </dxb:BarButtonItem.Style>
                        </dxb:BarButtonItem>

                        <!-- Bar View Button -->
                        <dxb:BarButtonItem Name="BandView" GlyphSize="Medium" Content="Bar View" Glyph="{DynamicResource SwitchInventoryBarGraph}" Command="{Binding ShowBarGraphViewCommand}" CommandParameter="{Binding ElementName=ChartInventoryAmountByWeek}">
                            <dxb:BarButtonItem.Style>
                                <Style>
                                    <Style.Triggers>
                                        <Trigger Property="dxb:BarButtonItem.IsMouseOver" Value="True">
                                            <Setter Property="dxb:BarButtonItem.Glyph" Value="{DynamicResource SwitchInventoryBarGraphMouseOver}" />
                                        </Trigger>
                                        <Trigger Property="dxb:BarButtonItem.IsMouseOver" Value="False">
                                            <Setter Property="dxb:BarButtonItem.Glyph" Value="{DynamicResource SwitchInventoryBarGraph}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </dxb:BarButtonItem.Style>
                        </dxb:BarButtonItem>
                    </dxb:PopupMenu>
                </dx:DropDownButton>


            </Grid>
        </DockPanel>

        <dxlc:LayoutControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0" Padding="0" Grid.Row="1" 
                            Grid.Column="0" Grid.ColumnSpan="3" Orientation="Vertical" Style="{DynamicResource LayoutControlStyle}"
                            DragScrolling="False"  >
            
            <dxlc:LayoutGroup  ScrollViewer.VerticalScrollBarVisibility="Hidden">
                  
                        <dxlc:LayoutGroup UseLayoutRounding="True"  x:Name="InventoryStatus"
                                          Header="INVENTORY STATUS" View="GroupBox" 
                                          Orientation="Vertical" IsCollapsible="False"
                                          HorizontalAlignment="Stretch" Width="350"
                                          >

                            <dxlc:LayoutGroup Background="{Binding Color}" Height="50" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_STOCK}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding StockAmount}" />
                            </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Background="{Binding RunningMaterialColor}" Height="50" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_RunningMaterial}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding RunningMaterial.ArticleStockAmount}" />
                            </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Background="{Binding SpecialMaterialColor}" Height="49" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_SpecialMaterial}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SpecialMaterial.ArticleStockAmount}" />
                            </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Background="{Binding SpecialAssetColor}" Height="49" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_SpecialAsset}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SpecialAsset.ArticleStockAmount}" />
                            </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Background="{Binding SpecialToolingColor}" Height="49" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_SpecialTooling}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SpecialTooling.ArticleStockAmount}" />
                            </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Background="{Binding SpecialRunningColor}" Height="49" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_SpecialRunning}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SpecialRunning.ArticleStockAmount}" />
                            </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Background="{Binding SoftwareRunningColor}" Height="49" Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Dashboard2_Software}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SoftwareRunning.ArticleStockAmount}" />
                    </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Background="{Binding ObsoleteColor}" Height="49" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_OBSOLETE}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding ObsoleteAmount}" />
                            </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Background="{Binding SleepingColor}" Height="49" Orientation="Horizontal">
                                <TextBlock  Text="{DynamicResource Dashboard2_SLEEPING}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SleepingAmount}" />
                            </dxlc:LayoutGroup>

                 

                    <dxlc:LayoutGroup Background="{Binding WarehouseStockTargetsColor}" Height="49" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_WarehouseStockTargets}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding WarehouseStockTargets.ArticleStockAmount}" />
                            </dxlc:LayoutGroup>

                 


                    <!--<dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_STOCK}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding StockAmount}" />
                            </dxlc:LayoutGroup>
                            <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_OBSOLETE}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding ObsoleteAmount}" />
                            </dxlc:LayoutGroup>
                            <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                                <TextBlock Text="{DynamicResource Dashboard2_SLEEPING}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SleepingAmount}" />
                            </dxlc:LayoutGroup>-->
                        </dxlc:LayoutGroup>
                 
                <dxlc:LayoutGroup Header="{DynamicResource Dashboard2_INVENTORYAMOUNTBYWEEK}"  
                                      View="GroupBox" Orientation="Vertical" IsCollapsible="False" Margin="0"
                                       HorizontalAlignment="Stretch" Grid.Column="1" 
                                      Grid.Row="1" Grid.ColumnSpan="2">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition/>
                                <!--<RowDefinition Height="40"/>-->
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Background="#3a69b5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="100*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="100*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="100*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{DynamicResource Dashboard2_Min}" Grid.Row="0" Grid.Column="0" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                                <TextBlock Text="{Binding MinAmount}" Grid.Row="0" Grid.Column="2" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" FontWeight="Bold" HorizontalAlignment="Stretch"/>
                                <TextBlock Text="{DynamicResource Dashboard2_Max}" Grid.Row="0" Grid.Column="4" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                                <TextBlock Text="{Binding MaxAmount}" Grid.Row="0" Grid.Column="6" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" FontWeight="Bold" HorizontalAlignment="Stretch"/>
                                <TextBlock Text="{DynamicResource Dashboard2_Avg}" Grid.Row="0" Grid.Column="8" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                                <TextBlock Text="{Binding AvgAmount}" Grid.Row="0" Grid.Column="10" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" FontWeight="Bold" HorizontalAlignment="Stretch"/>
                                <TextBlock Text="{DynamicResource Dashboard2_Act}" Grid.Row="0" Grid.Column="12" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                                <TextBlock Text="{Binding ActAmount}" Grid.Row="0" Grid.Column="16" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" FontWeight="Bold" HorizontalAlignment="Stretch"/>
                            </Grid>
                            <Grid Grid.Row="1">
                                <dxc:ChartControl x:Name="ChartInventoryAmountByWeek" Grid.Row="1"
                                  DataSource="{Binding GraphDataTableInventory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  VerticalAlignment="Stretch" Loaded="ChartInventoryAmountByWeek_Loaded"  CrosshairEnabled="True" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <dxc:ChartControl.Legend>
                                    <!--//Shubham[skadam] GEOS2-4852 set a legend for the inventory graph 11 01 2024-->
                                        <!--[nsatpute][10-01-2025][GEOS2-5852]-->
                                        <dxc:Legend VerticalPosition="TopOutside" HorizontalPosition="Center"  Orientation="Horizontal" UseCheckBoxes="True"/>
                                         <!--<dxc:Legend HorizontalPosition="RightOutside" VerticalPosition="Top" Orientation="Vertical" UseCheckBoxes="True" />-->
                                    </dxc:ChartControl.Legend>
                                    <dxmvvm:Interaction.Triggers>
                                        <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding ChartLoadInventoryCommand}" CommandParameter="{Binding ElementName=ChartInventoryAmountByWeek}" />
                                        <dxmvvm:EventToCommand EventName="CustomDrawCrosshair" Command="{Binding ChartDashboardInventoryDrawCrosshairCommand}" PassEventArgsToCommand="True" />
                                    </dxmvvm:Interaction.Triggers>
                                </dxc:ChartControl>
                            </Grid>

                            <!--<Grid Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">-->
                            <!--<Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Row="0" Grid.Column="0" Margin="100,0,5,0" Text="{Binding RunningMaterial.Abbreviation}"   >
                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RunningMaterial.HtmlColor}" Value="{x:Null}">
                                                    <Setter Property="TextBlock.Foreground" Value="{DynamicResource DashboardInventoryViewMaterialTypeTextBlockMain}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                            <Setter Property="TextBlock.Foreground" Value="{Binding RunningMaterial.HtmlColor}" />
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Grid.Row="0" Grid.Column="2" Margin="5,0,5,0" Text="{Binding SpecialMaterial.Abbreviation}"  >
                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RunningMaterial.HtmlColor}" Value="{x:Null}">
                                                    <Setter Property="TextBlock.Foreground" Value="{DynamicResource DashboardInventoryViewMaterialTypeTextBlockMain}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                            <Setter Property="TextBlock.Foreground" Value="{Binding RunningMaterial.HtmlColor}" />
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Grid.Row="0" Grid.Column="4" Margin="5,0,5,0" Text="{Binding SpecialAsset.Abbreviation}"     >
                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RunningMaterial.HtmlColor}" Value="{x:Null}">
                                                    <Setter Property="TextBlock.Foreground" Value="{DynamicResource DashboardInventoryViewMaterialTypeTextBlockMain}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                            <Setter Property="TextBlock.Foreground" Value="{Binding RunningMaterial.HtmlColor}" />
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Grid.Row="0" Grid.Column="6" Margin="5,0,5,0" Text="{Binding SpecialTooling.Abbreviation}"  >
                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RunningMaterial.HtmlColor}" Value="{x:Null}">
                                                    <Setter Property="TextBlock.Foreground" Value="{DynamicResource DashboardInventoryViewMaterialTypeTextBlockMain}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                            <Setter Property="TextBlock.Foreground" Value="{Binding RunningMaterial.HtmlColor}" />
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Grid.Row="0" Grid.Column="8" Margin="5,0,5,0" Text="{Binding SpecialRunning.Abbreviation}"  >
                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RunningMaterial.HtmlColor}" Value="{x:Null}">
                                                    <Setter Property="TextBlock.Foreground" Value="{DynamicResource DashboardInventoryViewMaterialTypeTextBlockMain}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                            <Setter Property="TextBlock.Foreground" Value="{Binding RunningMaterial.HtmlColor}" />
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>-->
                          
                        </Grid>
                    
                       
                    </dxlc:LayoutGroup>
                    
               
            </dxlc:LayoutGroup>
            <!--<dxlc:LayoutGroup>
                <dxlc:LayoutGroup Orientation="Horizontal">
                    <dxlc:LayoutGroup Header="{DynamicResource Dashboard2_SALESSTATUSBYCUSTOMER}" Margin="0" Padding="0" VerticalAlignment="Center" View="GroupBox" Orientation="Vertical" HorizontalAlignment="Stretch">
                        <dxc:ChartControl x:Name="ChartSalesStatusByCustomer"
                                  DataSource="{Binding GraphDataTableCustomer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  VerticalAlignment="Stretch" Height="500" CrosshairEnabled="True" BorderThickness="1" ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <dxc:ChartControl.Legend>
                                <dxc:Legend HorizontalPosition="RightOutside" />
                            </dxc:ChartControl.Legend>
                            <dxmvvm:Interaction.Triggers>
                                <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding ChartLoadCustomerCommand}" CommandParameter="{Binding ElementName=ChartSalesStatusByCustomer}" />
                                <dxmvvm:EventToCommand EventName="CustomDrawCrosshair" Command="{Binding ChartDashboardSaleCustomDrawCrosshairCommand}" PassEventArgsToCommand="True" />
                            </dxmvvm:Interaction.Triggers>
                        </dxc:ChartControl>
                    </dxlc:LayoutGroup>
                    <dxlc:LayoutGroup Header="{DynamicResource Dashboard2_SALESSTATUSBYMONTH}" Margin="0" Padding="0" VerticalAlignment="Center" View="GroupBox" Orientation="Vertical" HorizontalAlignment="Stretch">
                        <dxc:ChartControl x:Name="ChartSalesStatusByMonths" 
                                  DataSource="{Binding GraphDataTable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  VerticalAlignment="Stretch" Height="500" CrosshairEnabled="True" BorderThickness="1" ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <dxc:ChartControl.Legend>
                                <dxc:Legend HorizontalPosition="RightOutside" />
                            </dxc:ChartControl.Legend>
                            <dxmvvm:Interaction.Triggers>
                                <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding ChartLoadCommand}" CommandParameter="{Binding ElementName=ChartSalesStatusByMonths}" />
                            </dxmvvm:Interaction.Triggers>
                        </dxc:ChartControl>
                    </dxlc:LayoutGroup>
                </dxlc:LayoutGroup>
            </dxlc:LayoutGroup>-->
            
            <!--<Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

            <dxlc:LayoutGroup Grid.Column="0" Margin="5">
                <dxlc:LayoutGroup UseLayoutRounding="True" Height="Auto" x:Name="InventoryStatus" Header="INVENTORY STATUS" View="GroupBox" Orientation="Vertical" IsCollapsible="False" HorizontalAlignment="Stretch">

                    <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Dashboard2_STOCK}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding StockAmount}" />
                    </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Dashboard2_RunningMaterial}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding RunningMaterial.ArticleStockAmount}" />
                    </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Dashboard2_SpecialMaterial}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SpecialMaterial.ArticleStockAmount}" />
                    </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Dashboard2_SpecialAsset}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SpecialAsset.ArticleStockAmount}" />
                    </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Dashboard2_SpecialTooling}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SpecialTooling.ArticleStockAmount}" />
                    </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Dashboard2_SpecialRunning}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SpecialRunning.ArticleStockAmount}" />
                    </dxlc:LayoutGroup>

                    <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Dashboard2_OBSOLETE}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding ObsoleteAmount}" />
                    </dxlc:LayoutGroup>
                    <dxlc:LayoutGroup Height="50" Orientation="Horizontal">
                        <TextBlock Text="{DynamicResource Dashboard2_SLEEPING}" Style="{StaticResource TextBlock20Style}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <dxe:TextEdit 
                                IsReadOnly="True"
                                Mask="c"
                                MaskCulture="{Binding Path=CurrentCultureCurrencySymbol_warehouse, Source={x:Static GeosApplication:GeosApplication.Instance}}"
                                MaskType="Numeric"
                                MaskUseAsDisplayFormat="True"
                                FontSize="30"
                                ShowBorder="False"
                                HorizontalAlignment="Right"
                                IsPrintingMode="True"
                                Text="{Binding SleepingAmount}" />
                    </dxlc:LayoutGroup>
                </dxlc:LayoutGroup>

            </dxlc:LayoutGroup>
            <dxlc:LayoutGroup Grid.Column="1" Margin="5">
                <dxlc:LayoutGroup Header="{DynamicResource Dashboard2_INVENTORYAMOUNTBYWEEK}" Height="400" View="GroupBox" Orientation="Vertical" IsCollapsible="False" Margin="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="360"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Background="#3a69b5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="100*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="100*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="100*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Dashboard2_Min}" Grid.Row="0" Grid.Column="0" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                            <TextBlock Text="{Binding MinAmount}" Grid.Row="0" Grid.Column="2" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" FontWeight="Bold" HorizontalAlignment="Stretch"/>
                            <TextBlock Text="{DynamicResource Dashboard2_Max}" Grid.Row="0" Grid.Column="4" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                            <TextBlock Text="{Binding MaxAmount}" Grid.Row="0" Grid.Column="6" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" FontWeight="Bold" HorizontalAlignment="Stretch"/>
                            <TextBlock Text="{DynamicResource Dashboard2_Avg}" Grid.Row="0" Grid.Column="8" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                            <TextBlock Text="{Binding AvgAmount}" Grid.Row="0" Grid.Column="10" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" FontWeight="Bold" HorizontalAlignment="Stretch"/>
                            <TextBlock Text="{DynamicResource Dashboard2_Act}" Grid.Row="0" Grid.Column="12" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
                            <TextBlock Text="{Binding ActAmount}" Grid.Row="0" Grid.Column="16" Style="{StaticResource TextBlock30Style}" VerticalAlignment="Top" FontWeight="Bold" HorizontalAlignment="Stretch"/>
                        </Grid>
                        <Grid Grid.Row="1">
                            <dxc:ChartControl x:Name="ChartInventoryAmountByWeek" Grid.Row="1"
                                  DataSource="{Binding GraphDataTableInventory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  VerticalAlignment="Stretch" Height="300" CrosshairEnabled="True" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <dxc:ChartControl.Legend>
                                    <dxc:Legend HorizontalPosition="RightOutside" />
                                </dxc:ChartControl.Legend>
                                <dxmvvm:Interaction.Triggers>
                                    <dxmvvm:EventToCommand EventName="Loaded" Command="{Binding ChartLoadInventoryCommand}" CommandParameter="{Binding ElementName=ChartInventoryAmountByWeek}" />
                                    <dxmvvm:EventToCommand EventName="CustomDrawCrosshair" Command="{Binding ChartDashboardInventoryDrawCrosshairCommand}" PassEventArgsToCommand="True" />
                                </dxmvvm:Interaction.Triggers>
                            </dxc:ChartControl>
                        </Grid>
                    </Grid>
                </dxlc:LayoutGroup>
            </dxlc:LayoutGroup>
            </Grid>-->
        </dxlc:LayoutControl>

    </Grid>
</UserControl>
