<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.PLM.Views.PLMSystemSettingsView"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                         xmlns:local="clr-namespace:Emdep.Geos.Modules.PLM.Views"
                         xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
                         xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                         xmlns:PLMApplication="clr-namespace:Emdep.Geos.Modules.PLM"
                         xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                         xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
                         xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
                         xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                         xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
                         xmlns:ve="http://schemas.devexpress.com/winfx/2008/xaml/docking/visualelements"
                         mc:Ignorable="d" 
                         Title="{DynamicResource PCMSystemSetting}"
                         TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}" >
    
    
    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding CancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <!--<dxwui:WinUIDialogWindow.Resources>
        <Style TargetType="{x:Type ve:AutoHideTray}">
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <ve:psvStackPanel HorizontalAlignment="Right" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </dxwui:WinUIDialogWindow.Resources>-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="80" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
        </Grid.RowDefinitions>

        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            
        </Grid.ColumnDefinitions>-->
     
          



                <Label Grid.Column="0" Grid.Row="0" Content="{DynamicResource PCMSystemSettingECOSSync}" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" FontSize="20"></Label>
                 <Label Content="{DynamicResource PCMSystemSettingECOSSyncMessage}" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" FontSize="14"></Label>



   
            <dxg:GridControl x:Name="systemgrid" VerticalAlignment="Stretch" HorizontalAlignment="Left"  Grid.Row="2" Width="243"
                              DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow, Mode=FindAncestor}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             ItemsSource="{Binding SystemSettinglist}" SelectionMode="Row"
                             SelectedItem="{Binding SelectedSystemSetting}"  
                             ><!--ShowBorder="False" BorderThickness="0"-->
            <!--<dxg:GridControl.Columns>-->

            <dxg:GridColumn Header="Name" FieldName="AppSettingName"  AllowBestFit="true" ReadOnly="True" FilterPopupMode="Default">
            </dxg:GridColumn>

            <dxg:GridColumn Header=" " ReadOnly="True" Width="Auto"  AllowBestFit="true" FilterPopupMode="Default" Visible="True" AllowPrinting="False" ShowInColumnChooser="False" AllowMoving="false">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                     
                            <Button Name="SynchronizeRowItem" ToolTip="{DynamicResource PCMSynchronize}" 
                                    Command="{Binding View.DataContext.SynchronizeCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" CommandParameter="{Binding ElementName=SynchronizeRowItem}" ><!--CommandParameter="{Binding RowData.Row}"-->
                                <Button.ContentTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Height="20">
                                            <Image Height="15" Width="15"  Style="{ DynamicResource SynchronizeStyle}" />
                                            <TextBlock Text ="Synchronize"  VerticalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                        <!--<Image Height="15" Width="20" Style="{ DynamicResource SynchronizeStyle}" />-->
                                   
                                    </DataTemplate>
                                </Button.ContentTemplate>
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Cursor" Value="Hand"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                         
                    </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>

          

            <!--</dxg:GridControl.Columns>-->
            <dxg:GridControl.View>
                <dxg:TableView  x:Name="tablesettings" 
                                                        HorizontalScrollbarVisibility="Disabled"
                                                        NavigationStyle="Row"
                                                        ShowGroupPanel="False"
                                                        ShowIndicator="False"
                                                        ShowColumnHeaders="True"
                                                        ShowSearchPanelMode ="Never"
                                                        ShowAutoFilterRow="False"
                                                        VerticalScrollbarVisibility="Auto"
                                                        FadeSelectionOnLostFocus="False"
                                                        HorizontalAlignment="Stretch"
                                                        AllowBestFit="True"
                                                        >
                    <!--EnableImmediatePosting="True" SearchPanelHorizontalAlignment="Stretch"
                                                 ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                                 EnableSmartColumnsGeneration="True"-->

                    <dxg:TableView.ColumnHeaderStyle>
                        <Style TargetType="dxg:GridColumnHeader">
                            <!--<Setter Property="dxg:BaseGridColumnHeader.ShowFilterButtonOnHover" Value="False" />-->
                            <Setter Property="Background" Value="Gray" />
                            <Setter Property="Foreground" Value="Black" />
                            <Setter Property="FontWeight" Value="Bold" />
                        </Style>
                    </dxg:TableView.ColumnHeaderStyle>
                </dxg:TableView>
            </dxg:GridControl.View>
        </dxg:GridControl>


        <Grid Grid.Row="4" HorizontalAlignment="Left" VerticalAlignment="Top" Height="Auto" Width="Auto" Background="Transparent" >
            <!--End-->
            <StackPanel
                    HorizontalAlignment="Center" 
                    Orientation="Horizontal"
                    Width="Auto">
               

                <!--Cancel-->
                <Button Content="{DynamicResource Cancel}"
                    Command="{Binding CancelButtonCommand}" 
                    Height="30"
                    Width="100"
                    Margin="60,20,0,0"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Bottom" /> <!--Margin="20,20,20,20"-->

            </StackPanel>
        </Grid>

    </Grid>

</dxwui:WinUIDialogWindow> 
