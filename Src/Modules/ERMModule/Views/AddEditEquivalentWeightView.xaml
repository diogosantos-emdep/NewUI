<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.ERM.Views.AddEditEquivalentWeightView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.ERM.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:Interactive="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:Converter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             mc:Ignorable="d" 
             Style="{DynamicResource WinUIDialogWindowStyle}"
             Title="{Binding WindowHeader}"
             TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
             >

    <dxwui:WinUIDialogWindow.Resources>
        <dxmvvm:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Converter:GroupboxBackroundColorConvertor x:Key="GroupboxBackroundColorConvertor" />
        <Converter:ForegroundColorConverter x:Key="ForegroundColorConverter" />
    </dxwui:WinUIDialogWindow.Resources>

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding EscapeButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="15"/>
            <!--<RowDefinition Height="30"/>
            <RowDefinition Height="5"/>-->
            <RowDefinition Height="30"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="15"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>




        <!--Equivalent weight -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource EquivalentWeight}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center" FontSize="16"/>
        <dxe:TextEdit Grid.Row="2" Grid.Column="2"  Text="{Binding EquivalentWeight, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Mask="##0.##00;" MaskType="Numeric" MaskUseAsDisplayFormat="True"  VerticalContentAlignment="Top" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AcceptsReturn="False" InvalidValueBehavior="AllowLeaveEditor" AllowNullInput="True">
            <Binding Path="EquivalentWeight" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                <Binding.ValidationRules>
                    <Validation:AddEditEquivalencyWeightValidation FieldName="EquivalentWeight" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True" />
                </Binding.ValidationRules>
            </Binding>
        </dxe:TextEdit>
        <!--Start Date-->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="{DynamicResource StartDate}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center" FontSize="16" />
        <dxe:DateEdit Grid.Row="4" Grid.Column="2" VerticalContentAlignment="Center"
                          IsTextEditable="true"
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      Text="{Binding StartDate}" >
             <dxe:DateEdit.EditValue>
            <Binding Path="DataContext.StartDate" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}">
                <Binding.ValidationRules>
                        <Validation:AddEditEquivalencyWeightValidation FieldName="DataContext.StartDate" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                </Binding.ValidationRules>
            </Binding>
            </dxe:DateEdit.EditValue>
           
           

        </dxe:DateEdit>

        <!--End Date-->
        <TextBlock Grid.Row="6" Grid.Column="0" Grid.RowSpan="2" Text="{DynamicResource EndDate}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center" TextWrapping="Wrap" FontSize="16"/>

        <dxe:DateEdit Grid.Row="6" Grid.Column="2" VerticalContentAlignment="Center"
                          IsTextEditable="true"
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      Text="{Binding EndDate}"
                          >
            <dxe:DateEdit.EditValue>
                <Binding Path="DataContext.EndDate" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}">
                    <Binding.ValidationRules>
                        <Validation:AddEditEquivalencyWeightValidation FieldName="DataContext.EndDate" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                    </Binding.ValidationRules>
                </Binding>
            </dxe:DateEdit.EditValue>
        </dxe:DateEdit>
        <Grid Grid.Row="8" ShowGridLines="False" Grid.Column="2" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button  HorizontalAlignment="Right"
                     Style="{DynamicResource Button16Style}"
                     VerticalAlignment="Top"
                     Content="{DynamicResource AddCatalogueAcceptButton}"
                     Command="{Binding  DataContext.AcceptEquivalentWeightActionCommand,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}"
                     Height="30"
                     Width="80" 
                      
                     />

            <Button Grid.Row="10" Grid.Column="2"
                    HorizontalAlignment="Left"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Top"
                    Content="{DynamicResource AddCatalogueCancelButton}"
                    Command="{Binding DataContext.CancelButtonCommand,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}"
                  
                    Height="30"
                    Width="80" />
        </Grid>

    </Grid>

</dxwui:WinUIDialogWindow>

