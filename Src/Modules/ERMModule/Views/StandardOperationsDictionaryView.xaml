<UserControl x:Class="Emdep.Geos.Modules.ERM.Views.StandardOperationsDictionaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.ERM.Views"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxn="http://schemas.devexpress.com/winfx/2008/xaml/navbar"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
             xmlns:ViewModel="clr-namespace:Emdep.Geos.Modules.ERM.ViewModels"
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             mc:Ignorable="d" 
             xmlns:Converters="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
              xmlns:UIConverter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Converters:ForegroundColorConverter x:Key="ForegroundColorConverter"/>
    </UserControl.Resources>
    <Grid ShowGridLines="false">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Content="{DynamicResource StandardOperationsDictionaryList}" Style="{StaticResource LabelTitleStyle}" Grid.Row="0" VerticalAlignment="Center"/>

            <Grid Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right">
                <Grid HorizontalAlignment="Right" Width="Auto" Height="45">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="45"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!--Add Button-->

                    <Button Content=" " Grid.Row="0" Grid.Column="3" Height="40" Width="40" HorizontalAlignment="Right" IsEnabled="{Binding Source={x:Static GeosApplication:GeosApplication.Instance}, Path=IsEditSODPermissionERM}"
                            Command="{Binding AddStandardOperationsDictionaryListCommand}" CommandParameter="{Binding ElementName=StandardOperationsDictionaryListTableView}" Margin="5,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource ButtonAdd}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{ DynamicResource PCMAddButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!-- Refresh Button -->
                    <Button Content=" " Grid.Row="0" Grid.Column="4" Height="40" Width="40" HorizontalAlignment="Right"
                            Command="{Binding RefreshStandardOperationsDictionaryListCommand}" CommandParameter="{Binding ElementName=StandardOperationsDictionaryListTableView}" Margin="0,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource ButtonRefresh}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{ DynamicResource PCMRefreshButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!--Print Button-->
                    <Button Content=" " Grid.Row="0" Grid.Column="5" Height="40" Width="40" HorizontalAlignment="Right"
                            Command="{Binding PrintStandardOperationsDictionaryListCommand}" CommandParameter="{Binding ElementName=StandardOperationsDictionaryListTableView}" Margin="0,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource ButtonPrint}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{DynamicResource PCMPrintButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!-- Export to Excel -->
                    <Button Content=" " Grid.Row="0" Grid.Column="6" Height="40" Width="40" HorizontalAlignment="Right"
                            Command="{Binding ExportStandardOperationsDictionaryListCommand}" CommandParameter="{Binding ElementName=StandardOperationsDictionaryListTableView}" Margin="0,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource ButtonExportToExcel}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{DynamicResource PCMExportToExcelButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </Grid>
            </Grid>
        </Grid>


        <Grid Grid.Row="1" ShowGridLines="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <dxg:GridControl x:Name="stdtimegrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="1"
                             ItemsSource="{Binding StandardOperationsDictionaryList, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                             SelectedItem="{Binding SelectedStandardOperationsDictionaryList, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                             ImplyNullLikeEmptyStringWhenFiltering="True">
                <dxg:GridColumn Header="{DynamicResource GridColCode}" FieldName="Code" MinWidth="80" ReadOnly="True" FixedWidth="False" FilterPopupMode="Default"/>
                <dxg:GridColumn Header="{DynamicResource GridColName}" FieldName="Name" MinWidth="180"  ReadOnly="True" FixedWidth="False" FilterPopupMode="Default"/>
                <dxg:GridColumn Header="{DynamicResource GridColDescription}" FieldName="Description" MinWidth="220" ReadOnly="True" FixedWidth="False" FilterPopupMode="Default"/>
                <dxg:GridColumn Header="{DynamicResource GridColPlant}" FieldName="Plants" MinWidth="110" ReadOnly="True" FixedWidth="False" FilterPopupMode="CheckedList"/>
                <dxg:GridColumn Header="{DynamicResource GridColEffectiveDate}" FieldName="EffectiveDate" MinWidth="110" FixedWidth="False" FilterPopupMode="Default" IsSmart="True"/>
                <dxg:GridColumn Header="{DynamicResource GridColExpiryDate}" FieldName="ExpiryDate" MinWidth="110"  FixedWidth="False" FilterPopupMode="Default" IsSmart="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <Border x:Name="ExpiryDateBorder" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <dxe:DateEdit Name="PART_Editor" Mask="dd/MM/yyyy" ShowClearButton="False" IsTextEditable="False"
                                              MaskUseAsDisplayFormat="True" ShowError="False" ShowErrorToolTip="False" InvalidValueBehavior="AllowLeaveEditor">
                                    <dxe:DateEdit.Style>
                                        <Style TargetType="{x:Type dxe:DateEdit}">
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Value="True">
                                                            <Condition.Binding>
                                                                <MultiBinding Converter="{UIConverter:StandardTimeGridDateCellColorConverter}">
                                                                    <Binding Path="Data.ExpiryDate" />
                                                                    <Binding Path="Data.IdStatus" />
                                                                </MultiBinding>
                                                            </Condition.Binding>
                                                        </Condition>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Foreground" Value="Red" />
                                                </MultiDataTrigger>

                                            </Style.Triggers>
                                        </Style>
                                    </dxe:DateEdit.Style>
                                </dxe:DateEdit>
                            </Border>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <dxg:GridColumn Header="{DynamicResource GridColLastUpdate}" FieldName="LastUpdated" MinWidth="110"  FixedWidth="False" FilterPopupMode="Default" IsSmart="True"/>
                <dxg:GridColumn Header="{DynamicResource GridColStatus}" FieldName="Status.Value" MinWidth="150" ReadOnly="True" FixedWidth="False" FilterPopupMode="Default">
                    <dxg:GridColumn.CellStyle>
                        <Style TargetType="{x:Type dxg:LightweightCellEditor}">
                            <Setter Property="Background" Value="{Binding RowData.Row.Status.HtmlColor}"/>
                            <Setter Property="Foreground" Value="{Binding Path=Background,Converter={StaticResource ForegroundColorConverter}}"/>
                        </Style>
                    </dxg:GridColumn.CellStyle>
                </dxg:GridColumn>
                <dxg:GridColumn Header=" " MinWidth="30" Width="40" ReadOnly="True" FixedWidth="True" FilterPopupMode="Default" Visible="True" AllowPrinting="False" ShowInColumnChooser="False" AllowMoving="false">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <Button Name="deleteRowItem" ToolTip="{DynamicResource Delete}" IsEnabled="{Binding Source={x:Static GeosApplication:GeosApplication.Instance}, Path=IsEditSODPermissionERM}"
                                    Command="{Binding View.DataContext.DeleteStandardOperationsDictionaryCommand}" CommandParameter="{Binding ElementName=StandardOperationsDictionaryListTableView}">
                                <Button.ContentTemplate>
                                    <DataTemplate>
                                        <Image Height="16" Width="16"  Style="{ DynamicResource DeleteStyle}" />
                                    </DataTemplate>
                                </Button.ContentTemplate>
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Cursor" Value="Hand"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <dxmvvm:Interaction.Behaviors>
                    
                    <UIHelper:GridSelectingBehavior />

                  
                </dxmvvm:Interaction.Behaviors>

                <dxg:GridControl.TotalSummary>
                    <dxg:GridSummaryItem x:Name="summary" FieldName="" SummaryType="Count" DisplayFormat="Total Count : {0}" Visible="True" Alignment="Left"/>
                </dxg:GridControl.TotalSummary>
                <dxg:GridControl.View>
                    <dxg:TableView  x:Name="StandardOperationsDictionaryListTableView"
                                   ShowGroupedColumns="True"  
                                   HorizontalScrollbarVisibility="Auto"  
                                   VerticalScrollbarVisibility="Visible" 
                                   ShowColumnHeaders="True"  
                                   ShowGroupPanel="False"  
                                   AllowEditing="True"  
                                   ShowHorizontalLines="True"  
                                   ShowVerticalLines="True"  
                                   ShowSearchPanelMode="Always" 
                                   SearchPanelHorizontalAlignment="Stretch" 
                                   EnableImmediatePosting="True"
                                   ImmediateUpdateRowPosition="True"
                                   AllowColumnMoving="True"
                                   NavigationStyle="Row"
                                   ShowFixedTotalSummary="True"
                                   ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                   FocusedRow="{Binding SelectedStandardOperation, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                   ShowFilterPopup="{DXEvent Handler='@args.PopupBaseEdit.PopupWidth=200'}" 
                                   IsColumnChooserVisible="{Binding IsViewStandardOperationsDictionaryColumnChooserVisible,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                   AllowBestFit="True"
                                   AllowPerPixelScrolling="True"
                                   AutoMoveRowFocus="False"
                                   AllowScrollAnimation="True"
                                   ShowIndicator="True"
                                   AllowResizing="True"
                                   AllowConditionalFormattingMenu="True"
                                   HorizontalContentAlignment="Center"
                                   UseLegacyFilterEditor="True"
                                   AllowConditionalFormattingManager="True">

                        <dxg:TableView.Resources>
                            <DataTemplate x:Key="StandardOperationsDictionaryListPrintHeaderTemplate">
                                <Grid Width="{Binding UsablePageWidth}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="50" />
                                        <ColumnDefinition Width="130" />
                                    </Grid.ColumnDefinitions>
                                    <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" Height="50" />
                                    <dxe:TextEdit Text="Standard Operations Dictionary Report" IsPrintingMode="True" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" />
                                    <Image Source="D:\Repository\GEOS WorkBench\Src\Modules\PCMModule\Assets\Images\GeosReportLogo.png" Height="50" Width="80"  dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                                </Grid>
                            </DataTemplate>

                            <DataTemplate x:Key="StandardOperationsDictionaryListPrintFooterTemplate">
                                <dxe:TextEdit HorizontalContentAlignment="Center" 
                                                  Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                                  dxp:ExportSettings.TargetType="PageNumber" 
                                                  dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                                  dxp:PageNumberExportSettings.Format="Page {0} of {1}"/>
                            </DataTemplate>

                        </dxg:TableView.Resources>
                        <dxmvvm:Interaction.Behaviors>
                            <dxmvvm:EventToCommand Command="{Binding EditSODCommand}" 
                                                   EventName="MouseDoubleClick"
                                                   CommandParameter="{Binding ElementName=StandardOperationsDictionaryListTableView}">
                                <dxmvvm:EventToCommand.EventArgsConverter>
                                    <dx:EventArgsToDataRowConverter />
                                </dxmvvm:EventToCommand.EventArgsConverter>
                            </dxmvvm:EventToCommand>
                            <dxmvvm:EventToCommand EventName="ShowFilterPopup"
                                               Command="{Binding CustomShowFilterPopupCommand}"
                                               PassEventArgsToCommand="True"/>

                            <dxmvvm:EventToCommand EventName="Loaded"
                                                                                     Command="{Binding STLGridControlLoadedCommand}"
                                                                                    PassEventArgsToCommand="True"
                                                                                     CommandParameter="{Binding ElementName=stdtimegrid}" >
                            </dxmvvm:EventToCommand>

                            <dxmvvm:EventToCommand EventName="ShownColumnChooser"
                                                                                 Command="{Binding StandardTimeTableViewLoadedCommand}"
                                                                                    PassEventArgsToCommand="True"
                                                                                 CommandParameter="{Binding ElementName=StandardOperationsDictionaryListTableView}" >
                            </dxmvvm:EventToCommand>

                            <dxmvvm:EventToCommand EventName="Unloaded"
                                            Command="{Binding BPLGridControlUnloadedCommand}"
                                            PassEventArgsToCommand="True"
                                            CommandParameter="{Binding ElementName=stdtimegrid}" >
                            </dxmvvm:EventToCommand>


                            <dxmvvm:EventToCommand SourceObject="{Binding ElementName=StandardOperationsDictionaryListTableView, Path=FormatConditions}" 
                                                                                                   EventName="CollectionChanged" 
                                                                                                   Command="{Binding STLGridControlUnloadedCommand}"
                                                                                                   CommandParameter="{Binding ElementName=stdtimegrid}"/>

                            <dxmvvm:EventToCommand Command="{Binding StandardOperationsDictionaryListTableViewLoadedCommand}"
                                                                           EventName="Loaded" PassEventArgsToCommand="True" />
                        </dxmvvm:Interaction.Behaviors>
                        
                    </dxg:TableView>
                </dxg:GridControl.View>

            </dxg:GridControl>
        </Grid>
    </Grid>
</UserControl>
