<UserControl x:Class="Emdep.Geos.Modules.OTM.Views.PORequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.OTM.Views"
             xmlns:dxwuin="http://schemas.devexpress.com/winfx/2008/xaml/windowsui/navigation"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:Converters="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxnav="http://schemas.devexpress.com/winfx/2008/xaml/navigation"
             xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
             xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol" 
             xmlns:OTMCommon ="clr-namespace:Emdep.Geos.Modules.OTM.CommonClass"
             xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dxcn="http://schemas.devexpress.com/winfx/2008/xaml/core/internal"
             xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             xmlns:UIConverter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="2000">
    <UserControl.Resources>
        <!--[pramod.misal][04.02.2025][GEOS2-6726]-->
        <!--<UIHelper:ToolTipFlyoutControlBehavior FlyoutControl="{Binding Source={StaticResource flyoutControl}}" />-->
        <Converters:ForegroundColorConverter x:Key="ForegroundColorConverter"/>
        <Converters:ImageConverterByModule x:Key="ImageConverterByModule"/>
        <Converters:ImageUrlToByteArrayConverter x:Key="ImageUrlToByteArrayConverter" />
        <Converters:EmployeeCodeInitialToImageConverter x:Key="EmployeeCodeInitialToImageConverter" />
        <Converters:EditGridCellDataToStringConverter  x:Key="EditGridCellDataToStringConverter" />
        <Converters:CurrencyToImageConverter  x:Key="CurrencyToImageConverter" />
        <Style x:Key="LeftPanelTileItemStyle" TargetType="{x:Type dxnav:TileBarItem}">
            <Setter Property="Foreground" Value="{Binding BackColor,Converter={Converters:ForegroundColorConverter}}"/>
            <Setter Property="BorderBrush" Value="#FFD6D6D6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{Binding BackColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Height" Value="{Binding Height}"/>
            <Setter Property="Width" Value="{Binding Width}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="SelectionPadding" Value="0" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Width="195" Height="75"  >
                            <Label Content="{Binding EntitiesCount,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Right"   Margin="0,-5,8,0" VerticalContentAlignment="Top" FontSize="36" Visibility="{Binding EntitiesCountVisibility}"></Label>
                            <Label Content="{Binding Caption, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Left" Margin="6,0,0,3" VerticalAlignment="Bottom" FontSize="15" ></Label>
                        </StackPanel>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderThickness" Value="5"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource TileBarItemColorStyle}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="LeftPanelTypeTileItemStyle" TargetType="{x:Type dxnav:TileBarItem}">
            <Setter Property="Foreground" Value="{Binding ForeColor,Converter={Converters:ForegroundColorConverter}}"/>
            <Setter Property="BorderBrush" Value="#FFD6D6D6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{Binding BackColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Height" Value="{Binding Height}"/>
            <Setter Property="Width" Value="{Binding Width}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="SelectionPadding" Value="0" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Width="195" Height="75"  >
                                <TextBlock Text="{Binding EntitiesCount,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" FontSize="36" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-5,8,0" Opacity="0.6" Visibility="{Binding EntitiesCountVisibility}"/>
                                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Bottom" Text="{Binding Caption}" FontSize="13" Margin="6,0,0,3"/>
                                <Image HorizontalAlignment="Left" VerticalAlignment="Top" Source="{Binding ImageUri, Converter={StaticResource ImageConverterByModule}, ConverterParameter='OTM'}" dxb:ImageColorizer.IsEnabled="False"
                               Stretch="None" Margin="8,-60,8,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderThickness" Value="5"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource TileBarItemColorStyle}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="HideButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border BorderThickness="0" Background="{TemplateBinding Background}">
                            <Grid>
                                <Image Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Source="/Emdep.Geos.Modules.OTM;component/Assets/Images/ArrowRight.png"
                                   Visibility="{TemplateBinding IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter Inverse=True}}"/>
                                <Image Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Source="/Emdep.Geos.Modules.OTM;component/Assets/Images/ArrowLeft.png"
                                   Visibility="{TemplateBinding IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter}}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="SummaryTemplate">
            <ContentControl>
                <dxg:GridSummaryItem FieldName="{Binding Path=(dxcn:DependencyObjectExtensions.DataContext).FieldName, RelativeSource={RelativeSource Self}}"
                                     SummaryType="{Binding Path=(dxcn:DependencyObjectExtensions.DataContext).Type, RelativeSource={RelativeSource Self}}"
                                     DisplayFormat= "{Binding Path=(dxcn:DependencyObjectExtensions.DataContext).DisplayFormat, RelativeSource={RelativeSource Self}}" />
            </ContentControl>
        </DataTemplate>
    </UserControl.Resources>

    <Grid x:Name="PORequests1" ShowGridLines="False" >
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <!-- Tab PORequests View-->
        <Grid HorizontalAlignment="Right" Grid.Row="0" Grid.Column="1" Height="Auto" Width="Auto" ShowGridLines="False"  Margin="0,-25,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="AUTO"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="AUTO"/>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="AUTO" />
                <ColumnDefinition Width="AUTO" />
                <ColumnDefinition Width="35" />
                <ColumnDefinition Width="AUTO" />
                <ColumnDefinition Width="AUTO" />
                <ColumnDefinition Width="AUTO" />
                <ColumnDefinition Width="AUTO" />
            </Grid.ColumnDefinitions>
            <!--Plant-->
            <DockPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" Margin="25,0,0,0" >
                <Image Style="{DynamicResource OTMPlantImageStyle}" Width="24" Height="24" Margin="7,0,3,0" VerticalAlignment="Center" />
                <TextBlock Text="{DynamicResource CommonPlant}"  FontWeight="Bold" Margin="5,2,5,0" VerticalAlignment="Center" />
            </DockPanel>
            <dxe:ComboBoxEdit       Grid.Column="1"  Width="150"  Height="30"  IncrementalFiltering="True"
                                          VerticalContentAlignment="Center"  AutoComplete="True" VerticalAlignment="Center" HorizontalAlignment="Left" 
                                          ApplyItemTemplateToSelectedItem="True" IsSynchronizedWithCurrentItem="True" IncrementalSearch="False" IsTextEditable="False" 
                                          ItemsSource="{Binding Source={x:Static OTMCommon:OTMCommon.Instance}, Path=UserAuthorizedPlantsList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                          SelectedItem="{Binding Source={x:Static OTMCommon:OTMCommon.Instance}, Path=SelectedSinglePlantForPO,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                          DisplayMember="Alias" Margin="5,0,5,0" >
                <dxe:ComboBoxEdit.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <Image Source="{Binding CountryIconUrl, Converter={StaticResource ImageUrlToByteArrayConverter}, UpdateSourceTrigger=PropertyChanged ,ConverterParameter='image/png'}"  
                                   VerticalAlignment="Center" Height="24" Width="24" Margin="2,0,0,0" />
                            <TextBlock Text="{Binding Alias}" Margin="5,0,0,0" Padding="3" >
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Foreground" Value="Gray" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DockPanel>
                    </DataTemplate>
                </dxe:ComboBoxEdit.ItemTemplate>
                <dxmvvm:Interaction.Triggers>

                    <dxmvvm:EventToCommand EventName="EditValueChanged"         
                                                       Command="{Binding DataContext.CommandPlantEditValueChanged,RelativeSource={RelativeSource AncestorType=UserControl}}"              
                                                       CommandParameter="{Binding ElementName=PORequestesTableView}"/>
                </dxmvvm:Interaction.Triggers>
            </dxe:ComboBoxEdit>
            <TextBlock Text="{DynamicResource OTMPeriod}"  FontWeight="Bold" Grid.Column="3"  VerticalAlignment="Center" />
            <!-- Period -->
            <DockPanel Grid.Column="4"  HorizontalAlignment="Right"   VerticalAlignment="Center" Margin="15,0,0,0" >
                <TextBlock Text="{Binding Source={x:Static OTMCommon:OTMCommon.Instance}, Path=FromDate,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Text=" - " HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding Source={x:Static OTMCommon:OTMCommon.Instance}, Path=ToDate,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0"/>
            </DockPanel>
            <!--Calender Icon  VerticalAlignment="Stretch"-->
            <dxwui:AppBar x:Name="Appbar" HideMode="AlwaysVisible" Background="Transparent" Grid.Column="2" Grid.Row="0"
                                  IsSticky="True" HorizontalAlignment="Stretch" 
                                  IsOpen="True" IsExitButtonEnabled="False">
                <!--Margin="70,0,-30,0"-->
                <dxwui:AppBarButton x:Name="appbarbutton" ToolTip="{DynamicResource PeriodSelect}" HorizontalAlignment="Right" 
                                        GlyphHeight="24" GlyphWidth="24" AllowGlyphTheming="False" Margin="2,0,-30,0"
                                        Style="{DynamicResource CalendarImageGlyphStyle}"
                                        GlyphStretch="Fill" IsEllipseEnabled="False" >
                    <dxwui:AppBarButton.Flyout>
                        <dxwui:MenuFlyout ShowIndicator="True" x:Name="MenuLayout">
                            <dxwui:MenuFlyoutItem StaysOpenOnClick="True">
                                <Grid Width="Auto" Height="Auto">
                                    <DockPanel>
                                        <StackPanel Orientation="Vertical" x:Name="PeriodCategories">
                                            <!--This Year-->
                                            <Button x:Name="ThisYear" Content="{DynamicResource OTMThisYear}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding DataContext.PeriodCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding ElementName=ThisYear}">
                                            </Button>
                                            <!--Last Year-->
                                            <Button x:Name="LastYear" Content="{DynamicResource OTMLastYear}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding DataContext.PeriodCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding ElementName=LastYear}">
                                            </Button>
                                            <!--This Month-->
                                            <Button x:Name="ThisMonth" Content="{DynamicResource OTMThisMonth}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding DataContext.PeriodCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding ElementName=ThisMonth}">
                                            </Button>
                                            <!--Last month-->
                                            <Button x:Name="LastMonth" Content="{DynamicResource OTMLastMonth}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding DataContext.PeriodCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding ElementName=LastMonth}">
                                            </Button>
                                            <!--This week-->
                                            <Button x:Name="ThisWeek" Content="{DynamicResource OTMThisWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding DataContext.PeriodCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding ElementName=ThisWeek}">
                                            </Button>
                                            <!--Last week-->
                                            <Button x:Name="LastWeek" Content="{DynamicResource OTMLastWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding DataContext.PeriodCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding ElementName=LastWeek}">
                                            </Button>
                                            <!--Last 12 month-->
                                            <Button x:Name="Last12Months" Content="{DynamicResource OTMLast12Months}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding DataContext.PeriodCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding ElementName=Last12Months}">
                                            </Button>
                                            <!--Custom Range-->
                                            <Button x:Name="CustomRange" Content="{DynamicResource PeriodCustomRange}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding DataContext.PeriodCustomRangeCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding ElementName=CustomRange}">
                                            </Button>
                                            <StackPanel Orientation="Horizontal" Margin="0,50,0,0">
                                                <Button Content="{DynamicResource OTMApply}" HorizontalAlignment="Center" VerticalAlignment="Center"
             Height="30" Width="60" Background="Yellow" 
             Command="{Binding DataContext.ApplyCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}" 
			 >
                                                    <Button.CommandParameter>
                                                        <MultiBinding Converter="{Converters:ConverterForRefresh}">
                                                            <Binding ElementName="MenuLayout" />
                                                            <Binding ElementName="PORequestGrid" />
                                                        </MultiBinding>
                                                    </Button.CommandParameter>
                                                </Button>
                                                <Button Content="{DynamicResource OTMCancel}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="60" 
                                                            Command="{Binding DataContext.CancelCommand ,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding ElementName=MenuLayout}">
                                                </Button>
                                            </StackPanel>
                                        </StackPanel>
                                        <StackPanel Visibility="{Binding DataContext.IsCalendarVisible, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Orientation="Horizontal">
                                            <StackPanel Orientation="Vertical">
                                                <dxe:TextEdit Text="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                </dxe:TextEdit>
                                                <!--Calendar-->
                                                <dxe:DateEditCalendar DateTime="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Vertical">
                                                <dxe:TextEdit Text="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                </dxe:TextEdit>
                                                <!--Calendar-->
                                                <dxe:DateEditCalendar DateTime="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </DockPanel>
                                </Grid>
                            </dxwui:MenuFlyoutItem>
                        </dxwui:MenuFlyout>
                    </dxwui:AppBarButton.Flyout>
                </dxwui:AppBarButton>
            </dxwui:AppBar>
            <!-- Refresh Button -->
            <Button Content=" " Grid.Row="0" Grid.Column="6" Height="40" Width="40" HorizontalAlignment="Right"  
                            Command="{Binding DataContext.RefreshPORequestsViewCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}" 
                            CommandParameter="{Binding ElementName=PORequestesTableView}" Margin="0,0,5,5">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonRefresh}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{ DynamicResource PORequestsRefreshButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
            <!-- Print Button -->
            <Button Content=" " Grid.Row="0" Grid.Column="7" Height="40" Width="40" HorizontalAlignment="Right"  
                            Command="{Binding DataContext.PrintPORequestsViewCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}" 
                            CommandParameter="{Binding ElementName=PORequestesTableView}" Margin="0,0,5,5">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonPrint}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{ DynamicResource PrinterButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
            <!-- Export Button -->
            <Button Content=" " Grid.Row="0" Grid.Column="8" Height="40" Width="40" HorizontalAlignment="Right"  
                            Command="{Binding DataContext.ExportPORequestsViewCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}" 
                            CommandParameter="{Binding ElementName=PORequestesTableView}" Margin="0,0,5,5">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonExportToExcel}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{ DynamicResource ExcelExportButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </Grid>
        <DockPanel Grid.Row="1" Grid.ColumnSpan="2" 
                       Visibility="{Binding ISVisiblePOReq,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
            <Grid ShowGridLines="False" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="1875*"/>
                    <ColumnDefinition Width="68*"/>
                </Grid.ColumnDefinitions>
                <dxwui:NavigationFrame x:Name="Navigatedframe" AnimationType="None">
                    <dxmvvm:Interaction.Behaviors>
                        <dxwuin:FrameNavigationService/>
                    </dxmvvm:Interaction.Behaviors>
                </dxwui:NavigationFrame>
                <!--Visibility="{Binding ElementName=expandButton, Path=IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter}}"-->
                <UIHelper:DpiResizingPanel Grid.Column="0" DockPanel.Dock="Left" Visibility="{Binding IsTileBarVisible}">
                    <dxnav:TileBar x:Name="tileBar" DockPanel.Dock="Left" 
                               Visibility="{Binding ElementName=expandButton, Path=IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                               Margin="-20,-20,-23,0"
                               SelectedItem="{Binding SelectedTileBarItems,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                               Orientation="Vertical"
                               ItemSpacing="5"
                               ItemsSource="{Binding FilterStatusListOfTiles ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               ItemContainerStyle="{StaticResource LeftPanelTileItemStyle}"  
                               Focusable="True">
                        <dxmvvm:Interaction.Behaviors>
                            <!--<dxmvvm:EventToCommand Command="{Binding CommandFilterPOStatusTileClick}" EventName="SelectionChanged" PassEventArgsToCommand="True"/>
                                    <dxmvvm:EventToCommand Command="{Binding CommandTileBarClickDoubleClick}"  EventName="MouseDoubleClick" CommandParameter="{Binding ElementName=TripsTableView}"/>-->
                            <dxmvvm:EventToCommand Command="{Binding DataContext.CommandFilterPOStatusTileClick,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}" EventName="SelectionChanged" PassEventArgsToCommand="True"/>
                            <dxmvvm:EventToCommand Command="{Binding DataContext.CommandTileBarClickDoubleClick,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}" EventName="MouseDoubleClick" CommandParameter="{Binding ElementName=PORequestesTableView}"/>
                        </dxmvvm:Interaction.Behaviors>
                    </dxnav:TileBar>
                </UIHelper:DpiResizingPanel>
                <!-- Margin="0,108,1875.252,108" SelectionMode="Cell" Visibility="{Binding IsTileBarVisible}"-->
                <ToggleButton x:Name="expandButton"  Grid.Column="1" DockPanel.Dock="Left" Visibility="{Binding IsTileBarVisible}" Style="{StaticResource HideButtonStyle}" IsChecked="True" VerticalAlignment="Center"/>
                <dxg:GridControl x:Name="PORequestGrid" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Grid.Column="2"  AllowLiveDataShaping="True" 
                                 ItemsSource="{Binding PoRequestesList,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" SelectionMode="Cell" AutoGenerateColumns="None"
                                 SelectedItem="{Binding SelectedPoRequest,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="Auto" 
                                 ImplyNullLikeEmptyStringWhenFiltering="True" FilterString="{Binding DataContext.MyFilterString,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}" Grid.ColumnSpan="2" Margin="0.2,0,0,0">
                    <dxg:GridControl.Bands>
                        <dxg:GridControlBand Name="BandPORequest">
                            <!--DateTime-->
                            <!--//[pramod.misal][20.11.2025][PredefinedGeometryStock2DModel-9429] https://helpdesk.emdep.com/browse/GEOS2-9429  Commented GetPORequestDetails_V2670 for GEOS2-9429-->
                            <!--<dxg:GridColumn FieldName="DateTime"  Header="{DynamicResource PODateTime}" MinWidth="120" AllowResizing="true" FixedWidth="True" Visible="False" ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.DateTime,UpdateSourceTrigger=PropertyChanged,StringFormat={}{0:dd/MM/yyyy HH:mm},ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}"  HorizontalAlignment="Center" Margin="5,0,0,0" >
                                                <TextBlock.ToolTip>
                                                    <ToolTip>
                                                        <StackPanel>
                                                            <TextBlock Text ="{Binding RowData.Row.DateTime,UpdateSourceTrigger=PropertyChanged,StringFormat={}{0:dd/MM/yyyy HH:mm},ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" />
                                                        </StackPanel>
                                                    </ToolTip>
                                                </TextBlock.ToolTip>
                                                        </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                                <dxg:GridColumn.PrintCellStyle>
                                    <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                        <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                                        <Setter Property="DisplayTemplate">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <dxe:TextEdit Name="PART_Editor" Text="{Binding RowData.Row.DateTime,StringFormat={}{0:dd/MM/yyyy HH:mm},ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left"
                                                               IsPrintingMode="True">
                                                    </dxe:TextEdit>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxg:GridColumn.PrintCellStyle>
                            </dxg:GridColumn>-->

                            <!--Date-->
                            <!--[pramod.misal][20.11.2025][PredefinedGeometryStock2DModel-9429] https://helpdesk.emdep.com/browse/GEOS2-9429-->  
                            <dxg:GridColumn FieldName="Date" Header="{DynamicResource OTMPODateTitle}"  MaxWidth="82" Width="82" MinWidth="82"  AllowResizing="true"  FixedWidth="True" Visible="True" ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.Date,UpdateSourceTrigger=PropertyChanged,StringFormat={}{0:dd/MM/yyyy},ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}"  HorizontalAlignment="Center" Margin="5,0,0,0" >
                                                <TextBlock.ToolTip>
                                                    <ToolTip>
                                                        <StackPanel>
                                                            <TextBlock Text ="{Binding RowData.Row.Date,UpdateSourceTrigger=PropertyChanged,StringFormat={}{0:dd/MM/yyyy},ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" />
                                                        </StackPanel>
                                                    </ToolTip>
                                                </TextBlock.ToolTip>
                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                                <dxg:GridColumn.PrintCellStyle>
                                    <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                        <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                                        <Setter Property="DisplayTemplate">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <dxe:TextEdit Name="PART_Editor" Text="{Binding RowData.Row.Date,StringFormat={}{0:dd/MM/yyyy},ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left"
                                   IsPrintingMode="True">
                                                    </dxe:TextEdit>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxg:GridColumn.PrintCellStyle>
                            </dxg:GridColumn>
                            
                            <!--Time-->
                            <!--[pramod.misal][20.11.2025][PredefinedGeometryStock2DModel-9429] https://helpdesk.emdep.com/browse/GEOS2-9429-->
                            <dxg:GridColumn FieldName="Time" Header="{DynamicResource POTime}" Width="50" MaxWidth="50" MinWidth="50" AllowResizing="true" FixedWidth="True" Visible="True" ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.Time,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" >
                                                <TextBlock.ToolTip>
                                                    <ToolTip>
                                                        <StackPanel>
                                                            <TextBlock Text ="{Binding RowData.Row.Time,UpdateSourceTrigger=PropertyChanged}" />
                                                        </StackPanel>
                                                    </ToolTip>
                                                </TextBlock.ToolTip>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                                <dxg:GridColumn.PrintCellStyle>
                                    <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                        <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                                        <Setter Property="DisplayTemplate">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <dxe:TextEdit Name="PART_Editor" Text="{Binding RowData.Row.Time}" HorizontalAlignment="Left"
                                                 IsPrintingMode="True">
                                                    </dxe:TextEdit>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxg:GridColumn.PrintCellStyle>
                            </dxg:GridColumn>
                            <!--Sender-->
                            <!--[pramod.misal][04.02.2025][GEOS2-6726]-->
                            <dxg:GridColumn FieldName="SenderName"  Header="{DynamicResource POSender}" 
                                                  MinWidth="100"  Width="170" ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.SenderName,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>


                            <!-- To  Recipient-->
                            <!--[pramod.misal][04.02.2025][GEOS2-6726]-->
                            <dxg:GridColumn FieldName="ToRecipientName" Header="{DynamicResource PORecipient}" MinWidth="100" Width="140"
                                                 ReadOnly="True" AllowUnboundExpressionEditor="False"   FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl 
                                                          ItemsSource="{Binding RowData.Row.ToRecipientNameList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto"                              
                                                          Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" VerticalAlignment="Center" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="auto" Margin="2">
                                                        <TextBlock Text="{Binding RecipientName, UpdateSourceTrigger=PropertyChanged}"  Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>


                                <dxg:GridColumn.PrintCellStyle>
                                    <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                        <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                                        <Setter Property="DisplayTemplate">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <dxe:TextEdit Name="PART_Editor" Text="{Binding Row.ToRecipientName}" HorizontalAlignment="Left"
                                                     IsPrintingMode="True">
                                                    </dxe:TextEdit>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxg:GridColumn.PrintCellStyle>

                            </dxg:GridColumn>

                            <!--CC Recipients -->
                            <!-- Commented for [pramod.misal][04.02.2025][GEOS2-6726]-->
                            <!--[pramod.misal][04.02.2025][GEOS2-6726]-->
                            <dxg:GridColumn FieldName="CCName" Header="{DynamicResource CCRecipient}" MinWidth="100" Width="150"
                                                        ReadOnly="True" AllowUnboundExpressionEditor="False" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl  ItemsSource="{Binding RowData.Row.CCNameList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto"                              
                                                          Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" VerticalAlignment="Center" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="auto" Margin="2">
                                                        <TextBlock Text="{Binding CCName, UpdateSourceTrigger=PropertyChanged}"  Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                                <dxg:GridColumn.PrintCellStyle>
                                    <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                        <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                                        <Setter Property="DisplayTemplate">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <dxe:TextEdit Name="PART_Editor" Text="{Binding Row.CCName}" HorizontalAlignment="Left"
                                                      IsPrintingMode="True">
                                                    </dxe:TextEdit>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxg:GridColumn.PrintCellStyle>
                            </dxg:GridColumn>
                            <!--Inbox-->
                            <dxg:GridColumn FieldName="Inbox" Header="{DynamicResource POInbox}" MinWidth="100" Width="160"  ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Inbox" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Height="24" VerticalAlignment="Center">
                                            <!-- Mail Icon -->
                                            <Image Style="{DynamicResource OTMMailIconImageStyle}" Height="20" Width="20" Margin="5,0,0,0" />
                                            <!-- Email TextBlock -->
                                            <TextBlock Text="{Binding Data.Inbox, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource HyperlinkTextBlock}" TextDecorations="Underline"  Margin="5,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Subject-->
                            <dxg:GridColumn FieldName="Subject" Header="{DynamicResource POSubject}" MinWidth="100" Width="200"  ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Subject" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.Subject,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--[pramod.misal][GEOS2-6719][14-02-2025]-->
                            <!--Group-->
                            <dxg:GridColumn FieldName="Group" Header="{DynamicResource OTMPOGridGroup}"  
                         MinWidth="90" Width="90" ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Group" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.Group,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--IdCustomerGroup-->
                            <!--<dxg:GridColumn FieldName="IdCustomerGroup"  Visible="False" 
                                    MinWidth="90" Width="90" ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="IdCustomerGroup" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.IdCustomerGroup,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>-->
                            <!--[rahul.gadhave][GEOS2-9020][23.07.2025] -->
                            <!--IdCustomerPlant-->
                            <!--<dxg:GridColumn FieldName="IdCustomerPlant"  Visible="False"
                                 MinWidth="90" Width="90" ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="IdCustomerPlant" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.IdCustomerPlant,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>-->
                            <!--[pramod.misal][GEOS2-6719][14-02-2025]-->
                            <!--Plant-->
                            <dxg:GridColumn FieldName="Plant"   Header="{DynamicResource OTMPOGridPlant}"  
                             Width="100" MinWidth="100" ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Plant" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.Plant,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Requester-->
                            <dxg:GridColumn FieldName="Requester" Header="{DynamicResource PORequester}" MinWidth="100"  Width="150" ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Requester" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.Requester,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Offers-->
                            <dxg:GridColumn FieldName="Offers" Header="{DynamicResource POOffers}" MinWidth="100" Width="100" ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Offers" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="auto" Margin="2">
                                            <TextBlock Text="{Binding RowData.Row.Offers, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Attachment Count-->
                            <!--[pooja.jadhav][17.02.2025][GEOS2-6724]-->
                            <!--commenting code for task-->
                            <!--[pooja.jadhav][17.02.2025][GEOS2-6724]-->


                            <!--Attachment Count-->
                            <!--<dxg:GridColumn Header="" FieldName="AttachmentCnt" Width="50" MinWidth="50" FixedWidth="True" FilterPopupMode="CheckedList" ReadOnly="True">
                                <dxg:GridColumn.HeaderCustomizationAreaTemplate>
                                    <DataTemplate>
                                        <Image Height="20" Width="26" HorizontalAlignment="Stretch" Style="{DynamicResource OTMAttachmentIconImageStyle}" />
                                    </DataTemplate>
                                </dxg:GridColumn.HeaderCustomizationAreaTemplate>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <dxg:GridControl SelectionMode="None"  ItemsSource="{Binding RowData.Row.EmailAttachmentDetailsList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                                                     Width="auto"  Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" VerticalAlignment="Center" ShowBorder="False">
                                            -->
                            <!--EmailAttachmentList-->
                            <!--
                                            <dxg:GridControl.Columns>
                                                <dxg:GridColumn FieldName="AttachmentCnt" FilterPopupMode="CheckedList" Width="*" >
                                                    <dxg:GridColumn.CellTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Horizontal" Height="24">
                                                                <TextBlock Text ="{Binding RowData.Row.AttachmentCnt,UpdateSourceTrigger=PropertyChanged }" VerticalAlignment="Center"  HorizontalAlignment="Center" Margin="5,0,0,0">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                             -->
                            <!--Default value for TextDecorations-->
                            <!-- 
                                                                            <Setter Property="TextDecorations" Value="Underline"/>
                                                                            <Setter Property="Foreground" Value="#008ECC"></Setter>
                                                                            <Style.Triggers>
                                                                                 -->
                            <!--Trigger for when AttachmentCnt is empty-->
                            <!-- 
                                                                                <DataTrigger Binding="{Binding RowData.Row.AttachmentCnt}" Value="">
                                                                                    <Setter Property="TextDecorations" Value="{x:Null}" />
                                                                                </DataTrigger>
                                                                                 -->
                            <!--Trigger for when AttachmentCnt is null-->
                            <!-- 
                                                                                <DataTrigger Binding="{Binding RowData.Row.AttachmentCnt}" Value="{x:Null}">
                                                                                    <Setter Property="TextDecorations" Value="{x:Null}" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </dxg:GridColumn.CellTemplate>
                                                </dxg:GridColumn>
                                            </dxg:GridControl.Columns>
                                            <dxmvvm:Interaction.Behaviors>
                                                <UIHelper:ToolTipAttachmentCountflyoutControlBehaviour x:Name="AttachmentCountflyoutControlBehaviour" FlyoutControl="{Binding ElementName=AttachmentCountflyoutControl}">
                                                </UIHelper:ToolTipAttachmentCountflyoutControlBehaviour>
                                            </dxmvvm:Interaction.Behaviors>
                                            <dxg:GridControl.View>
                                                <dxg:TableView Name="treeListView1" ShowSearchPanelMode="Never"                                    
                                                                       ShowDataNavigator="False" ShowColumnHeaders="False"                                    
                                                                       NavigationStyle="None" ShowHorizontalLines="False" ShowDetailButtons="false"                                    
                                                                       ShowVerticalLines="False" ShowGroupPanel="False" ShowIndicator="False" 
                                                                       VerticalScrollbarVisibility="Auto" HorizontalAlignment="Stretch"/>
                                            </dxg:GridControl.View>
                                        </dxg:GridControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>-->

                            <dxg:GridColumn FieldName="AttachmentCnt" Header=""   Width="50" MinWidth="50"  AllowPrinting="False"
                             ReadOnly="True" AllowUnboundExpressionEditor="False" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.HeaderCustomizationAreaTemplate>
                                    <DataTemplate>
                                        <Image Height="20" Width="26" HorizontalAlignment="Stretch" Style="{DynamicResource OTMAttachmentIconImageStyle}" />
                                    </DataTemplate>
                                </dxg:GridColumn.HeaderCustomizationAreaTemplate>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl  ItemsSource="{Binding RowData.Row.EmailAttachmentList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto"                              
                                                         Height="auto" MaxHeight="300" HorizontalAlignment="Left" Margin="5,0,0,0" VerticalAlignment="Center" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel  Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Height="auto" Margin="-20">
                                                        <TextBlock Text ="{Binding AttachmentCnt,UpdateSourceTrigger=PropertyChanged }" VerticalAlignment="Center"  HorizontalAlignment="Center" Margin="5,0,0,0">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <!--Default value for TextDecorations-->
                                                                    <Setter Property="TextDecorations" Value="Underline"/>
                                                                    <Setter Property="Foreground" Value="#008ECC"></Setter>
                                                                    <Style.Triggers>
                                                                        <!--Trigger for when AttachmentCnt is empty-->
                                                                        <DataTrigger Binding="{Binding AttachmentCnt}" Value="">
                                                                            <Setter Property="TextDecorations" Value="{x:Null}" />
                                                                        </DataTrigger>
                                                                        <!--Trigger for when AttachmentCnt is null-->
                                                                        <DataTrigger Binding="{Binding AttachmentCnt}" Value="{x:Null}">
                                                                            <Setter Property="TextDecorations" Value="{x:Null}" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>

                            </dxg:GridColumn>

                            <!--Attachments-->


                            <dxg:GridColumn FieldName="Attachments" Header="{DynamicResource POAttachments}"  MinWidth="100" Width="200"  ReadOnly="True" FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Attachments" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <!--[rdixit][22.10.2024][Bet changes for task GEOS2-6520]-->
                                <!--[nsatpute][23.10.2024][Beta changes for task GEOS2-6520]-->
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl x:Name="PART_GridControl" VerticalAlignment="Center"
                                                    ItemsSource="{Binding RowData.Row.EmailAttachmentList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto"                              
                                                    Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Height="auto" Margin="2">
                                                        <Image Source="{Binding AttachmentImage,UpdateSourceTrigger=PropertyChanged}" Height="20" Width="20" OpacityMask="{Binding Path=AttachmentImage}">
                                                            <Image.Style>
                                                                <Style TargetType="Image">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding AttachmentName}" Value="">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding AttachmentName}" Value="{x:Null}">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Image.Style>
                                                        </Image>
                                                        <!--<TextBlock FF083493  Text="{Binding RowData.Row.AttachmentName, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource HyperlinkTextBlock}" TextDecorations="Underline" Margin="5,0,0,0"/>-->
                                                        <TextBlock >
                                                                                <Hyperlink  x:Name="hyperlink_Code" Command="{Binding DataContext.OpenAttachmentCommand ,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding }" TextDecorations="Underline">
                                                                                    <Run Text="{Binding AttachmentName, UpdateSourceTrigger=PropertyChanged}" />   
                                                                                </Hyperlink>
                                                        </TextBlock>

                                                    </StackPanel>
                                                    <DataTemplate.Triggers>
                                                        <Trigger   Property="dxg:DataViewBase.IsFocusedRow" Value="True">
                                                            <Setter TargetName="hyperlink_Code" Property="Foreground" Value="{DynamicResource OTMColorMain}" />
                                                        </Trigger>

                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="hyperlink_Code" Property="Foreground" Value="OrangeRed" />
                                                        </Trigger>

                                                    </DataTemplate.Triggers>








                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--PO found-->
                            <dxg:GridColumn FieldName="POFound" Header="{DynamicResource POPOfound}" MinWidth="70" Width="70" ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="POFound" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.POFound,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>

                            <!--Status-->
                            <dxg:GridColumn Header="{DynamicResource POStatus}" FieldName="PORequestStatus.Value"  FilterPopupMode="CheckedList" MinWidth="100" Width="100" ReadOnly="True">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock  Text ="{Binding RowData.Row.PORequestStatus.Value,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"  VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock" >
                                                    <Setter Property="Background" Value="{Binding RowData.Row.PORequestStatus.HtmlColor,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                                                    <Setter Property="HorizontalAlignment"  Value="Stretch"/>
                                                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                                                    <Setter Property="Foreground" Value="{Binding Path=Background,Converter={StaticResource ForegroundColorConverter}}"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                                <dxg:GridColumn.PrintCellStyle>
                                    <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                        <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                                        <Setter Property="DisplayTemplate">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <dxe:TextEdit Name="PART_Editor" Text="{Binding RowData.Row.PORequestStatus.Value}" HorizontalAlignment="Stretch" IsPrintingMode="True">
                                                    </dxe:TextEdit>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxg:GridColumn.PrintCellStyle>
                            </dxg:GridColumn>
                            <!--PO FIELDS-->
                        </dxg:GridControlBand>
                        <!--</dxg:GridControl.Bands>-->
                        <!--<dxg:GridControl.Bands>-->
                        <dxg:GridControlBand Name="BandPORequestField" Header="PO FIELDS" HorizontalHeaderContentAlignment="Center">
                            <!--Customer-->
                            <dxg:GridColumn FieldName="Customer" Header="{DynamicResource POCustomer}" MinWidth="100"  Width="100" ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Customer" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto" VerticalAlignment="Center"                             
                                                Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Height="auto" Margin="2">
                                                        <TextBlock Text="{Binding Customer, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--[rahul.gadhave][GEOS2-6718][29-01-2025]-->
                            <!--Invoice To-->
                            <dxg:GridColumn FieldName="InvoioceTO" Header="{DynamicResource POInVoiceTO}" MinWidth="100" Width="200"  ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="InvoioceTO" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto" VerticalContentAlignment="Center"    VerticalAlignment="Center"                          
                                                Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Height="auto" Margin="2,0,0,0">
                                                        <TextBlock Text="{Binding InvoiceTO,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Date issued-->
                            <dxg:GridColumn Header="{DynamicResource PODateIssued}" FieldName="DateIssued" MinWidth="120" FixedWidth="True" FilterPopupMode="CheckedList" ReadOnly="True">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="DateIssued" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto"  VerticalAlignment="Center"                            
                                            Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Height="auto" Margin="2">
                                                        <TextBlock Text="{Binding DateIssued, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Number-->
                            <dxg:GridColumn FieldName="PONumber" Header="{DynamicResource PONumber}" MinWidth="100"  Width="100" ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="PONumber" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto" VerticalAlignment="Center"                            
        Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Height="auto" Margin="2">
                                                        <TextBlock Text="{Binding PONumber, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--[rahul.gadhave][GEOS2-6718][29-01-2025]-->
                            <!--Offer-->
                            <dxg:GridColumn FieldName="Offer" Header="{DynamicResource POOffer}" MinWidth="100"  Width="100" ReadOnly="True"   FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Offer" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Height="24">
                                            <TextBlock Text ="{Binding RowData.Row.Offer,UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Contact-->
                            <dxg:GridColumn FieldName="Contact" Header="{DynamicResource POContact}" MinWidth="100" Width="140" ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Contact" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto" VerticalAlignment="Center"  VerticalContentAlignment="Center"                           
                                                Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel   Orientation="Horizontal" Height="auto" Margin="2">
                                                        <TextBlock Text="{Binding Contact, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Amount-->
                            <dxg:GridColumn Header="{DynamicResource POAmount}" FieldName="TransferAmount" FilterPopupMode="CheckedList" MinWidth="90" Width="90" ReadOnly="True">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="TransferAmount" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto" VerticalAlignment="Center"                             
                                            Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Height="auto" Margin="2">
                                                        <TextBlock Text="{Binding TransferAmount, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                                <dxg:GridColumn.PrintCellStyle>
                                    <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                        <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                                        <Setter Property="DisplayTemplate">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <!-- Apply the same logic in PrintCellStyle if needed -->
                                                    <dxe:TextEdit Name="PART_Editor" VerticalAlignment="Center" HorizontalAlignment="Right" IsPrintingMode="True">
                                                        <dxe:TextEdit.Style>
                                                            <Style TargetType="dxe:TextEdit">
                                                                <Setter Property="Text" Value="{Binding RowData.Row.TransferAmount, StringFormat={}{0:N2}, ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" />
                                                                <Style.Triggers>
                                                                    <!-- Trigger to set Text to null if TransferAmount is 0.00 -->
                                                                    <DataTrigger Binding="{Binding RowData.Row.TransferAmount}" Value="0.00">
                                                                        <Setter Property="Text" Value="{x:Null}" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </dxe:TextEdit.Style>
                                                    </dxe:TextEdit>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxg:GridColumn.PrintCellStyle>
                            </dxg:GridColumn>
                            <!--Currency-->
                            <dxg:GridColumn Header="{DynamicResource POCurrency}" FieldName="Currency" FilterPopupMode="CheckedList" Width="80" MinWidth="80" FixedWidth="False" AllowResizing="true" ReadOnly="True">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="Currency" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto" VerticalAlignment="Center"                             
        Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel VerticalAlignment="Center"   Orientation="Horizontal" Height="auto" Margin="2">
                                                        <StackPanel.Resources>
                                                            <Style TargetType="dxe:ImageEdit">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Currency, UpdateSourceTrigger=PropertyChanged}" Value="{x:Null}">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Currency, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </StackPanel.Resources>
                                                        <dxe:ImageEdit ShowMenu="False" Height="20" Width="30" VerticalAlignment="Center" HorizontalAlignment="Stretch" Stretch="UniformToFill"   
                                                                              Source="{Binding Currency,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource CurrencyToImageConverter}, ConverterParameter='image/png'}"                                    
                                                                              IsReadOnly="True"/>
                                                        <TextBlock Text="{Binding Currency, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                                <dxg:GridColumn.PrintCellStyle>
                                    <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                        <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                                        <Setter Property="DisplayTemplate">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <dxe:TextEdit Name="PART_Editor" Text="{Binding Row.Currency}" HorizontalAlignment="Left" VerticalAlignment="Center"
                                                                    IsPrintingMode="True">
                                                    </dxe:TextEdit>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxg:GridColumn.PrintCellStyle>
                            </dxg:GridColumn>
                            <!--Ship To-->
                            <dxg:GridColumn FieldName="ShipTo" Header="{DynamicResource POShipTo}" MinWidth="100" Width="200" ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="ShipTo" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto" VerticalAlignment="Center"                             
Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Height="auto" Margin="2">
                                                        <TextBlock Text="{Binding ShipTo, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Incoterms-->
                            <dxg:GridColumn FieldName="POIncoterms" Header="{DynamicResource POIncoterms}" MinWidth="100" Width="150" ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="POIncoterms" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto"  VerticalContentAlignment="Center"                            
                                            Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel  Orientation="Horizontal" Height="auto"  Margin="2">
                                                        <TextBlock Text="{Binding Incoterm, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                            <!--Payment terms-->
                            <dxg:GridColumn FieldName="POPaymentTerm" Header="{DynamicResource POPaymentTerms}" MinWidth="100" Width="150" ReadOnly="True"  FilterPopupMode="CheckedList">
                                <dxg:GridColumn.CellToolTipBinding>
                                    <Binding Path="POPaymentTerm" />
                                </dxg:GridColumn.CellToolTipBinding>
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <ItemsControl ItemsSource="{Binding RowData.Row.PoData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto"   VerticalAlignment="Center"                           
                                                    Height="auto" MaxHeight="300" HorizontalAlignment="Stretch" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Height="auto" Margin="2">
                                                        <TextBlock Text="{Binding POPaymentTerm, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                        </dxg:GridControlBand>
                        <!--Status renove and take in 1st band for https://helpdesk.emdep.com/browse/GEOS2-9043 -->
                        <!--<dxg:GridControlBand>
                            <dxg:GridColumn Header="{DynamicResource POStatus}" FieldName="PORequestStatus.Value"  FilterPopupMode="CheckedList" MinWidth="100" Width="100" ReadOnly="True">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock  Text ="{Binding RowData.Row.PORequestStatus.Value,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"  VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock" >
                                                    <Setter Property="Background" Value="{Binding RowData.Row.PORequestStatus.HtmlColor,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                                                    <Setter Property="HorizontalAlignment"  Value="Stretch"/>
                                                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                                                    <Setter Property="Foreground" Value="{Binding Path=Background,Converter={StaticResource ForegroundColorConverter}}"/>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                                <dxg:GridColumn.PrintCellStyle>
                                    <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                                        <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                                        <Setter Property="DisplayTemplate">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <dxe:TextEdit Name="PART_Editor" Text="{Binding RowData.Row.PORequestStatus.Value}" HorizontalAlignment="Stretch" IsPrintingMode="True">
                                                    </dxe:TextEdit>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxg:GridColumn.PrintCellStyle>
                            </dxg:GridColumn>
                        </dxg:GridControlBand>-->
                    </dxg:GridControl.Bands>
                    <dxmvvm:Interaction.Behaviors>
                        <UIHelper:ToolTipFlyoutControlBehavior x:Name="flyoutGridControlBehavior" FlyoutControl="{Binding ElementName=flyoutControl}">
                        </UIHelper:ToolTipFlyoutControlBehavior>
                        <UIHelper:ToolTipCCRecFlyoutControlBehavior x:Name="flyoutGridControlCCRecBehavior" FlyoutControl="{Binding ElementName=CCflyoutControl}">
                        </UIHelper:ToolTipCCRecFlyoutControlBehavior>
                        <!--[pramod.misal][GEOS2-7030][28.02.2025]-->
                        <UIHelper:ToolTipTORecFlyoutControlBehavior x:Name="TOflyoutGridControlBehavior" FlyoutControl="{Binding ElementName=TOflyoutControl}">
                        </UIHelper:ToolTipTORecFlyoutControlBehavior>
                        <!--[pramod.misal][GEOS2-7030][28.02.2025]-->
                        <UIHelper:ToolTipAttachmentCountflyoutControlBehaviour x:Name="AttachmentCountflyoutControlBehaviour" FlyoutControl="{Binding ElementName=AttachmentCountflyoutControl}">
                        </UIHelper:ToolTipAttachmentCountflyoutControlBehaviour>
                    </dxmvvm:Interaction.Behaviors>
                    <dxg:GridControl.TotalSummary>
                        <dxg:GridSummaryItem x:Name="summary" FieldName="Name" SummaryType="Count" DisplayFormat="Total Count: {0}" Visible="True" Alignment="Left"/>
                    </dxg:GridControl.TotalSummary>
                    <!--FocusedRow="{Binding Path=SelectedPoRequest,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"-->
                    <dxg:GridControl.View>
                        <dxg:TableView x:Name="PORequestesTableView"
                                   NavigationStyle="Row"
                                   RowDetailsVisibilityMode="Visible"
                                   ShowHorizontalLines="True"
                                   ShowSearchPanelMode="Always"
                                   ShowColumnHeaders="True"
                                   VerticalScrollbarVisibility="Auto" 
                                   HorizontalScrollbarVisibility="Auto"
                                   AllowFixedColumnMenu="False"
                                   ShowFixedTotalSummary="True" 
                                   SearchPanelHorizontalAlignment="Stretch"
                                   ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                   AllowGrouping="True"
                                   ShowGroupPanel="False"     
                                   FocusedRow="{Binding Path=SelectedPoRequest,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                   AllowConditionalFormattingMenu="False"
                                   FilterEditorShowOperandTypeIcon="True"
                                   AllowColumnMoving="True"
                                   ShowIndicator="False"
                                   AutoWidth="False"                                       
                                   EnableImmediatePosting="True"
                                   ImmediateUpdateRowPosition="True"
                                   AllowEditing="True"
                                   ShowTotalSummary="False"
                                   FilterRowDelay="10"
                                   IsColumnChooserVisible="{Binding DataContext.IsPOrequestsColumnChooserVisible,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   UseLegacyFilterEditor="True"
                                   NewItemRowPosition="None"
                                   RowDetailsTemplate="{Binding SelectedItem.DetailTemplate, ElementName=rowTemplateComboBox}"
                                   RowStyle="{Binding SelectedItem.RowStyle, ElementName=rowTemplateComboBox}"
                                   ShowFilterPopup="{DXEvent Handler='@args.PopupBaseEdit.PopupWidth=200'}">
                            <dxg:TableView.Resources>
                                <DataTemplate x:Key="PORequestsPrintHeaderTemplate">
                                    <Grid Width="{Binding UsablePageWidth}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="50" />
                                            <ColumnDefinition Width="130" />
                                        </Grid.ColumnDefinitions>
                                        <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" Height="50" />
                                        <dxe:TextEdit Text="PO Requests Report" IsPrintingMode="True" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" />
                                        <Image Source="/Emdep.Geos.Modules.OTM;component/Assets/Images/GeosReportLogo.png" Height="50" Width="80"  dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                                    </Grid>
                                </DataTemplate>
                                <DataTemplate x:Key="PORequestsPrintFooterTemplate">
                                    <dxe:TextEdit HorizontalContentAlignment="Center" 
                                                  Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                                  dxp:ExportSettings.TargetType="PageNumber" 
                                                  dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                                  dxp:PageNumberExportSettings.Format="Page {0} of {1}"/>
                                </DataTemplate>
                            </dxg:TableView.Resources>
                            <dxmvvm:Interaction.Behaviors>
                                <dxmvvm:EventToCommand Command="{Binding DataContext.FilterEditorCreatedCommand, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                   EventName="FilterEditorCreated"
                                                   PassEventArgsToCommand="True"/>
                                <dxmvvm:EventToCommand Command="{Binding DataContext.TableViewLoadedCommand,RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                   EventName="Loaded"
                                                   PassEventArgsToCommand="True" />

                                <dxmvvm:EventToCommand Command="{Binding DataContext.TableViewUnloadedLoadedCommand,RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                       EventName="Unloaded"
                                                       PassEventArgsToCommand="True" />
                                <!--[pramod.misal][04.02.2025][GEOS2-6726]-->
                                <dxmvvm:EventToCommand EventName="ShowFilterPopup"
                                                           Command="{Binding DataContext.CustomShowFilterPopupCommand,RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                           PassEventArgsToCommand="True"/>

                                <!--[pramod.misal][14.04.2025][GEOS2-7244]-->
                                <dxmvvm:EventToCommand  Command="{Binding DataContext.CommandPoRequestGridDoubleClick,RelativeSource={RelativeSource AncestorType=UserControl}}" EventName="MouseDoubleClick" CommandParameter="{Binding ElementName=PORequestesTableView}">
                                    <dxmvvm:EventToCommand.EventArgsConverter>
                                        <dx:EventArgsToDataRowConverter   />
                                    </dxmvvm:EventToCommand.EventArgsConverter>
                                </dxmvvm:EventToCommand>
                                <!--[pramod.misal][GEOS2-7030][28.02.2025] This features must only be delivered after we have a solution to the persistance of changes issue: https://helpdesk.emdep.com/browse/GEOS2-9358. -->
                                <UIHelper:FixedColumnHelper >
                                </UIHelper:FixedColumnHelper>


                            </dxmvvm:Interaction.Behaviors>
                            
                        </dxg:TableView>
                    </dxg:GridControl.View>
                </dxg:GridControl>
                <!--[pramod.misal][04.02.2025][GEOS2-6726]-->
                <!--flyoutControl-->
                <dxe:FlyoutControl x:Name="flyoutControl" StaysOpen="True" BorderThickness="2" Padding="3"  
                                Content="{Binding RelativeSource={RelativeSource Self}, Path=PlacementTarget.DataContext}">
                    <dxe:FlyoutControl.ContentTemplate>
                        <DataTemplate>
                            <Grid ShowGridLines="False">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0" Margin="3,2,0,2" Height="60" Width="60">
                                    <Ellipse Stroke="Black" StrokeThickness="2" Fill="Transparent"/>
                                    <Image Height="56" Width="56" HorizontalAlignment="Center" VerticalAlignment="Center"
													           Stretch="UniformToFill"
													           Source="{Binding Row.SenderEmployeeCodeWithInitialLetters, Converter={StaticResource EmployeeCodeInitialToImageConverter}, UpdateSourceTrigger=PropertyChanged ,ConverterParameter='image/png'}">
                                        <Image.Clip>
                                            <EllipseGeometry Center="28,28" RadiusX="28" RadiusY="28"/>
                                        </Image.Clip>
                                    </Image>
                                </Grid>
                                <StackPanel Orientation="Vertical"  Grid.Column="1">
                                    <TextBlock Text="{Binding Row.SenderName}" FontSize="14" Style="{DynamicResource TextBlock16Style}" />
                                    <TextBlock Text="{Binding Row.SenderJobDescriptionTitle}"  FontSize="12"  Style="{DynamicResource TextBlock16Style}" FontStyle="Italic"/>
                                    <StackPanel Orientation="Horizontal" Height="24" VerticalAlignment="Center">
                                        <Image Style="{DynamicResource OTMMailIconImageStyle}" Height="20" Width="20" Margin="0,0,5,0" />
                                        <TextBlock Style="{DynamicResource HyperlinkTextBlock}" Margin="2,0,0,0">
                                                        <Hyperlink Command="{Binding DataContext.HyperlinkForEmail,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Row.Sender}" TextDecorations="Underline">
                                                            <Run Text="{Binding Row.Sender, UpdateSourceTrigger=PropertyChanged}" />
                                                        </Hyperlink>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </dxe:FlyoutControl.ContentTemplate>
                </dxe:FlyoutControl>
                <!--[pooja.jadhav][17.02.2025][GEOS2-6724]-->
                <!--AttachmentCountflyoutControl-->

                <!--<dxe:FlyoutControl x:Name="AttachmentCountflyoutControlold" StaysOpen="True" BorderThickness="2" Padding="3"  
                                 Content="{Binding RelativeSource={RelativeSource Self}, Path=PlacementTarget.DataContext.EmailAttachmentList}">
                    <dxe:FlyoutControl.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                                <ItemsControl ItemsSource="{Binding}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5">
                                                <Image Source="{Binding AttachmentImage}" Height="20" Width="20" OpacityMask="{Binding AttachmentImage}">
                                                    <Image.Style>
                                                        <Style TargetType="Image">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding AttachmentName}" Value="">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding AttachmentName}" Value="{x:Null}">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Image.Style>
                                                </Image>
                                                <TextBlock Style="{DynamicResource HyperlinkTextBlock}" Margin="5,0,0,0">
                                                                <Hyperlink Command="{Binding DataContext.OpenAttachmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                                        CommandParameter="{Binding}" TextDecorations="Underline">
                                                                    <Run Text="{Binding AttachmentName}" />
                                                                </Hyperlink>
                                                </TextBlock>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </dxe:FlyoutControl.ContentTemplate>
                </dxe:FlyoutControl>-->


                <dxe:FlyoutControl x:Name="AttachmentCountflyoutControl" StaysOpen="True" BorderThickness="2" Padding="3"  
                 Content="{Binding RelativeSource={RelativeSource Self}, Path=DataContext}">
                    <dxe:FlyoutControl.ContentTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ShowGridLines="False">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="30"/>
                                                <!-- Adjusted width for image column -->
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Image in First Column -->
                                            <Image Grid.Column="0" Source="{Binding AttachmentImage}" Height="20" Width="20" 
           OpacityMask="{Binding AttachmentImage}" VerticalAlignment="Center">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding AttachmentName, UpdateSourceTrigger=PropertyChanged}" Value="">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding AttachmentName, UpdateSourceTrigger=PropertyChanged}" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>

                                            <!-- Attachment Name in Second Column -->
                                            <TextBlock Grid.Column="1" Style="{DynamicResource HyperlinkTextBlock}" VerticalAlignment="Center">
        <Hyperlink Command="{Binding DataContext.OpenAttachmentCommand,RelativeSource={RelativeSource AncestorType=UserControl}}" 
                   CommandParameter="{Binding}" TextDecorations="Underline">
            <Run Text="{Binding AttachmentName, UpdateSourceTrigger=PropertyChanged}" />
        </Hyperlink>
                                            </TextBlock>
                                        </Grid>


                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </dxe:FlyoutControl.ContentTemplate>
                </dxe:FlyoutControl>
                <!--[pramod.misal][04.02.2025][GEOS2-6726]-->
                <!--TOflyoutControl-->

                <dxe:FlyoutControl x:Name="TOflyoutControl" StaysOpen="True" BorderThickness="2" Padding="3"
                                    Content="{Binding RelativeSource={RelativeSource Self}, Path=DataContext}">
                    <dxe:FlyoutControl.ContentTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ShowGridLines="False" Height="64" Width="300">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid Grid.Column="0" Margin="2,2,0,2" Height="60" Width="60">
                                                <Ellipse Stroke="Black" StrokeThickness="2" Fill="Transparent"/>
                                                <Image Height="56" Width="56" HorizontalAlignment="Center" VerticalAlignment="Center"
				                                                   Stretch="UniformToFill"
				                                                   Source="{Binding ToRecipientNameEmployeeCodesWithInitialLetters, Converter={StaticResource EmployeeCodeInitialToImageConverter}, UpdateSourceTrigger=PropertyChanged ,ConverterParameter='image/png'}">
                                                    <Image.Clip>
                                                        <EllipseGeometry Center="28,28" RadiusX="28" RadiusY="28"/>
                                                    </Image.Clip>
                                                </Image>
                                            </Grid>
                                            <StackPanel Orientation="Vertical"  Grid.Column="1">
                                                <TextBlock Text="{Binding RecipientName}" FontSize="14" Style="{DynamicResource TextBlock16Style}" />
                                                <TextBlock Text="{Binding ToRecipientNameJobDescriptionTitles}"  FontSize="12"  Style="{DynamicResource TextBlock16Style}" FontStyle="Italic"/>
                                                <StackPanel Orientation="Horizontal" Height="24" VerticalAlignment="Center">
                                                    <Image Style="{DynamicResource OTMMailIconImageStyle}" Height="20" Width="20" Margin="0,0,5,0" />
                                                    <!--<TextBlock Text="{Binding Row.Inbox, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource HyperlinkTextBlock}" TextDecorations="Underline"  Margin="5,0,0,0"/>-->
                                                    <TextBlock Style="{DynamicResource HyperlinkTextBlock}" Margin="2,0,0,0">
                                                                    <Hyperlink Command="{Binding DataContext.HyperlinkForEmail,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Recipientmail}" TextDecorations="Underline">
                                                                        <Run Text="{Binding Recipientmail, UpdateSourceTrigger=PropertyChanged}" />
                                                                    </Hyperlink>
                                                    </TextBlock>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </dxe:FlyoutControl.ContentTemplate>
                </dxe:FlyoutControl>

                <!--[pramod.misal][04.02.2025][GEOS2-6726]-->
                <!--CCflyoutControl-->
                <dxe:FlyoutControl x:Name="CCflyoutControl" StaysOpen="True" BorderThickness="2" Padding="3"
                                    Content="{Binding RelativeSource={RelativeSource Self}, Path=DataContext}">
                    <dxe:FlyoutControl.ContentTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ShowGridLines="False" Height="64" Width="300">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid Grid.Column="0" Margin="2,2,0,2" Height="60" Width="60">
                                                <Ellipse Stroke="Black" StrokeThickness="2" Fill="Transparent"/>
                                                <Image Height="56" Width="56" HorizontalAlignment="Center" VerticalAlignment="Center"
								           Stretch="UniformToFill"
								           Source="{Binding CCNameEmployeeCodesWithInitialLetters, Converter={StaticResource EmployeeCodeInitialToImageConverter}, UpdateSourceTrigger=PropertyChanged ,ConverterParameter='image/png'}">
                                                    <Image.Clip>
                                                        <EllipseGeometry Center="28,28" RadiusX="28" RadiusY="28"/>
                                                    </Image.Clip>
                                                </Image>
                                            </Grid>
                                            <StackPanel Orientation="Vertical"  Grid.Column="1">
                                                <TextBlock Text="{Binding CCName}" FontSize="14" Style="{DynamicResource TextBlock16Style}" />
                                                <TextBlock Text="{Binding CCNameJobDescriptionTitles}"  FontSize="12"  Style="{DynamicResource TextBlock16Style}" FontStyle="Italic"/>
                                                <StackPanel Orientation="Horizontal" Height="24" VerticalAlignment="Center">
                                                    <Image Style="{DynamicResource OTMMailIconImageStyle}" Height="20" Width="20" Margin="0,0,5,0" />
                                                    <TextBlock Text="{Binding Row.Inbox, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource HyperlinkTextBlock}" TextDecorations="Underline"  Margin="5,0,0,0"/>
                                                    <TextBlock Style="{DynamicResource HyperlinkTextBlock}" Margin="2,0,0,0">
                                                    <Hyperlink Command="{Binding DataContext.HyperlinkForEmail,RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding CCEmail}" TextDecorations="Underline">
                                                        <Run Text="{Binding CCEmail, UpdateSourceTrigger=PropertyChanged}" />
                                                    </Hyperlink>
                                                    </TextBlock>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </dxe:FlyoutControl.ContentTemplate>
                </dxe:FlyoutControl>
            </Grid>
        </DockPanel>
    </Grid>

</UserControl>