<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.APM.Views.ImportActionPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:Validation="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             xmlns:local="clr-namespace:Emdep.Geos.Modules.APM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Title="{Binding WindowHeader,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
             Style="{DynamicResource WinUIDialogWindowStyle}">

    <!--[GEOS2-6021][rdixit][17.02.2025]-->
    <dxwui:WinUIDialogWindow.Resources>
        <dxmvvm:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </dxwui:WinUIDialogWindow.Resources>

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding CancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>


    <dxwui:WinUIDialogWindow.TitleStyle>
        <Style TargetType="ContentControl">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="5"/>
                            </Grid.ColumnDefinitions>

                            <!--<TextBlock Text="{DynamicResource APMActionImportHeader}" Style="{DynamicResource CustomTitleStyle}" FontWeight="Bold"/>-->

                            <Button Content=" " Height="40" Width="40" Grid.Column="2" 
                                    Command="{Binding DataContext.DownloadExcelCommand, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}" 
                                    HorizontalAlignment="Right">
                                <Button.ToolTip>
                                    <TextBlock Text="{DynamicResource APMActionImportDownloadHeader}"/>
                                </Button.ToolTip>
                                <Button.ContentTemplate>
                                    <DataTemplate>
                                        <Image Style="{DynamicResource APMExportButtonImageStyle}" Height="24" Width="24" HorizontalAlignment="Right" Margin="-10" />
                                    </DataTemplate>
                                </Button.ContentTemplate>
                            </Button>

                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </dxwui:WinUIDialogWindow.TitleStyle>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <!-- Left panel -->
            <ColumnDefinition Width="Auto" />
            <!-- Separator -->
            <ColumnDefinition Width="*" />
            <!-- Right panel -->
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

            <Grid Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,-15,-90,0" Text="{DynamicResource APMActionImportHeader}" Style="{DynamicResource CustomTitleStyle}" FontWeight="Bold" VerticalAlignment="Center"  />
                <!--Files-->
                <TextBlock Grid.Row="2" Grid.Column="0" Margin="5,0,0,0" Text="{DynamicResource AddEditActionPlanAttachmentsFilesHeader}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center"  />

                <dxe:ComboBoxEdit Margin="-40,0,0,0" Grid.Row="2" Grid.Column="2" IsEnabled="True"            
                              VerticalContentAlignment="Center" ScrollViewer.VerticalScrollBarVisibility="Hidden" 
                              ItemsSource="{Binding AttachmentFiles}"      
                              SelectedIndex="0"
                              DisplayMember="OriginalFileName" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"            
                              AllowDefaultButton="False" AutoComplete="False" ImmediatePopup="False" IsTextEditable="False"            
                              IsReadOnly="False" Style="{DynamicResource ComboBoxEditStyle}">
                    <dxe:ComboBoxEdit.StyleSettings>
                        <dxe:TokenComboBoxStyleSettings  NewTokenPosition="Far" EnableTokenWrapping="True"  />
                    </dxe:ComboBoxEdit.StyleSettings>
                </dxe:ComboBoxEdit>

                <Button Grid.Row="4" Grid.Column="2" Content="{DynamicResource ChooseFile}" Width="115" HorizontalAlignment="Right" Command="{Binding ChooseFileActionCommand}"/>

            </Grid>

            <Grid Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

                <Button Grid.Row="1"  Margin="-30,20,0,0"  Grid.Column="0" Style="{DynamicResource Button16Style}" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Content="{DynamicResource AddActionPlanAccept}" Command="{Binding AcceptButtonCommand}"
                    CommandParameter="{Binding ElementName=flowLayoutControl}" Height="30" Width="100" />

                <Button Grid.Row="1"  Margin="0,20,0,0" Grid.Column="2" Style="{DynamicResource Button16Style}" HorizontalAlignment="Left" VerticalAlignment="Top"
                    Height="30" Width="100" Content="{DynamicResource AddActionPlanCancel}" Command="{Binding CancelButtonCommand}"/>
            </Grid>
        </Grid>
        <Border Grid.Column="1" Width="1" Background="Gray" Margin="15,-20,15,-20" />
        <!--[pallavi.kale][GEOS2-8993][17.10.2025]-->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{DynamicResource APMActionDownloadHeader}" Style="{DynamicResource CustomTitleStyle}" FontWeight="Bold" Margin="0,-10,0,0"/>

            <Button Grid.Row="2" Content="{DynamicResource APMActionDownload}" Command="{Binding DownloadExcelCommand}" Width="120" Height="30" Margin="-250,4,0,0" />

            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{DynamicResource APMActionLastUpdated}" Style="{StaticResource TextBlock14Style}" Margin="5,10,0,0"/>

                <TextBlock Grid.Column="1" Text="{Binding LastUpdatedDate, StringFormat='{}{0:dd/MM/yyyy}'}" Style="{StaticResource TextBlock14Style}" Margin="5,10,0,0" />
            </Grid>
        </Grid>
    </Grid>
</dxwui:WinUIDialogWindow>

