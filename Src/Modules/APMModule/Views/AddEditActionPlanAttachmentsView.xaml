<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.APM.Views.AddEditActionPlanAttachmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:Validation="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             xmlns:local="clr-namespace:Emdep.Geos.Modules.APM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Title="{Binding WindowHeader,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
             TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
             Style="{DynamicResource WinUIDialogWindowStyle}">


    <dxwui:WinUIDialogWindow.Resources>
        <dxmvvm:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </dxwui:WinUIDialogWindow.Resources>

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding CancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="180"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="5"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            
            <!--Files-->
            <TextBlock Grid.Row="0" Grid.Column="0"  Text="{DynamicResource AddEditActionPlanAttachmentsFilesHeader}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center"  />
            
            <dxe:ComboBoxEdit Grid.Row="0" Grid.Column="2" IsEnabled="True"
            VerticalContentAlignment="Center"
            ItemsSource="{Binding AttachmentObjectList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
            ScrollViewer.VerticalScrollBarVisibility="Hidden"
            DisplayMember="SavedFileName"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            AllowDefaultButton="False"
            AutoComplete="False"
            ImmediatePopup="False"
            ValidateOnEnterKeyPressed="True" 
            ValidateOnTextInput="True"
            IsTextEditable="False"
            IsReadOnly="False"
                              
            Style="{DynamicResource ComboBoxEditStyle}">
                <dxe:ComboBoxEdit.StyleSettings>
                    <dxe:TokenComboBoxStyleSettings  NewTokenPosition="Far" EnableTokenWrapping="True"  />
                </dxe:ComboBoxEdit.StyleSettings>
                <dxe:ComboBoxEdit.SelectedItem>
                    <Binding Path="ActionPlanAttachmentFile"  ValidatesOnDataErrors="True"  UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                        <Binding.ValidationRules>
                            <Validation:AddActionPlanAttachmentValidations FieldName="ActionPlanAttachmentFile" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                        </Binding.ValidationRules>
                    </Binding>
                </dxe:ComboBoxEdit.SelectedItem>
            </dxe:ComboBoxEdit>

            <Button Grid.Row="2" Grid.Column="2" Content="{DynamicResource ChooseFile}" Width="115" HorizontalAlignment="Right" Command="{Binding ChooseFileActionCommand}"/>
            
            <!--Name-->
            <TextBlock Grid.Row="4" Grid.Column="0"  Text="{DynamicResource AddEditActionPlanAttachmentsNameHeader}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center"  />

            <dxe:TextEdit Grid.Row="4" Grid.Column="2" IsReadOnly="True"
                       Text="{Binding FileNameString,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  VerticalContentAlignment="Center" NullText="{DynamicResource AddEditActionPlanAttachmentsNameHeader}">
                <dxe:TextEdit.EditValue>
                    <Binding Path="DataContext.FileNameString" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" ValidatesOnDataErrors="True">
                        <!--<Binding.ValidationRules>
                        <Validation:AddTripAttachmentValidations FieldName="FileNameString" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                        </Binding.ValidationRules>-->
                    </Binding>
                </dxe:TextEdit.EditValue>
            </dxe:TextEdit>
            
            <!--Description-->
            <TextBlock Grid.Row="6" Grid.Column="0"  Text="{DynamicResource AddEditActionPlanAttachmentsDescriptionHeader}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center"  />
            <dxe:TextEdit Grid.Row="6" Grid.Column="2"
              NullText="{DynamicResource AddEditActionPlanAttachmentsDescriptionHeader}" 
              VerticalContentAlignment="Top"
              TextWrapping="Wrap" 
              ScrollViewer.VerticalScrollBarVisibility="Auto" 
              ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                          Text="{Binding Description,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
              AcceptsReturn="True">
              
            </dxe:TextEdit>

        </Grid>

            <Grid Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="1" Grid.Column="0"
                    Style="{DynamicResource Button16Style}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Content="{DynamicResource AddCatalogueAcceptButton}"
                           Command="{Binding AcceptButtonCommand}"
                    CommandParameter="{Binding ElementName=flowLayoutControl}"
                    Height="30"
                    Width="100" 
                    
                    />

            <Button Grid.Row="1" Grid.Column="2"
                    Style="{DynamicResource Button16Style}"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Content="{DynamicResource AddCatalogueCancelButton}"
                    Command="{Binding CancelButtonCommand}"
                    Height="30"
                    Width="100" />
        </Grid>
    </Grid>
</dxwui:WinUIDialogWindow>
