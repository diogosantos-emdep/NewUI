<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.SAM.Views.DownloadTemplateFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.SAM.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:ViewModels="clr-namespace:Emdep.Geos.Modules.SAM.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600"
                         HorizontalAlignment="Center"
                         x:Name="DownloadTemplate"
                         Style="{DynamicResource WinUIDialogWindowStyle}"
                         Title="{DynamicResource DownloadTemplateHeader}"
                         TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}">

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding CancelCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>
    
    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="15"/>
            <RowDefinition Height="55"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1"  Grid.ColumnSpan="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="{DynamicResource SAMDowloadLanguage}" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{DynamicResource Label16Style}"/>
            <dxe:ComboBoxEdit
                           Name="DLanguage"
                           ShowSizeGrip="True"
                           Grid.Column="2"
                           Width="164"
                           Margin="0,12,132,0"
                           IsTextEditable="False" HorizontalAlignment="Right" Height="30" VerticalAlignment="Top" HorizontalContentAlignment="Center"
           ItemsSource="{Binding DataContext.ListLanguage,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxwui:WinUIDialogWindow}}}"
                 SelectedItem="{Binding DataContext.SelectedLanguage,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay,RelativeSource={RelativeSource AncestorType={x:Type dxwui:WinUIDialogWindow}}}"
                            DisplayMember="Name" /> 
                
            
        </Grid>
        
        <!--Buttons-->

        <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="5"
              HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="35"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button  Grid.Column="0" Content="{DynamicResource AcceptButton}"
                     Command="{Binding AcceptCommand}"
                     Height="30"
                     Width="100"
                     Grid.Row="5"
                     HorizontalAlignment="Right"
                     Style="{DynamicResource Button16Style}"
                     VerticalAlignment="Bottom" />

            <Button Grid.Column="2" 
                    Content="{DynamicResource Cancel}"
                    Command="{Binding CancelCommand}" 
                    Height="30"
                    Width="100"
                    Grid.Row="5"
                    HorizontalAlignment="Left"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Bottom" />
        </Grid>
    </Grid>

</dxwui:WinUIDialogWindow>