<UserControl x:Class="Emdep.Geos.Modules.SAM.Views.OrdersReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.SAM.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxwuin="http://schemas.devexpress.com/winfx/2008/xaml/windowsui/navigation" 
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dxnav="http://schemas.devexpress.com/winfx/2008/xaml/navigation"
             xmlns:Converters="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:UIConverter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters" 
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:ViewModels="clr-namespace:Emdep.Geos.Modules.SAM.ViewModels"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxci="http://schemas.devexpress.com/winfx/2008/xaml/core/internal"
             xmlns:SAMCommon ="clr-namespace:Emdep.Geos.Modules.SAM"
             xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
             xmlns:Interactive="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:Behaviors="clr-namespace:Emdep.Geos.UI.Behaviors;assembly=Emdep.Geos.UI.Common"
             xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             Style="{DynamicResource UserControlStyle}" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
            <Label Content="{DynamicResource SAMOrdersReportGridTitle}" Style="{StaticResource LabelTitleStyle}" />
            <!--//Shubham[skadam] [V.2.6.7.0] GEOS2-8849&GEOS2-8855&GEOS2-8859 SAM module very slow when trying to load informations - Orders ->Structures 16 09 2025-->
            <Image gif:ImageBehavior.RepeatBehavior="Forever" gif:ImageBehavior.AnimatedSource="/Emdep.Geos.Modules.SAM;component/Assets/Images/animatedWarning.gif" 
                   Visibility="{Binding IsShowFailedPlantWarning,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,Converter={StaticResource BoolToVis}}" 
                   Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,2,5,0" Height="30" Width="30">
                <Image.ToolTip>
                    <TextBlock Text="{Binding WarningFailedPlants}"/>
                </Image.ToolTip>
            </Image>
            </StackPanel>
            <!-- Warehouse combobox section -->
            <Grid Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Style="{DynamicResource WarehouseImageStyle}"
                       Width="24" Height="24"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" 
                           Text="{DynamicResource SAMPlant:}" FontWeight="Bold"
                           VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Grid>

            <dxe:ComboBoxEdit Grid.Column="4" Name="CbeWareHouse" Margin="0,0,5,0"
                              Style="{DynamicResource ComboBoxEditStyle}" 
                              Height="35"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              IsTextEditable="False"
                              DisplayMember="ShortName"
                              ItemsSource ="{Binding Source={x:Static SAMCommon:SAMCommon.Instance}, Path=PlantOwnerList ,UpdateSourceTrigger=PropertyChanged , Mode=TwoWay}"
                              EditValue="{Binding Source={x:Static SAMCommon:SAMCommon.Instance}, Path=SelectedPlantOwnerList,UpdateSourceTrigger=PropertyChanged}">

                <dxe:ComboBoxEdit.StyleSettings>
                    <dxe:CheckedComboBoxStyleSettings />
                </dxe:ComboBoxEdit.StyleSettings>

                <dxmvvm:Interaction.Triggers>
                    <dxmvvm:EventToCommand EventName="PopupClosed"
                                           Command="{Binding DataContext.PlantOwnerPopupClosed, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                           PassEventArgsToCommand="True"/>
                </dxmvvm:Interaction.Triggers>
            </dxe:ComboBoxEdit>


            <!-- Period -->
            <DockPanel Grid.Column="5" Grid.Row="0" HorizontalAlignment="Right"   VerticalAlignment="Center" 
                                    Margin="15,0,0,0" >
                <TextBlock Text="{Binding FromDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" 
                                   Margin="0,0,5,0"/>
                <TextBlock Text=" - " HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" 
                                   Margin="0,0,5,0"/>
                <TextBlock Text="{Binding ToDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" 
                                   Margin="0,0,5,0"/>
            </DockPanel>

            <!--Calender Icon-->
            <dxwui:AppBar x:Name="Appbar" HideMode="AlwaysVisible" Background="Transparent" Grid.Column="6"
                                  IsSticky="True" HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  IsOpen="True" IsExitButtonEnabled="False" Margin="0,-15,5,0">

                <dxwui:AppBarButton x:Name="appbarbutton" ToolTip="{DynamicResource PeriodSelect}"
                                        GlyphHeight="24" GlyphWidth="24" AllowGlyphTheming="False"
                                        Style="{DynamicResource CalendarImageGlyphStyle}"
                                        GlyphStretch="Fill" IsEllipseEnabled="False" >
                    <dxwui:AppBarButton.Flyout>
                        <dxwui:MenuFlyout ShowIndicator="True" x:Name="MenuLayout">
                            <dxwui:MenuFlyoutItem StaysOpenOnClick="True">
                                <Grid Width="Auto" Height="Auto">
                                    <DockPanel>
                                        <StackPanel Orientation="Vertical" x:Name="PeriodCategories">
                                            <!--This Year-->
                                            <Button x:Name="ThisYear" Content="This Year" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=ThisYear}">
                                            </Button>

                                            <!--Last Year-->
                                            <Button x:Name="LastYear" Content="Last Year" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastYear}">
                                            </Button>


                                            <!--This Month-->
                                            <Button x:Name="ThisMonth" Content="{DynamicResource SAMThisMonth}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=ThisMonth}">
                                            </Button>

                                            <!--Last one month-->
                                            <!--<Button x:Name="LastOneMonth" Content="{DynamicResource SAMLastOneMonth}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastOneMonth}">
                                                    </Button>-->

                                            <!--Last month-->
                                            <Button x:Name="LastMonth" Content="{DynamicResource SAMLastMonth}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastMonth}">
                                            </Button>

                                            <!--This week-->
                                            <Button x:Name="ThisWeek" Content="{DynamicResource SAMThisWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=ThisWeek}">
                                            </Button>

                                            <!--Last one week-->
                                            <!--<Button x:Name="LastOneWeek" Content="{DynamicResource SAMLastOneWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastOneWeek}">
                                                    </Button>-->

                                            <!--Last week-->
                                            <Button x:Name="LastWeek" Content="{DynamicResource SAMLastWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastWeek}">
                                            </Button>

                                            <!--Last 12 month-->
                                            <Button x:Name="Last12Months" Content="Last 12 Months" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=Last12Months}">
                                            </Button>

                                            <!--Custom Range-->
                                            <Button x:Name="CustomRange" Content="{DynamicResource PeriodCustomRange}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCustomRangeCommand}"
                                                            CommandParameter="{Binding ElementName=CustomRange}">
                                            </Button>
                                            <StackPanel Orientation="Horizontal" Margin="0,50,0,0">
                                                <Button Content="{DynamicResource SAMApply}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="60" Background="Yellow" 
                                                            Command="{Binding ApplyCommand}" CommandParameter="{Binding ElementName=MenuLayout}">
                                                </Button>
                                                <Button Content="{DynamicResource SAMCancel}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="60" 
                                                            Command="{Binding CancelCommand}" CommandParameter="{Binding ElementName=MenuLayout}">
                                                </Button>
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Visibility="{Binding DataContext.IsCalendarVisible, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Orientation="Horizontal">
                                            <StackPanel Orientation="Vertical">
                                                <dxe:TextEdit Text="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                </dxe:TextEdit>
                                                <!--Calendar-->
                                                <dxe:DateEditCalendar DateTime="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Vertical">
                                                <dxe:TextEdit Text="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                </dxe:TextEdit>
                                                <!--Calendar-->
                                                <dxe:DateEditCalendar DateTime="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </DockPanel>
                                </Grid>
                            </dxwui:MenuFlyoutItem>
                        </dxwui:MenuFlyout>
                    </dxwui:AppBarButton.Flyout>
                </dxwui:AppBarButton>
            </dxwui:AppBar>



            <!-- Refresh Button -->
            <Button Content=" " Grid.Column="8"
                    Height="40" Width="40" HorizontalAlignment="Right"
                    Command="{Binding RefreshWorkOrderViewCommand}"
                    CommandParameter="{Binding ElementName=tableView}" >
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonRefresh}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{ DynamicResource WareHouseRefreshButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
            
            <!-- Print Button -->
            <Button Content=" " Grid.Column="10" IsEnabled="True"
                    Height="40" Width="40"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Command="{Binding PrintWorkOrderViewCommand}"
                    CommandParameter="{Binding ElementName=tableView}" >
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonPrint}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{ DynamicResource PrinterButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <!-- Export Button -->
            <Button Content=" "  IsEnabled="True"
                    Grid.Column="12"
                    Height="40" Width="40"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Command="{Binding ExportWorkOrderViewCommand}"
                    CommandParameter="{Binding ElementName=tableView}">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonExportToExcel}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{DynamicResource ExcelExportButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

        </Grid>

        <DockPanel Grid.Row="1" >
            <dxwui:NavigationFrame x:Name="Navigatedframe" AnimationType="None">
                <dxmvvm:Interaction.Behaviors>
                    <dxwuin:FrameNavigationService/>
                </dxmvvm:Interaction.Behaviors>
            </dxwui:NavigationFrame>

            <dxg:GridControl x:Name="grid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="Auto"
                             ItemsSource="{Binding MainOtsList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             FocusManager.IsFocusScope="True" 
                             ImplyNullLikeEmptyStringWhenFiltering="True"
                             SelectionMode="Cell"
                             AutoGenerateColumns="None">

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridOffer}" FieldName="Quotation.Offer.Code" FilterPopupMode="CheckedList" MinWidth="100" ReadOnly="True"  AllowResizing="True"/>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridWorkOrder}" FieldName="MergeCode" MinWidth="150" FilterPopupMode="CheckedList" AllowResizing="True">

                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center" Margin="10 0 0 0">
                             <Hyperlink x:Name="hyperlink" 
                                               TargetName="_blank"
                                               Command="{Binding DataContext.WorkOrderViewHyperlinkClickCommand,RelativeSource={RelativeSource AncestorType=dxg:GridControl}}"
                                               CommandParameter="{Binding ElementName=tableView}" Style="{DynamicResource hyperlinkGridStyle}">
                                        <InlineUIContainer>
                                            <TextBlock Text="{Binding Data.MergeCode}"  Style="{DynamicResource HyperlinkTextBlock}" TextDecorations="Underline"/>
                                        </InlineUIContainer>
                                    </Hyperlink>
                        </TextBlock>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>

                </dxg:GridColumn>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridGroup}" FieldName="Quotation.Site.Customer.CustomerName" MinWidth="80" FilterPopupMode="CheckedList"  AllowResizing="True"/>
                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridPlant}" FieldName="Quotation.Site.Name" MinWidth="100" FilterPopupMode="CheckedList"  AllowResizing="True"/>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridCountry}" FieldName="Quotation.Site.Country.Name" MinWidth="100" FilterPopupMode="CheckedList">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Height="24">
                                <Image  
                            Source="{Binding RowData.Row.Quotation.Site.Country.CountryIconBytes,UpdateSourceTrigger=PropertyChanged}"  
                            VerticalAlignment="Center" Height="24" Width="24" Margin="2,0,0,0" />
                                <TextBlock Text ="{Binding RowData.Row.Quotation.Site.Country.Name,UpdateSourceTrigger=PropertyChanged}"  VerticalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridRegion}" FieldName="Quotation.Site.Region" MinWidth="80" FilterPopupMode="CheckedList"  AllowResizing="True"/>
                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridProject}" FieldName="Quotation.Offer.CarProject.Name" MinWidth="80" FilterPopupMode="CheckedList"  AllowResizing="True"/>
                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridOEM}" FieldName="Quotation.Offer.CarOEM.Name" MinWidth="80" FilterPopupMode="CheckedList"  AllowResizing="True"/>
                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridCategory}" FieldName="Quotation.Offer.ProductCategory.Category.Name" MinWidth="100" FilterPopupMode="CheckedList"  AllowResizing="True"/>
                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridModulesCount}" FieldName="Modules" MinWidth="50" FilterPopupMode="CheckedList"  AllowResizing="True"/>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridPODate}" FieldName="PoDate" 
                                MinWidth="100" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridPlannedStartDate}" FieldName="ExpectedStartDate" 
                                MinWidth="100" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridRealStartDate}" FieldName="RealStartDate" 
                                MinWidth="100" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridPlannedEndDate}" FieldName="ExpectedEndDate" 
                                MinWidth="100" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridRealEndDate}" FieldName="RealEndDate" 
                                MinWidth="100" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridEstimatedDuration}" FieldName="TimeEstimationInHours" 
                                MinWidth="100" AllowResizing="True">
                    <!--<dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <dxe:TextEdit
                                EditValue="{Binding Path=CellData.Value,RelativeSource={RelativeSource AncestorType={x:Type dxg:LightweightCellEditor}}}"
                                Name="PART_Editor" HorizontalAlignment="Stretch" HorizontalContentAlignment="Right" VerticalAlignment="Stretch"  ShowBorder="False" 
                                Margin="0" Padding="0" MaskType="Numeric" Mask="n2" MaskUseAsDisplayFormat="True">
                            </dxe:TextEdit>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>-->
                </dxg:GridColumn>

                <!--<dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridRealDuration}" FieldName="RealDuration" 
                                MinWidth="100" AllowResizing="True"/>-->

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridRealDuration}" FieldName="RealDurationDouble" 
                                MinWidth="100" AllowResizing="True"/>

                <!--<dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridRealDuration}" FieldName="RealDurationFloat" 
                                MinWidth="100" AllowResizing="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <dxe:TextEdit
                                EditValue="{Binding Path=CellData.Value,RelativeSource={RelativeSource AncestorType={x:Type dxg:LightweightCellEditor}}}"
                                Name="PART_Editor" HorizontalAlignment="Stretch" HorizontalContentAlignment="Right" VerticalAlignment="Stretch"  ShowBorder="False" 
                                Margin="0" Padding="0" MaskType="Numeric" Mask="n2" MaskUseAsDisplayFormat="True">
                            </dxe:TextEdit>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>-->

                <dxg:GridColumn Name="CostDeviation" Header="{DynamicResource SAMOrdersReportGridCostDeviation}" FieldName="CostDeviation" MinWidth="100" AllowResizing="True"
                            SortFieldName="CostDeviation">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <dxe:TextEdit IsPrintingMode="True"
                                EditValue="{Binding Path=CellData.Value,RelativeSource={RelativeSource AncestorType={x:Type dxg:LightweightCellEditor}}}"
                                Name="PART_Editor" HorizontalAlignment="Stretch" HorizontalContentAlignment="Right" VerticalAlignment="Stretch"  ShowBorder="False" 
                                Margin="0" Padding="0"
                                DisplayFormatString="P0">
                            </dxe:TextEdit>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                    <dxg:GridColumn.PrintCellStyle>
                        <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                            <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                            <Setter Property="DisplayTemplate">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <dxe:TextEdit Name="PART_Editor" Text="{Binding Data.CostDeviationPecentage}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Right"
                                                        IsPrintingMode="True">
                                        </dxe:TextEdit>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </dxg:GridColumn.PrintCellStyle>
                </dxg:GridColumn>

                <dxg:GridColumn Header="{DynamicResource SAMOrdersReportGridShippingDate}" FieldName="FirstShipment.DeliveryDate" 
                                MinWidth="100" FilterPopupMode="CheckedList"
                                EditSettings="{dxe:TextSettings Mask='dd/MM/yyyy' ,
                                MaskUseAsDisplayFormat=True, MaskType=DateTime,
                               HorizontalContentAlignment=Right}" AllowResizing="True"/>
                
                <dxg:GridControl.TotalSummary>
                    <dxg:GridSummaryItem x:Name="summary" FieldName="Offer" SummaryType="Count" DisplayFormat="Total Count : {0}" Visible="True" Alignment="Left" />
                </dxg:GridControl.TotalSummary>
                
                <dxg:GridControl.View>
                    <dxg:TableView  x:Name="tableView"  ShowTotalSummary="True"
                                           AllowGrouping="True"
                                           ShowSearchPanelMode="Always"                                       
                                       NavigationStyle="Cell"
                                       ShowFixedTotalSummary="True"
                                       ShowHorizontalLines="True"
                                       AllowPerPixelScrolling="True"
                                       ShowGroupPanel="False" 
                                       ShowColumnHeaders="True" ShowVerticalLines="True"  AllowHorizontalScrollingVirtualization="True"
                                       ShowIndicator="True" AutoWidth="False" 
                                       HorizontalScrollbarVisibility="Auto" 
                                       VerticalScrollbarVisibility="Auto"
                                       HorizontalContentAlignment="Center"
                                       ShowAutoFilterRow="False"
                                       RowMinHeight="20"
                                       AllowSorting="True" 
                                       AllowScrollHeaders="True"
                                       AllowEditing="False"
                                       AllowColumnMoving="True"
                                       AutoMoveRowFocus="False"
                                       AllowScrollAnimation="False" 
                                       AllowBestFit="True" AllowResizing="True"
                                       AllowConditionalFormattingMenu="True"
                                       SearchPanelHorizontalAlignment="Stretch" AllowConditionalFormattingManager="True">
                        <dxmvvm:Interaction.Behaviors>
                            <dxmvvm:EventToCommand Command="{Binding TableViewLoadedCommand}"
                                                   EventName="Loaded"
                                                   PassEventArgsToCommand="True" />
                        </dxmvvm:Interaction.Behaviors>
                        <dxg:TableView.Resources>
                            <DataTemplate x:Key="PageHeader">
                                <Grid Width="{Binding UsablePageWidth}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="50" />
                                        <ColumnDefinition Width="130" />
                                    </Grid.ColumnDefinitions>
                                    <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E"   HorizontalAlignment="Stretch" 
                                              VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" FontWeight="Bold" FontSize="20" Height="50" />
                                    <dxe:TextEdit Text="Work Planning Report" IsPrintingMode="True"     HorizontalAlignment="Stretch" 
                                              VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" FontWeight="Bold" FontSize="20" />
                                    <Image Source="/Emdep.Geos.Modules.PCM;component/Assets/Images/GeosReportLogo.png" Height="50" Width="80"  dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                                </Grid>
                            </DataTemplate>
                            <DataTemplate x:Key="PageFooter">
                                <dxe:TextEdit HorizontalContentAlignment="Center" 
                                            Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                            dxp:ExportSettings.TargetType="PageNumber" 
                                            dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                            dxp:PageNumberExportSettings.Format="Page {0} of {1}">
                                </dxe:TextEdit>
                            </DataTemplate>
                        </dxg:TableView.Resources>
                    </dxg:TableView>
                </dxg:GridControl.View>

                <dxmvvm:Interaction.Behaviors>
                </dxmvvm:Interaction.Behaviors>

            </dxg:GridControl>

        </DockPanel>
    </Grid>
</UserControl>
