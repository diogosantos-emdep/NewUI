<UserControl  x:Class="Emdep.Geos.Modules.SAM.Views.WorklogReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.SAM.Views"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:Converters="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxwuin="http://schemas.devexpress.com/winfx/2008/xaml/windowsui/navigation"
             xmlns:dxnav="http://schemas.devexpress.com/winfx/2008/xaml/navigation" 
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             xmlns:dxdb="http://schemas.devexpress.com/winfx/2008/xaml/demobase"
             xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
             xmlns:SAMCommon="clr-namespace:Emdep.Geos.Modules.SAM"
             xmlns:ViewModel="clr-namespace:Emdep.Geos.Modules.SAM.ViewModels"
             xmlns:Interactive="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:dxsch="http://schemas.devexpress.com/winfx/2008/xaml/scheduler"
             xmlns:dxschg="http://schemas.devexpress.com/winfx/2008/xaml/scheduling"
             xmlns:Scheduler="clr-namespace:DevExpress.Xpf.Scheduler.UI;assembly=DevExpress.Xpf.Scheduler.v19.2"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:behaviors="clr-namespace:Emdep.Geos.UI.Behaviors;assembly=Emdep.Geos.UI.Common"
             xmlns:dxschcore="clr-namespace:DevExpress.XtraScheduler;assembly=DevExpress.XtraScheduler.v19.2.Core"
             xmlns:dxa="http://schemas.devexpress.com/winfx/2008/xaml/accordion"
             xmlns:UIConverter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:dxschv="http://schemas.devexpress.com/winfx/2008/xaml/scheduling/visual"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxscht="http://schemas.devexpress.com/winfx/2008/xaml/scheduling/themekeys"
             mc:Ignorable="d" 
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             d:DesignHeight="300" d:DesignWidth="300">


        <UserControl.Resources>
        <ResourceDictionary>
            <UIConverter:AppointmentForeColorConverter x:Key="appointmentForeColorConverter" />
            <UIConverter:ObjectToIntConverter x:Key="ObjectToIntConverter" />

            <DataTemplate x:Key="AppointmentToolTipContentTemplate">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="2" Text="{Binding Description}" />
                </Grid>
            </DataTemplate>

        <DataTemplate x:Key="AppointmentContentTemplate">
            <StackPanel Orientation="Horizontal">
                    <TextBlock TextAlignment="Center" Text="{Binding Description}" Foreground="{Binding Label, Converter={StaticResource appointmentForeColorConverter}}" />
                </StackPanel>
        </DataTemplate>
     
            <!--Style for Toggle Button-->
        <Style x:Key="HideButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border BorderThickness="0" Background="{TemplateBinding Background}">
                            <Grid>
                                <Image Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Source="/Emdep.Geos.Modules.Warehouse;component/Assets/Images/ArrowRight.png"
                                   Visibility="{TemplateBinding IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter Inverse=True}}" />
                                <Image Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Source="/Emdep.Geos.Modules.Warehouse;component/Assets/Images/ArrowLeft.png"
                                   Visibility="{TemplateBinding IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter}}" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


            <DataTemplate x:Key="resourceHeaderContentTemplate">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Image
                    MaxWidth="120"
                    MaxHeight="120"
                    HorizontalAlignment="Center"
                    DockPanel.Dock="Top"
                    RenderOptions.BitmapScalingMode="NearestNeighbor"
                    Source="{Binding Resource.CustomFields.Photo}"
                    Stretch="Uniform" />
                    <StackPanel Grid.Row="1">
                        <TextBlock
                        HorizontalAlignment="Center"
                        FontWeight="Bold"
                        Text="{Binding Resource.Caption}" />
                        
                    </StackPanel>
                    <StackPanel Grid.Row="2">
                        <TextBlock
                        HorizontalAlignment="Center"
                        Text="{Binding Resource.CustomFields.TotalHour}" />

                    </StackPanel>
                </Grid>
            </DataTemplate>
            <Style TargetType="dxschv:ResourceHeaderControl">
                <Setter Property="ContentOrientation" Value="Horizontal" />
                <Setter Property="Background" Value="LightGray"/>
                <Setter Property="Foreground" Value="Black"/>
            </Style>


            <DataTemplate x:Key="appointmentFlyoutContentTemplate">
                <dxlc:LayoutControl Width="auto" Height="auto" IsHitTestVisible="False" Orientation="Vertical" Padding="11">
                    <dxlc:LayoutControl Margin="0,0,0,0" VerticalAlignment="Top" Orientation="Vertical" Padding="0">
                        <dxlc:LayoutItem Label="OT : ">
                            <dxlc:LayoutItem.LabelStyle>
                                <Style TargetType="dxlc:LayoutItemLabel">
                                    <Setter Property="FontWeight" Value="Bold" />
                                </Style>
                            </dxlc:LayoutItem.LabelStyle>
                            <TextBlock Text="{Binding Appointment.CustomFields.OtCode}" />
                            <dxlc:LayoutItem.Style>
                                <Style TargetType="{x:Type dxlc:LayoutItem}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Appointment.CustomFields.IdOT}" Value="-1">
                                            <Setter Property="Visibility" Value="Collapsed"></Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Appointment.CustomFields.IdOT}" Value="-2">
                                            <Setter Property="Visibility" Value="Collapsed"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </dxlc:LayoutItem.Style>
                        </dxlc:LayoutItem>
                        <dxlc:LayoutItem Label="Hours : ">
                            <dxlc:LayoutItem.LabelStyle>
                                <Style TargetType="dxlc:LayoutItemLabel">
                                    <Setter Property="FontWeight" Value="Bold" />
                                </Style>
                            </dxlc:LayoutItem.LabelStyle>
                            <TextBlock Text="{Binding Appointment.CustomFields.Hours}" />
                        </dxlc:LayoutItem> 
                        <dxlc:LayoutItem Label="Site : ">
                            <dxlc:LayoutItem.LabelStyle>
                                <Style TargetType="dxlc:LayoutItemLabel">
                                    <Setter Property="FontWeight" Value="Bold" />
                                </Style>
                            </dxlc:LayoutItem.LabelStyle>
                            <TextBlock Text="{Binding Appointment.CustomFields.CustomerName}" />

                            <dxlc:LayoutItem.Style>
                                <Style TargetType="{x:Type dxlc:LayoutItem}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Appointment.CustomFields.IdOT}" Value="-1">
                                            <Setter Property="Visibility" Value="Collapsed"></Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Appointment.CustomFields.IdOT}" Value="-2">
                                            <Setter Property="Visibility" Value="Collapsed"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </dxlc:LayoutItem.Style>
                        </dxlc:LayoutItem>
                        
                    </dxlc:LayoutControl>
                </dxlc:LayoutControl>
            </DataTemplate>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        </ResourceDictionary>

    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
            <Label Content="{DynamicResource SAMWorklogReport}" HorizontalAlignment="Left" Style="{StaticResource LabelTitleStyle}" Grid.Row="0" Grid.Column="0" />
            <!--//Shubham[skadam] [V.2.6.8.0] GEOS2-8851 SAM module very slow when trying to load informations - Reports-> Worklog Report (4/6) 15 10 2025-->
            <Image gif:ImageBehavior.RepeatBehavior="Forever" gif:ImageBehavior.AnimatedSource="/Emdep.Geos.Modules.SAM;component/Assets/Images/animatedWarning.gif" 
                   Visibility="{Binding IsShowFailedPlantWarning,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,Converter={StaticResource BoolToVis}}" 
                   Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,2,5,0" Height="30" Width="30">
                <Image.ToolTip>
                    <TextBlock Text="{Binding WarningFailedPlants}"/>
                </Image.ToolTip>
            </Image>
        </StackPanel>
        <DockPanel Grid.Column="2" Grid.Row="0" HorizontalAlignment="Stretch">
            <Grid HorizontalAlignment="Right" Grid.Row="0" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="AUTO"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="AUTO"/>
                    <ColumnDefinition Width="AUTO" />
                    <ColumnDefinition Width="AUTO" />
                    <ColumnDefinition Width="AUTO" />
                    <ColumnDefinition Width="AUTO" />
                    <ColumnDefinition Width="AUTO" />
                    <ColumnDefinition Width="AUTO" />
                </Grid.ColumnDefinitions>

                <Grid HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="1">
                    <Grid.ColumnDefinitions >
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!--Plant-->
                    <Grid Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Plant Image -->
                        <Image Grid.Column="0" Style="{DynamicResource WarehouseImageStyle}"
                       Width="24" Height="24" Margin="0,-10,5,0"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="2" Margin="0,-10,5,0"
                           Text="{DynamicResource CommonPlant}" FontWeight="Bold"
                           VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Grid>
                    <!--<TextBlock Grid.Column="0" Text="{DynamicResource CommonPlant}"  FontWeight="Bold"  Grid.Row="0" HorizontalAlignment="Right" Height="25" IsEnabled="True"  Margin="0,-10,5,0"/>-->

                    <dxe:ComboBoxEdit Style="{DynamicResource ComboBoxEditStyle}" Grid.Column="1"  Width="150" Height="30"                        
                                      DisplayMember="ShortName" Margin="0,-10,5,0"
                                      ItemsSource ="{Binding Source={x:Static SAMCommon:SAMCommon.Instance}, Path=PlantOwnerList ,UpdateSourceTrigger=PropertyChanged , Mode=TwoWay}"
                                    EditValue="{Binding Source={x:Static SAMCommon:SAMCommon.Instance}, Path=SelectedPlantOwnerList,UpdateSourceTrigger=PropertyChanged}">
                        <dxe:ComboBoxEdit.StyleSettings>
                            <dxe:CheckedComboBoxStyleSettings />
                        </dxe:ComboBoxEdit.StyleSettings>
                        <dxmvvm:Interaction.Triggers>
                            <dxmvvm:EventToCommand EventName="PopupClosed"
                                               Command="{Binding DataContext.PlantOwnerPopupClosedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                               PassEventArgsToCommand="True"/>
                        </dxmvvm:Interaction.Triggers>
                    </dxe:ComboBoxEdit>

                    <!-- Period -->
                    <DockPanel Grid.Column="2" Grid.Row="0" HorizontalAlignment="Right"   VerticalAlignment="Center" Margin="15,0,0,0" >
                        <TextBlock Text="{Binding FromDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,-10,5,0"/>
                        <TextBlock Text=" - " HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Margin="0,-10,5,0"/>
                        <TextBlock Text="{Binding ToDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,-10,5,0"/>
                    </DockPanel>
                    <!--Calender Icon-->
                    <dxwui:AppBar x:Name="Appbar" HideMode="AlwaysVisible" Background="Transparent" Grid.Column="3"
                                  IsSticky="True" HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  IsOpen="True" IsExitButtonEnabled="False" Margin="0,-10,5,0">

                        <dxwui:AppBarButton x:Name="appbarbutton" ToolTip="{DynamicResource PeriodSelect}"
                                        GlyphHeight="24" GlyphWidth="24"
                                        Style="{DynamicResource SAMCalendarImageGlyphStyle}"
                                        GlyphStretch="Fill" IsEllipseEnabled="False" >
                            <dxwui:AppBarButton.Flyout>
                                <dxwui:MenuFlyout ShowIndicator="True" x:Name="MenuLayout">
                                    <dxwui:MenuFlyoutItem StaysOpenOnClick="True">
                                        <Grid Width="Auto" Height="Auto">
                                            <DockPanel>
                                                <StackPanel Orientation="Vertical" x:Name="PeriodCategories">
                                                    <!--This Month-->
                                                    <Button x:Name="ThisMonth" Content="{DynamicResource SAMThisMonth}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=ThisMonth}">
                                                    </Button>

                                                    <!--Last one month-->
                                                    <Button x:Name="LastOneMonth" Content="{DynamicResource SAMLastOneMonth}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastOneMonth}">
                                                    </Button>

                                                    <!--Last month-->
                                                    <Button x:Name="LastMonth" Content="{DynamicResource SAMLastMonth}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastMonth}">
                                                    </Button>

                                                    <!--This week-->
                                                    <Button x:Name="ThisWeek" Content="{DynamicResource SAMThisWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=ThisWeek}">
                                                    </Button>

                                                    <!--Last one week-->
                                                    <Button x:Name="LastOneWeek" Content="{DynamicResource SAMLastOneWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastOneWeek}">
                                                    </Button>

                                                    <!--Last week-->
                                                    <Button x:Name="LastWeek" Content="{DynamicResource SAMLastWeek}" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCommand}"
                                                            CommandParameter="{Binding ElementName=LastWeek}">
                                                    </Button>
                                                    <!--Custom Range-->
                                                    <Button x:Name="CustomRange" Content="{DynamicResource PeriodCustomRange}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="110"
                                                            Command="{Binding PeriodCustomRangeCommand}"
                                                            CommandParameter="{Binding ElementName=CustomRange}">
                                                    </Button>
                                                    <StackPanel Orientation="Horizontal" Margin="0,50,0,0">
                                                        <Button Content="{DynamicResource SAMApply}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="60" Background="Yellow" 
                                                            Command="{Binding ApplyCommand}" CommandParameter="{Binding ElementName=MenuLayout}">
                                                        </Button>
                                                        <Button Content="{DynamicResource SAMCancel}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                            Height="30" Width="60" 
                                                            Command="{Binding CancelCommand}" CommandParameter="{Binding ElementName=MenuLayout}">
                                                        </Button>
                                                    </StackPanel>
                                                </StackPanel>

                                                <StackPanel Visibility="{Binding DataContext.IsCalendarVisible, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Orientation="Horizontal">
                                                    <StackPanel Orientation="Vertical">
                                                        <dxe:TextEdit Text="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                        </dxe:TextEdit>
                                                        <!--Calendar-->
                                                        <dxe:DateEditCalendar DateTime="{Binding DataContext.StartDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Vertical">
                                                        <dxe:TextEdit Text="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                  Height="30" TextWrapping="Wrap" Mask="dd/MM/yyyy"  
                                                  MaskType="DateTimeAdvancingCaret"  
                                                  MaskUseAsDisplayFormat="True"
                                                  HorizontalAlignment="Stretch" IsReadOnly="True"  Margin="2,2,2,2">
                                                        </dxe:TextEdit>
                                                        <!--Calendar-->
                                                        <dxe:DateEditCalendar DateTime="{Binding DataContext.EndDate, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                      VerticalAlignment="Center"
                                                                      ShowToday="False"
                                                                      ShowClearButton="False"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </DockPanel>
                                        </Grid>
                                    </dxwui:MenuFlyoutItem>
                                </dxwui:MenuFlyout>
                            </dxwui:AppBarButton.Flyout>
                        </dxwui:AppBarButton>
                    </dxwui:AppBar>

                </Grid>

                <!--Refresh Button-->
                <Button Visibility="Visible" Grid.Row="0" Grid.Column="3" Height="40" Width="40" Margin="0,-15,5,0" HorizontalAlignment="Right" 
                                Command="{Binding RefreshButtonCommand}"
                                CommandParameter="{Binding ElementName=ItemListTableView}" >
                    <Button.ToolTip>
                        <TextBlock Text="{DynamicResource ButtonRefresh}"/>
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Image Style="{DynamicResource RefreshButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <!--Print Button-->
                <Button Visibility="Visible" Grid.Row="0" Grid.Column="4" Height="40" Width="40" Margin="0,-15,5,0" HorizontalAlignment="Right" 
                    Command="{Binding PrintButtonCommand}"
                    CommandParameter="{Binding ElementName=ItemListTableView}">
                    <Button.ToolTip>
                        <TextBlock Text="{DynamicResource ButtonPrint}"/>
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Image Style="{DynamicResource PrinterButtonImageStyle}" Height="24" Width="24" Margin="-10"/>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <!--Export Button-->
                <Button Visibility="Visible" Grid.Row="0" Grid.Column="5" Height="40" Width="40" Margin="0,-15,5,0" HorizontalAlignment="Right" 
                    Command="{Binding ExportButtonCommand}"
                    CommandParameter="{Binding ElementName=ItemListTableView}" >
                    <Button.ToolTip>
                        <TextBlock Text="{DynamicResource ButtonExportToExcel}"/>
                    </Button.ToolTip>
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Image Style="{DynamicResource ExcelExportButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <!-- Switch View -->
                <dx:DropDownButton x:Name="dropdownButton" Content="Dropdown button" 
                                   HorizontalAlignment="Right" Height="40" Width="40" Grid.Row="0" Grid.Column="9" Margin="5,-15,5,0" VerticalAlignment="Center">
                    <dx:DropDownButton.Style>
                        <Style>
                            <Setter Property="dx:DropDownButtonBase.ArrowAlignment" Value="Right" />
                            <Setter Property="dx:DropDownButtonBase.ArrowPadding" Value="0" />
                        </Style>
                    </dx:DropDownButton.Style>

                    <Button.ToolTip>
                        <TextBlock Text="Switch View"></TextBlock>
                    </Button.ToolTip>

                    <Button.ContentTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Style="{ DynamicResource SwitchOrganizationImageStyle}" Grid.Column="0" Height="24" Width="24" Margin="-20" />
                            </Grid>
                        </DataTemplate>
                    </Button.ContentTemplate>


                    <dxb:PopupMenu>
                        <dxb:BarButtonItem Name="SchedulerView" Content="Scheduler View" Glyph="{DynamicResource SwitchActivityCalendar}" Command="{Binding ShowSchedulerViewCommand}">
                            <dxb:BarButtonItem.Style>
                                <Style>
                                    <Style.Triggers>
                                        <Trigger Property="dxb:BarButtonItem.IsMouseOver" Value="True">
                                            <Setter Property="dxb:BarButtonItem.Glyph" Value="{DynamicResource SwitchActivityCalendarMouseOver}" />
                                        </Trigger>
                                        <Trigger Property="dxb:BarButtonItem.IsMouseOver" Value="False">
                                            <Setter Property="dxb:BarButtonItem.Glyph" Value="{DynamicResource SwitchActivityCalendar}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </dxb:BarButtonItem.Style>
                        </dxb:BarButtonItem>

                        <dxb:BarButtonItem Name="GridView" Content="Grid View" Glyph="{DynamicResource SwitchActivityGridview}" Command="{Binding ShowGridViewCommand}" CommandParameter="{Binding ElementName=scheduler}">
                            <dxb:BarButtonItem.Style>
                                <Style>
                                    <Style.Triggers>
                                        <Trigger Property="dxb:BarButtonItem.IsMouseOver" Value="True">
                                            <Setter Property="dxb:BarButtonItem.Glyph" Value="{DynamicResource SwitchActivityGridviewMouseOver}" />
                                        </Trigger>
                                        <Trigger Property="dxb:BarButtonItem.IsMouseOver" Value="False">
                                            <Setter Property="dxb:BarButtonItem.Glyph" Value="{DynamicResource SwitchActivityGridview}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </dxb:BarButtonItem.Style>
                        </dxb:BarButtonItem>
                    </dxb:PopupMenu>
                </dx:DropDownButton>

            </Grid>
        </DockPanel>
        <!--scheduler-->
        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Visibility="{Binding IsSchedulerViewVisible,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <dxschg:SchedulerControl GroupType="Resource" ActiveViewType="TimelineView" LimitInterval="{Binding IntervalRange}" 
                                     FlyoutContentTemplate="{StaticResource appointmentFlyoutContentTemplate}"  AllowInplaceEditor="False" 
                                    x:Name="SchedulerControlTimelineView" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" Height="auto" Width="auto">

                <dxschg:DayView
                x:Name="dayView"
                NavigationButtonsVisibility="Auto"
                ResourcesPerPage="4"
                ShowAllDayArea="False"
                ShowWorkTimeOnly="False"
                SnapToCellsMode="Never"
                ResourceHeaderContentTemplate="{StaticResource resourceHeaderContentTemplate}"/>
                <dxschg:TimelineView
                x:Name="timelineView"
                NavigationButtonsVisibility="Auto"
                ResourcesPerPage="4"
                ResourceHeaderContentTemplate="{StaticResource resourceHeaderContentTemplate}"
                    ToolTip="{StaticResource AppointmentToolTipContentTemplate}"
                    StretchAppointments="True" 
                >
                    <dxschg:TimelineView.TimeScales>
                        <dxschg:TimeScaleWeek/>
                        <dxschg:TimeScaleDay />
                    </dxschg:TimelineView.TimeScales>

                    
                </dxschg:TimelineView>
                <dxschg:SchedulerControl.DataSource>
                    <dxschg:DataSource AppointmentsSource="{Binding Worklogs}" ResourcesSource="{Binding Users}">
                        <dxschg:DataSource.AppointmentMappings>
                            <dxschg:AppointmentMappings
                                    Id="IdUser"
                                    ResourceId="IdUser"
                                    Start="StartTime"
                                    End="EndTime"
                                    Description="Description"
                                    Subject="Description">
                                <dxschg:CustomFieldMapping Mapping="CustomerName" Name="CustomerName" />
                                <dxschg:CustomFieldMapping Mapping="IdOT" Name="IdOT" />
                                <dxschg:CustomFieldMapping Mapping="Hours" Name="Hours" />
                                <dxschg:CustomFieldMapping Mapping="OtCode" Name="OtCode" />

                            </dxschg:AppointmentMappings>
                        </dxschg:DataSource.AppointmentMappings>
                        <dxschg:DataSource.ResourceMappings>
                            <dxschg:ResourceMappings Caption="FullName" Id="IdUser">
                                <dxschg:CustomFieldMapping Mapping="Image" Name="Photo">
                                </dxschg:CustomFieldMapping>
                                <dxschg:CustomFieldMapping Mapping="Hours" Name="TotalHour">
                                </dxschg:CustomFieldMapping>
                            </dxschg:ResourceMappings>
                        </dxschg:DataSource.ResourceMappings>
                    </dxschg:DataSource>
                </dxschg:SchedulerControl.DataSource>
                <dxmvvm:Interaction.Triggers>
                    <dxmvvm:EventToCommand EventName="PopupMenuShowing"
                                            Command="{Binding PopupMenuShowingCommand}"
                                            PassEventArgsToCommand="True" />
                    <dxmvvm:EventToCommand EventName="AppointmentWindowShowing"
                                            Command="{Binding DisableAppointmentCommand}" 
                                            PassEventArgsToCommand="True" />
                </dxmvvm:Interaction.Triggers>
            </dxschg:SchedulerControl>
            
        </Grid>

        <!--GridView-->
        <Grid x:Name="WorklogReportGrid" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
              VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
              Visibility="{Binding IsGridViewVisible,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">

            <dxg:GridControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                             AllowLiveDataShaping="True" 
                             ItemsSource="{Binding WorkLogReportGridList,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" SelectionMode="Row"
                             SelectedItem="{Binding SelectedWorkLogReport,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"  
                             ImplyNullLikeEmptyStringWhenFiltering="True">

                <dxg:GridColumn Header="{DynamicResource SAMUser}" FieldName="WorklogUser.FullName" FilterPopupMode="CheckedList" Width="*" FixedWidth="True" ReadOnly="True" />
                <dxg:GridColumn Header="{DynamicResource SAMOT}" FieldName="OtCode" FilterPopupMode="CheckedList" Width="200" FixedWidth="True" ReadOnly="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Margin="5 0 0 0">
                             <Hyperlink x:Name="hyperlink" 
                                        TargetName="_blank"
                                        Command="{Binding DataContext.OTCodeHyperlinkClickCommand,RelativeSource={RelativeSource AncestorType=dxg:GridControl}}"
                                        CommandParameter="{Binding ElementName=ItemListTableView}" 
                                        Style="{DynamicResource hyperlinkGridStyle}">
                                        <InlineUIContainer>
                                            <TextBlock Text="{Binding Data.OtCode}" Style="{DynamicResource HyperlinkTextBlock}" TextDecorations="Underline" />
                                        </InlineUIContainer>
                                    </Hyperlink>
                        </TextBlock>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <dxg:GridColumn Header="{DynamicResource SAMReportStartDate}" FieldName="StartTime" FilterPopupMode="CheckedList" Width="150" FixedWidth="True" ReadOnly="True" />
                <dxg:GridColumn Header="{DynamicResource SAMStartTime}" FieldName="STime.TimeOfDay" FilterPopupMode="CheckedList" Width="150" FixedWidth="True" ReadOnly="True">
                    <dxg:GridColumn.EditSettings>
                        <dxe:DateEditSettings Mask="t" MaskType="DateTime" MaskUseAsDisplayFormat="True"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>
                <dxg:GridColumn Header="{DynamicResource SAMReportEndDate}" FieldName="EndTime" FilterPopupMode="CheckedList" Width="150" FixedWidth="True" ReadOnly="True" />
                <dxg:GridColumn Header="{DynamicResource SAMEndTime}" FieldName="ETime.TimeOfDay" FilterPopupMode="CheckedList" Width="150" FixedWidth="True" ReadOnly="True" >
                    <dxg:GridColumn.EditSettings>
                        <dxe:DateEditSettings Mask="t" MaskType="DateTime" MaskUseAsDisplayFormat="True"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>
                <dxg:GridColumn Header="{DynamicResource SAMTotalTime}" FieldName="Hours" FilterPopupMode="CheckedList" Width="150" FixedWidth="True" ReadOnly="True" >
                    <!--<dxg:GridColumn.EditSettings>
                        <dxe:DateEditSettings Mask="H\H m\M" MaskType="DateTime" MaskUseAsDisplayFormat="True"/>
                    </dxg:GridColumn.EditSettings>-->
                </dxg:GridColumn>

                <dxmvvm:Interaction.Behaviors>
                    <UIHelper:GridSelectingBehavior />
                </dxmvvm:Interaction.Behaviors>

                <dxg:GridControl.TotalSummary>
                    <dxg:GridSummaryItem x:Name="summary" FieldName="" SummaryType="Count" DisplayFormat="Total Count : {0}" Visible="True" Alignment="Left"/>
                </dxg:GridControl.TotalSummary>

                <dxg:GridControl.View>
                    <dxg:TableView x:Name="ItemListTableView"
                                   ShowGroupedColumns="True"  
                                   HorizontalScrollbarVisibility="Hidden"  
                                   VerticalScrollbarVisibility="Visible" 
                                   ShowIndicator="False"  
                                   ShowColumnHeaders="True"  
                                   ShowGroupPanel="False"  
                                   AllowEditing="True"  
                                   ShowHorizontalLines="True"  
                                   ShowVerticalLines="True"  
                                   ShowSearchPanelMode="Always" 
                                   SearchPanelHorizontalAlignment="Stretch" 
                                   EnableImmediatePosting="True"
                                   ImmediateUpdateRowPosition="True"
                                   AllowColumnMoving="True"
                                   NavigationStyle="Row"
                                   ShowFixedTotalSummary="True"
                                   ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                   FocusedRow="{Binding SelectedDetection, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                   ShowFilterPopup="{DXEvent Handler='@args.PopupBaseEdit.PopupWidth=200'}" >

                        <dxg:TableView.Resources>
                            <DataTemplate x:Key="WorkLogReportPrintHeaderTemplate">
                                <Grid Width="{Binding UsablePageWidth}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="50" />
                                        <ColumnDefinition Width="130" />
                                    </Grid.ColumnDefinitions>
                                    <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" Height="50" />
                                    <dxe:TextEdit Text="Work Log Report" IsPrintingMode="True" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" />
                                    <Image Source="/Emdep.Geos.Modules.SAM;component/Assets/Images/GeosReportLogo.png" Height="50" Width="80"  dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                                </Grid>
                            </DataTemplate>

                            <DataTemplate x:Key="WorkLogReportPrintFooterTemplate">
                                <dxe:TextEdit HorizontalContentAlignment="Center" 
                                                  Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                                  dxp:ExportSettings.TargetType="PageNumber" 
                                                  dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                                  dxp:PageNumberExportSettings.Format="Page {0} of {1}"/>
                            </DataTemplate>

                        </dxg:TableView.Resources>

                        <dxmvvm:Interaction.Behaviors>
                            <dxmvvm:EventToCommand Command="{Binding WorkLogReportDoubleClickCommand}" 
                                                   EventName="MouseDoubleClick"
                                                   CommandParameter="{Binding ElementName=ItemListTableView}">
                                <dxmvvm:EventToCommand.EventArgsConverter>
                                    <dx:EventArgsToDataRowConverter />
                                </dxmvvm:EventToCommand.EventArgsConverter>
                            </dxmvvm:EventToCommand>

                        </dxmvvm:Interaction.Behaviors>

                    </dxg:TableView>
                </dxg:GridControl.View>


            </dxg:GridControl>
        </Grid>

    </Grid>
</UserControl>
