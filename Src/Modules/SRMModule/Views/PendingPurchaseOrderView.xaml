<UserControl x:Class="Emdep.Geos.Modules.SRM.Views.PendingPurchaseOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.SRM.Views"
             xmlns:Converters="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxwuin="http://schemas.devexpress.com/winfx/2008/xaml/windowsui/navigation" 
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dxnav="http://schemas.devexpress.com/winfx/2008/xaml/navigation"
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:ViewModels="clr-namespace:Emdep.Geos.Modules.SRM.ViewModels"
             xmlns:SRMCommon ="clr-namespace:Emdep.Geos.Modules.SRM"
             xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
             
             Style="{DynamicResource UserControlStyle}"
             mc:Ignorable="d" 
             
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <Converters:ImageUrlToByteArrayConverter x:Key="ImageUrlToByteArrayConverter" /> <!--[rdixit][28.06.2023]-->
        <Converters:ImageConverter x:Key="ImageConverter"/>
        <Converters:ForegroundColorConverter x:Key="ForegroundColorConverter"/>

        <Style x:Key="HideButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border BorderThickness="0" Background="{TemplateBinding Background}">
                            <Grid>
                                <Image Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Source="/Emdep.Geos.Modules.Warehouse;component/Assets/Images/ArrowRight.png"
                                   Visibility="{TemplateBinding IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter Inverse=True}}"/>
                                <Image Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Center"
                                   Source="/Emdep.Geos.Modules.Warehouse;component/Assets/Images/ArrowLeft.png"
                                   Visibility="{TemplateBinding IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter}}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="printCellStatusStyle" TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
            <Style.Setters>
                <Setter Property="dxp:ExportSettings.TargetType" Value="Panel"/>
                <Setter Property="DisplayTemplate">
                    <Setter.Value>
                        <ControlTemplate>
                            <dxe:TextEdit Name="PART_Editor" Text="{Binding RowData.Row.LstWorkflowStatus}" HorizontalAlignment="Stretch" 
                                          IsPrintingMode="True">
                            </dxe:TextEdit>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style.Setters>
        </Style>
        
        <Style x:Key="LeftPanelTileItemStyle" TargetType="{x:Type dxnav:TileBarItem}">
            <!--<Setter Property="Foreground" Value="#FF787878"/>-->
            <Setter Property="Foreground" Value="{Binding ForeColor,Converter={Converters:ForegroundColorConverter}}"/>
            <Setter Property="BorderBrush" Value="#FFD6D6D6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{Binding BackColor}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Height" Value="{Binding Height}"/>
            <Setter Property="Width" Value="{Binding Width}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="SelectionPadding" Value="0" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Width="195" Height="75"  >
                            <Label Content="{Binding EntitiesCount,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Right"   Margin="0,-5,8,0" VerticalContentAlignment="Top" FontSize="36" Visibility="{Binding EntitiesCountVisibility}"></Label>
                            <Label Content="{Binding Caption, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Left" Margin="6,0,0,3" VerticalAlignment="Bottom" FontSize="15" ></Label>
                        </StackPanel>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderThickness" Value="5"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource TileBarItemColorStyle}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"  />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Content="{DynamicResource PurchaseOrdersOnGoing}" Style="{StaticResource LabelTitleStyle}" Grid.Row="0" />

            <DockPanel Grid.Row="0" Grid.Column="1">

                <Grid HorizontalAlignment="Right" Width="Auto" Height="45" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="45"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Waresouse combobox section -->
                    <!--<DockPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" >
                        <Image Style="{DynamicResource WarehouseImageStyle}" Width="24" Height="24" Margin="7,0,3,0" />
                        <TextBlock Text="{DynamicResource Warehouse}"  FontWeight="Bold" Margin="5,2,5,0" VerticalAlignment="Bottom" />
                    </DockPanel>
                    <dxe:ComboBoxEdit Name="CbeWareHouse" Style="{DynamicResource ComboBoxEditStyle}" Grid.Column="1" Width="150" Height="30"
                                       Margin="5,0,5,5" VerticalAlignment="Bottom" IsTextEditable="False"
                                      ItemsSource ="{Binding Source={x:Static SRMCommon:SRMCommon.Instance}, Path=WarehouseList ,UpdateSourceTrigger=PropertyChanged , Mode=TwoWay}"
                                    SelectedItem="{Binding Source={x:Static SRMCommon:SRMCommon.Instance}, Path=Selectedwarehouse,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                                      DisplayMember="Name" ShowError="False" ShowErrorToolTip="False"  >

                        <dxmvvm:Interaction.Triggers>
                            <dxmvvm:EventToCommand EventName="EditValueChanged"
                                               Command="{Binding CommandWarehouseEditValueChanged}" CommandParameter="{Binding ElementName=CbeWareHouse,Path=SelectedItem}"
                                              />
                        </dxmvvm:Interaction.Triggers>
                    </dxe:ComboBoxEdit>-->
                    <!--[Sudhir.Jangra][GEOS2-4401][02/05/2023]-->
                    <DockPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" >
                        <Image Style="{DynamicResource WarehouseImageStyle}" Width="24" Height="24" Margin="7,0,3,0" />
                        <TextBlock Text="{DynamicResource Warehouse}"  FontWeight="Bold" Margin="5,2,5,0" VerticalAlignment="Bottom" />
                    </DockPanel>
                    <dxe:ComboBoxEdit Name="CbeWareHouse" Style="{DynamicResource ComboBoxEditStyle}" Grid.Column="1" Width="150" Height="30"
                                       Margin="5,0,5,5" VerticalAlignment="Bottom" IsTextEditable="False"
                                      ItemsSource ="{Binding Source={x:Static SRMCommon:SRMCommon.Instance}, Path=WarehouseList ,UpdateSourceTrigger=PropertyChanged , Mode=TwoWay}"
                                    EditValue="{Binding Source={x:Static SRMCommon:SRMCommon.Instance}, Path=SelectedAuthorizedWarehouseList,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                                      DisplayMember="Name" ShowError="False" ShowErrorToolTip="False"  >
                        <dxe:ComboBoxEdit.StyleSettings>
                            <dxe:CheckedComboBoxStyleSettings/>
                        </dxe:ComboBoxEdit.StyleSettings>

                        <dxmvvm:Interaction.Triggers>
                            <dxmvvm:EventToCommand EventName="EditValueChanged"
                                               Command="{Binding CommandWarehouseEditValueChanged}" 
                                                   PassEventArgsToCommand="True"
                                              />
                        </dxmvvm:Interaction.Triggers>
                    </dxe:ComboBoxEdit>

                    <!--<dxe:ComboBoxEdit Name="CbeWareHouse" Style="{DynamicResource ComboBoxEditStyle}" Grid.Column="1" Width="150" Height="30"
                                      Margin="5,0,5,5" VerticalAlignment="Bottom" IsTextEditable="False"
                                      DisplayMember="Name" ShowError="False" ShowErrorToolTip="False"
                        ItemsSource ="{Binding Source={x:Static WarehouseCommon:WarehouseCommon.Instance}, Path=WarehouseList,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                      SelectedItem="{Binding Source={x:Static WarehouseCommon:WarehouseCommon.Instance}, Path=Selectedwarehouse,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">

                        <dxmvvm:Interaction.Triggers>
                            <dxmvvm:EventToCommand EventName="EditValueChanged"
                                               Command="{Binding CommandWarehouseEditValueChanged}" CommandParameter="{Binding ElementName=CbeWareHouse,Path=SelectedItem}"
                                              />
                        </dxmvvm:Interaction.Triggers>
                    </dxe:ComboBoxEdit>-->

                    <!-- Refresh Button -->

                    <Button Content=" " Grid.Row="0" Grid.Column="2" Height="40" Width="40" HorizontalAlignment="Right"  Command="{Binding RefreshPurchaseOrderViewCommand}" CommandParameter="{Binding ElementName=PendingPurchaseOrderTableView}" Margin="0,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource ButtonRefresh}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{ DynamicResource WareHouseRefreshButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!-- Print Button -->

                    <Button Content=" " Grid.Row="0" Grid.Column="3" Height="40" Width="40" HorizontalAlignment="Right"  Command="{Binding PrintPurchaseOrderViewCommand}" CommandParameter="{Binding ElementName=PendingPurchaseOrderTableView}" Margin="0,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource ButtonPrint}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{ DynamicResource PrinterButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!-- Export Button -->

                    <Button Content=" " Grid.Row="0" Grid.Column="4" Height="40" Width="40" HorizontalAlignment="Right"  Command="{Binding ExportPurchaseOrderViewCommand}" CommandParameter="{Binding ElementName=PendingPurchaseOrderTableView}" Margin="0,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource ButtonExportToExcel}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{ DynamicResource ExcelExportButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!--Send Mail Button-->

                    <Button Content=" " Grid.Row="0" Grid.Column="5" Height="40" Width="40" HorizontalAlignment="Right"
                                                Command="{Binding SendEmailCommand}" CommandParameter="{Binding ElementName=PendingPurchaseOrderTableView}" Margin="0,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource SRMSendPOreminderMail}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{DynamicResource SendPOEmailButtonImageStyle}"  Height="35" Width="30" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!--Send Mail PO to supplier Button-->

                    <Button Content=" " Grid.Row="0" Grid.Column="6" Height="40" Width="40" HorizontalAlignment="Right"  Command="{Binding SendPOEmailCommand}" CommandParameter="{Binding ElementName=PendingPurchaseOrderTableView}" Margin="0,0,5,5">
                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource SRMSendPOrequestMail}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{DynamicResource SendEmailButtonImageStyle}"  Height="35" Width="30" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <!--Save Button-->
                    <Button Content=" " Height="40" Width="40" Grid.Row="0" Grid.Column="7" Command="{Binding UpdateMultipleRowsPendingPurchaseGridCommand}"
                            CommandParameter="{Binding ElementName=PendingPurchaseOrderTableView}" HorizontalAlignment="Right" Margin="0,0,5,5" 
                    IsEnabled="{Binding Path=(UIHelper:SRMPurchaseOrderCellEditHelper.IsValueChanged), ElementName=PendingPurchaseOrderTableView}">


                        <Button.ToolTip>
                            <TextBlock Text="{DynamicResource ButtonSave}"/>
                        </Button.ToolTip>
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <Image Style="{ DynamicResource SaveButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </Grid>

            </DockPanel>
        </Grid>

        <DockPanel Grid.Row="1" HorizontalAlignment="Stretch" >
            <dxwui:NavigationFrame   x:Name="Navigatedframe" Grid.Row="1" AnimationType="None">
                <dxmvvm:Interaction.Behaviors>
                    <dxwuin:FrameNavigationService/>
                </dxmvvm:Interaction.Behaviors>
            </dxwui:NavigationFrame>

            <UIHelper:DpiResizingPanel DockPanel.Dock="Left">
                <dxnav:TileBar DockPanel.Dock="Left"
                               Visibility="{Binding ElementName=expandButton, Path=IsChecked, Converter={dxmvvm:BooleanToVisibilityConverter}}"
                               Margin="0,-20,0,0"
                               SelectedIndex="{Binding SelectedTileIndex,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                               Orientation="Vertical"
                               Width="215"
                               ItemSpacing="5"
                               ItemsSource="{Binding Listofitem ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               ItemContainerStyle="{StaticResource LeftPanelTileItemStyle}"
                               Padding="0"  >

                    <dxmvvm:Interaction.Behaviors>
                        <dxmvvm:EventToCommand Command="{Binding CommandShowFilterPopupClick}" EventName="SelectionChanged" PassEventArgsToCommand="True"/>
                        <dxmvvm:EventToCommand Command="{Binding CommandTileBarClickDoubleClick}"  EventName="MouseDoubleClick" CommandParameter="{Binding ElementName=PendingPurchaseOrderTableView}"/>
                    </dxmvvm:Interaction.Behaviors>
                </dxnav:TileBar>
            </UIHelper:DpiResizingPanel>

            <ToggleButton  x:Name="expandButton" DockPanel.Dock="Left" Style="{StaticResource HideButtonStyle}" IsChecked="True" VerticalAlignment="Center"/>
            <dxg:GridControl DockPanel.Dock="Right" Name="gridControl" SelectionMode="Row"
                             SelectedItem="{Binding SelectedPurchaseOrder,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"  
                             FilterString="{Binding MyFilterString,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             ShowBorder="True"
                             ItemsSource="{Binding ListPurchaseOrder,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" 
                             ImplyNullLikeEmptyStringWhenFiltering="True">
                <!--[pramod.misal][GEOS2-4445][29-06-2023]-->
                <dxg:GridColumn Header="Code" FieldName="Code" MinWidth="100" Width="150" FilterPopupMode="Default" ReadOnly="True"/>
                <!--<dxg:GridColumn Header="Supplier" FieldName="ArticleSupplier.Name" MinWidth="100" Width="300" FilterPopupMode="CheckedList" ReadOnly="True" />-->
                <dxg:GridColumn Header="Supplier" FieldName="ArticleSupplier.Name" MinWidth="100" Width="300" FilterPopupMode="Default" ReadOnly="True" />
                <dxg:GridColumn Header="Country" FieldName="ArticleSupplier.Country.Name" MinWidth="100" Width="150" FilterPopupMode="Default" ReadOnly="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Height="24">
                                <Image Source="{Binding RowData.Row.ArticleSupplier.Country.CountryIconUrl,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource ImageUrlToByteArrayConverter} ,ConverterParameter='image/png'}"   
                            VerticalAlignment="Center" Height="24" Width="24" Margin="2,0,0,0" /><!--[rdixit][28.06.2023]-->

                                <TextBlock Text ="{Binding RowData.Row.ArticleSupplier.Country.Name,UpdateSourceTrigger=PropertyChanged}"  VerticalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <dxg:GridColumn Header="Amount" FieldName="TotalAmount" MinWidth="100" Width="150" FilterPopupMode="Default" ReadOnly="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="4"/>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="1"/>
                                    </Grid.ColumnDefinitions>
                                    <!--//Shubham[skadam] GEOS2-5084 Apply same format always for amounts or quantities 26 12 2023-->
                                    <TextBlock Grid.Column="1" Text="{Binding RowData.Row.TotalAmount,StringFormat={}{0:N2}, ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding RowData.Row.Currency.Symbol}" VerticalAlignment="Center" />
                                </Grid>
                            </StackPanel>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                    <!--//Shubham[skadam] GEOS2-5084 Apply same format always for amounts or quantities 26 12 2023-->
                    <dxg:GridColumn.PrintCellStyle>
                        <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                            <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                            <Setter Property="DisplayTemplate">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <dxe:TextEdit Name="PART_Editor" Text="{Binding RowData.Row.TotalAmount,StringFormat={}{0:N2}, ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Right"
                                                        IsPrintingMode="True">
                                        </dxe:TextEdit>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </dxg:GridColumn.PrintCellStyle>
                </dxg:GridColumn>
                <!--[pallavi.kale][04-03-2025][GEOS2-7012]-->
                <dxg:GridColumn Header="{DynamicResource SRMPendingReviewCurrencyHeader}" FieldName="Currency.Name" FilterPopupMode="CheckedList" MinWidth="90" MaxWidth="150" ReadOnly="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Height="24">
                                <Image Source="{Binding RowData.Row.Currency.CurrencyIconbytes,UpdateSourceTrigger=PropertyChanged}"   
VerticalAlignment="Center" Height="24" Width="24" Margin="2,0,0,0" />
                                <TextBlock Text ="{Binding RowData.Row.Currency.Name,UpdateSourceTrigger=PropertyChanged}"  VerticalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                    <dxg:GridColumn.PrintCellStyle>
                        <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                            <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                            <Setter Property="DisplayTemplate">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <dxe:TextEdit Name="PART_Editor" Text="{Binding Row.Currency.Name}" HorizontalAlignment="Left"
                                    IsPrintingMode="True">
                                        </dxe:TextEdit>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </dxg:GridColumn.PrintCellStyle>
                </dxg:GridColumn>

                <dxg:GridColumn Header="Creation Date" FieldName="CreatedIn" MinWidth="80" Width="100" FixedWidth="True" FilterPopupMode="Default" ReadOnly="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <Border VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <TextBlock Text="{Binding Data.CreatedIn,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Center">
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <!--[rdixit][GEOS2-4353][16.06.2023]-->
                <dxg:GridColumn Header="Status" FieldName="WorkflowStatus.Name" MinWidth="100" Width="120" FixedWidth="True" FilterPopupMode="Default">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <!--[rdixit][GEOS2-4308][23.05.2023]-->
                            <dxe:ComboBoxEdit Name="PART_Editor" ItemsSource="{Binding RowData.Row.LstWorkflowStatusTransition,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                                                  HorizontalAlignment="Stretch" IsTextEditable="False"
                                                  DisplayMember="Name" ValueMember="Name"
                                                  AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True">
                            </dxe:ComboBoxEdit>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                    <dxg:GridColumn.CellStyle>

                        <Style TargetType="{x:Type dxg:LightweightCellEditor}">
                            <Style.Triggers>
                                <Trigger Property="SelectionState" Value="None">
                                    <Setter Property="Background" Value="{Binding RowData.Row.WorkflowStatus.HtmlColor}"/>
                                    <Setter Property="Foreground" Value="{Binding Path=Background,Converter={StaticResource ForegroundColorConverter}}"/>
                                </Trigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RowData.Row.WorkflowStatus.HtmlColor}" Value="" />
                                        <Condition Binding="{Binding Path=(dx:ThemeManager.TreeWalker).ThemeName, RelativeSource={RelativeSource Self}}" Value="BlackAndBlue" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Foreground" Value="White" />
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>


                    </dxg:GridColumn.CellStyle>
                    <dxg:GridColumn.PrintCellStyle>
                        <Style TargetType="dxe:TextEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                            <Setter Property="dxp:ExportSettings.TargetType" Value="Panel" />
                            <Setter Property="DisplayTemplate">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <dxe:TextEdit Name="PART_Editor" Text="{Binding RowData.Row.WorkflowStatus.Name}" HorizontalAlignment="Left"
                                                        IsPrintingMode="True">
                                        </dxe:TextEdit>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </dxg:GridColumn.PrintCellStyle>
                </dxg:GridColumn>
                <dxg:GridColumn Header="Expected Delivery" FieldName="DeliveryDate" SortOrder="Ascending" MinWidth="150" FilterPopupMode="Default" ReadOnly="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>

                            <!--<UIHelper:CellDecorator>
                                <Border x:Name="CloseDateBorder" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                    <dxe:DateEdit Name="PART_Editor" Mask="d" MaskType="DateTime" ShowClearButton="False" IsTextEditable="False"
                                          MaskUseAsDisplayFormat="True" ShowError="False" ShowErrorToolTip="False" InvalidValueBehavior="AllowLeaveEditor"
                                          Foreground="{Binding EditValue, RelativeSource={RelativeSource Self},Converter={Converters:CellColorConverter}}"
                                          MinValue="{Binding DataContext.ExpectedDateMinValue,ValidatesOnDataErrors=False}">
                                    </dxe:DateEdit>
                                </Border>
                            </UIHelper:CellDecorator>-->

                            <Border VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <TextBlock Text="{Binding Data.DeliveryDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style>
                                            <Style.Triggers>
                                                <DataTrigger Value="True" Binding="{Binding Data.DeliveryDate,Converter={Converters:DateCellColorConvertor}}">
                                                    <Setter Property="TextBlock.Foreground" Value="{DynamicResource SleepDaysRedColor}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <dxg:GridColumn Header="Delay" FieldName="Delay" UnboundType="String" MinWidth="80" FilterPopupMode="Default" ReadOnly="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Data.Delay}" HorizontalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="TextBlock.FontWeight" Value="Bold" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Data.Delay, Converter={Converters:GridViewCellValueColorConvertor}}" Value="-1">
                                                <Setter Property="TextBlock.Foreground" Value="Red" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Data.Delay, Converter={Converters:GridViewCellValueColorConvertor}}" Value="+1">
                                                <Setter Property="TextBlock.Foreground" Value="Green" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Data.Delay, Converter={Converters:GridViewCellValueColorConvertor}}" Value="0">
                                                <Setter Property="TextBlock.Foreground" Value="Transparent" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <dxg:GridColumn Header="Deliveries" FieldName="Deliveries" MinWidth="60" FilterPopupMode="Default" ReadOnly="True"/>
                <dxg:GridColumn Header="LatestDeliveryDate" FieldName="LatestDeliveryDate" MinWidth="140" FixedWidth="True" FilterPopupMode="Default" ReadOnly="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <Border VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                <TextBlock Text="{Binding Data.LatestDeliveryDate,StringFormat=d,ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}" HorizontalAlignment="Center">
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <dxg:GridColumn Header="Progress" FieldName="Status" MinWidth="130" Width="150" FixedWidth="True" FilterPopupMode="Default" ReadOnly="True">
                    <dxg:GridColumn.EditSettings>
                        <dxe:ProgressBarEditSettings Maximum="100" Minimum="0" ContentDisplayMode="Value" DisplayFormat="{}{0}%" />
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>

                <dxg:GridColumn Header="Warehouse" FieldName="WarehouseName" MaxWidth="30" FixedWidth="True" ReadOnly="True" 
                                 FilterPopupMode="Default"
                                Fixed="Right" AllowPrinting="True"  AllowMoving="False" />

                <dxg:GridColumn Header="File" MinWidth="40" Width="45" FixedWidth="True" Fixed="Right" FilterPopupMode="Default" ReadOnly="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="btnViewFile" ToolTip="View Document" 
                                                                    Command="{Binding DataContext.OpenPDFDocumentCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding RowData.Row}">
                                <Button.ContentTemplate>
                                    <DataTemplate>
                                        <Image Height="16" Width="16" Source="/Emdep.Geos.Modules.SRM;component/Assets/Images/pdf-icon.png" />
                                    </DataTemplate>
                                </Button.ContentTemplate>
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Cursor" Value="Hand"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>

                
                <dxg:GridColumn Header=" " FieldName="IsSendMail"   Width="40" FixedWidth="True"   AllowSorting="False" HorizontalHeaderContentAlignment="Center" AllowColumnFiltering="False" Fixed="Right" ShowInColumnChooser="False" Visible="True" AllowPrinting="False"  AllowMoving="False" ShowCheckBoxInHeader="True">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <dxe:CheckEdit x:Name="PART_Editor" IsReadOnly="False" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5,0,0,0" />
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>

                

                <dxg:GridControl.TotalSummary>
                    <dxg:GridSummaryItem x:Name="summary" FieldName="Name" SummaryType="Count" DisplayFormat="Total Count: {0}" Visible="True"   Alignment="Left"/>
                </dxg:GridControl.TotalSummary>
                <dxg:GridControl.GroupSummary>
                    <dxg:GridSummaryItem FieldName="Name" SummaryType="Count" DisplayFormat="Count: {0}" Visible="True"   Alignment="Right"/>
                </dxg:GridControl.GroupSummary>
                <dxmvvm:Interaction.Behaviors>
                    <UIHelper:GridSelectingBehavior />
                    <!--<dxmvvm:DependencyPropertyBehavior Binding="{Binding VisibleRowCount,Mode=OneWayToSource}"
                                                       EventName="FilterChanged"
                                                       PropertyName="VisibleRowCount" />-->
                </dxmvvm:Interaction.Behaviors>

                <dxg:GridControl.View>
                    <dxg:TableView x:Name="PendingPurchaseOrderTableView"                                 
                                   NavigationStyle="Cell"
                                   RowDetailsVisibilityMode="Visible"
                                   ShowHorizontalLines="True"
                                   ShowSearchPanelMode="Always"
                                   ShowColumnHeaders="True"
                                   IsColumnChooserVisible="{Binding IsAccountColumnChooserVisible,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                   VerticalScrollbarVisibility="Visible"
                                   AllowFixedColumnMenu="False"
                                   ShowFixedTotalSummary="True" 
                                   SearchPanelHorizontalAlignment="Stretch"
                                   ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                   UIHelper:SRMPurchaseOrderCellEditHelper.IsEnabled="True"
                                   FocusedRow="{Binding SelectedPurchaseOrder, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                   AllowGrouping="True"
                                   ShowIndicator="False" AutoWidth="True" ShowTotalSummary="True" SummaryCalculationMode="AllRows"
                                   ShowGroupPanel="False"
                                   ShowFilterPopup="{DXEvent Handler='@args.PopupBaseEdit.PopupWidth=200'}" 
                                   AllowColumnFiltering="True"
                                   UseLegacyFilterEditor="True"
                                   EnableImmediatePosting="True"
                                   ImmediateUpdateRowPosition="True"
                                   EditorShowMode="MouseDown">

                        <dxg:TableView.Resources>
                            <DataTemplate x:Key="PurchaseOrderListReportPrintHeaderTemplate">
                                <Grid Width="{Binding UsablePageWidth}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="50" />
                                        <ColumnDefinition Width="130" />
                                    </Grid.ColumnDefinitions>
                                    <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" Height="50" />
                                    <dxe:TextEdit Text="Purchase Orders Report" IsPrintingMode="True" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" />
                                    <Image Source="/Emdep.Geos.Modules.Warehouse;component/Assets/Images/GeosReportLogo.png" Height="50" Width="80"  dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                                </Grid>
                            </DataTemplate>

                            <DataTemplate x:Key="PurchaseOrderListReportPrintFooterTemplate">
                                <dxe:TextEdit HorizontalContentAlignment="Center" 
                                                  Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                                  dxp:ExportSettings.TargetType="PageNumber" 
                                                  dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                                  dxp:PageNumberExportSettings.Format="Page {0} of {1}"/>
                            </DataTemplate>

                        </dxg:TableView.Resources>

                        <dxmvvm:Interaction.Behaviors>
                            <dxmvvm:EventToCommand  Command="{Binding CommandGridDoubleClick}" EventName="MouseDoubleClick" CommandParameter="{Binding ElementName=PendingPurchaseOrderTableView}" >
                                <dxmvvm:EventToCommand.EventArgsConverter>
                                    <dx:EventArgsToDataRowConverter/>
                                </dxmvvm:EventToCommand.EventArgsConverter>
                            </dxmvvm:EventToCommand>

                            <dxmvvm:EventToCommand Command="{Binding FilterEditorCreatedCommand}"
                                                   EventName="FilterEditorCreated"
                                                   PassEventArgsToCommand="True" />
                            <dxmvvm:EventToCommand EventName="ShowFilterPopup"
                                                    Command="{Binding DataContext.CommandShowFilterPopupForWorkflowStatusTransitionClick, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    PassEventArgsToCommand="True"/>
                        </dxmvvm:Interaction.Behaviors>
                    </dxg:TableView>
                </dxg:GridControl.View>
            </dxg:GridControl>
        </DockPanel>

    </Grid>

</UserControl>
