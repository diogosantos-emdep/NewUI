<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.SRM.Views.ImportContactsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                        xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
                        xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                        xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                        xmlns:UIConverter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
                        xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
                        mc:Ignorable="d" 
                        d:DesignHeight="200" d:DesignWidth="500"
                        Title="{DynamicResource ImportContactViewHeader}"
                        TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
                        Style="{DynamicResource WinUIDialogWindowStyle}">

    <dxwui:WinUIDialogWindow.Resources>
        <UIConverter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </dxwui:WinUIDialogWindow.Resources>

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding ImportContactCancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:FolderBrowserDialogService 
           
            ShowNewFolderButton="{Binding ShowNewFolderButton}" 
            RootFolder="Desktop"
            RestorePreviouslySelectedDirectory="True"/>
    </dxmvvm:Interaction.Behaviors>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

     

        <Label Content="{DynamicResource ImportContactViewOutputPath}" HorizontalAlignment="Left" VerticalAlignment="Top"
               Grid.Row="1" Grid.Column="0" Margin="0,5,0,0">
        </Label>

        <dxe:ButtonEdit Name="FolderBrowser" Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top" Width="300" Margin="0,5,0,0" AllowDefaultButton="False" IsReadOnly="True">
            <dxe:ButtonEdit.Buttons>
                <dxe:ButtonInfo Content="..." Command="{Binding OpenFolderCommand}" />
            </dxe:ButtonEdit.Buttons>
        </dxe:ButtonEdit>

        <StackPanel Grid.Row="2" Grid.Column="1" 
                    HorizontalAlignment="Center" 
                    Orientation="Horizontal" 
                    Margin="0,5,0,0" Width="300">
            <Button Content="{DynamicResource ImportContactViewAccept}"
        		Command="{Binding ImportContactAcceptButtonCommand}"
        		Height="30"
        		Width="100"
        		Margin="0,0,30,0"
        		VerticalAlignment="Bottom" IsEnabled="{Binding ElementName=FolderBrowser,Path=Text.Length}" />
            <Button Content="{DynamicResource ImportContactViewCancel}"
            	Command="{Binding ImportContactCancelButtonCommand}" 
            	Height="30"
            	Width="100"
            	Margin="30,0,0,0"
            	VerticalAlignment="Bottom"  />

        </StackPanel>

    </Grid>
</dxwui:WinUIDialogWindow>


