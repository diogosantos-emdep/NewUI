<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.PCM.Views.AddEditHardLockLicenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.PCM.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
             xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             mc:Ignorable="d" 
                    Style="{DynamicResource WinUIDialogWindowStyle}"  
                         Title="{Binding WindowHeader}"
                         TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
             d:DesignHeight="300" d:DesignWidth="300">

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CancelButtonCommand,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"/>
    </dxwui:WinUIDialogWindow.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <dxdo:DockLayoutManager Name="dockmanager1" Grid.Row="0" Grid.Column="0">
           
            <dxdo:DockLayoutManager.AutoHideGroups>
                <dxdo:AutoHideGroup DockType="Left" AutoHideSize="300,200">
                    <dxdo:LayoutPanel Caption="{DynamicResource HardLockPlugins}" ItemWidth="300" Style="{DynamicResource LayoutAddButtonStyle}">
                        <dxdo:LayoutPanel.ControlBoxContentTemplate>
                            <DataTemplate>
                                <Button Content=" " 
                                        Background="Gray" 
                                        HorizontalContentAlignment="Center"
                                        Command="{Binding DataContext.AddNewPluginCommand, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow, Mode=FindAncestor}}">
                                    <Button.ToolTip>
                                        <TextBlock Text="{DynamicResource ButtonAdd}"/>
                                    </Button.ToolTip>
                                    <Button.ContentTemplate>
                                        <DataTemplate>
                                            <Image Source="{DynamicResource AddButtonImage}" 
                                                   Height="14" Width="14" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </DataTemplate>
                                    </Button.ContentTemplate>
                                </Button>
                            </DataTemplate>
                        </dxdo:LayoutPanel.ControlBoxContentTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="550"/>
                            </Grid.RowDefinitions>

                            <dxg:GridControl Name="listBoxFamily" Grid.Row="0" Margin="0,0,0,-30"
                                             ItemsSource="{Binding HardLockPluginList, Mode=TwoWay}"
                                             SelectedItem="{Binding SelectedHardLock,Mode=TwoWay}"
                                             HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  
                                              
                                             SelectionMode="MultipleRow"
                                             FilterCriteria="{Binding FilterCriteria, ElementName=searchControlFamily}"
                                             >
                                <dxg:GridControl.Columns>
                                    <dxg:GridColumn  Width="*" FieldName="Name"  FixedWidth="True" ReadOnly="True"/>
                                </dxg:GridControl.Columns>
                                <dxg:GridControl.View>
                                    <dxg:TableView x:Name="FamilyTableView" 
                                                                       ShowGroupedColumns="True"
                                                                       HorizontalScrollbarVisibility="Hidden"
                                                                       VerticalScrollbarVisibility="Auto"
                                                                       ShowIndicator="False"
                                                                       ShowColumnHeaders="False"
                                                                       ShowGroupPanel="False" 
                                                                       AllowEditing="False"
                                                                       NavigationStyle="Row"
                                                                       ShowHorizontalLines="False" 
                                                                       ShowVerticalLines="False"
                                                                       ShowSearchPanelMode="Always"                                                      
                                                                       SearchPanelHorizontalAlignment="Stretch"
                                                                       AllowDragDrop="True"                                                                       
                                                                       >
                                        <dxg:TableView.RowStyle>
                                            <Style TargetType="dxg:RowControl">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Row.IsCurrentPlugin, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Value="true">
                                                        <Setter Property="Foreground" Value="Gray"/>
                                                        <Setter Property="IsEnabled" Value="False"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </dxg:TableView.RowStyle>
                                        <dxmvvm:Interaction.Behaviors>
                                            <dxmvvm:EventToCommand Command="{Binding DataContext.CommandOnDragRecordOverSupportedPlugin, RelativeSource={RelativeSource AncestorType={x:Type dxwui:WinUIDialogWindow}}}"
                                                           EventName="DragRecordOver" PassEventArgsToCommand="True">
                                            </dxmvvm:EventToCommand>

                                            <dxmvvm:EventToCommand Command="{Binding DataContext.CommandCompleteRecordDragDropSupportedPlugin, RelativeSource={RelativeSource AncestorType={x:Type dxwui:WinUIDialogWindow}}}"
                                                           EventName="CompleteRecordDragDrop" PassEventArgsToCommand="True">
                                            </dxmvvm:EventToCommand>

                                        </dxmvvm:Interaction.Behaviors>
                                    </dxg:TableView>
                                </dxg:GridControl.View>
                            </dxg:GridControl>


                            <!--<dxe:SearchControl x:Name="searchControlFamily" Grid.Row="0" Margin="10"
                                               HorizontalAlignment="Stretch" VerticalAlignment="Center"
                                               FilterCondition="Contains"
                                               FilterByColumnsMode="Custom">
                                <dxe:SearchControl.ColumnProvider>
                                    <dxe:SelectorEditColumnProvider>
                                        <dxe:SelectorEditColumnProvider.CustomColumns>
                                            <sys:String>Name</sys:String>
                                        </dxe:SelectorEditColumnProvider.CustomColumns>
                                    </dxe:SelectorEditColumnProvider>
                                </dxe:SearchControl.ColumnProvider>
                            </dxe:SearchControl>

                            <dxe:ListBoxEdit Name="listBoxFamily" Grid.Row="1" Margin="10"
                                             ItemsSource="{Binding HardLockPluginList, Mode=TwoWay}"
                                             HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                             DisplayMember="Name" ValueMember="IdPlugin" Height="500" MaxHeight="500" MinHeight="500"
                                             FilterCriteria="{Binding FilterCriteria, ElementName=searchControlFamily}"
                                             dx:ListBoxDragDropBehavior.AllowDragDrop="True"
                                             SelectionMode="Multiple">

                                <dxmvvm:Interaction.Behaviors>
                                    <dxmvvm:EventToCommand Command="{Binding CommandOnDragRecordOverSupportedPlugin}"
                                                           Event="dx:ListBoxDragDropBehavior.DragRecordOver" 
                                                           PassEventArgsToCommand="True"/>
                                    <dxmvvm:EventToCommand Command="{Binding CommandCompleteRecordDragDropSupportedPlugin}"
                                                           Event="dx:ListBoxDragDropBehavior.CompleteRecordDragDrop" 
                                                           PassEventArgsToCommand="True"/>
                                </dxmvvm:Interaction.Behaviors>

                            </dxe:ListBoxEdit>-->
                        </Grid>
                    </dxdo:LayoutPanel>
                </dxdo:AutoHideGroup>
            </dxdo:DockLayoutManager.AutoHideGroups>
        </dxdo:DockLayoutManager>

        <Grid Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MaxWidth="500" Margin="5,0,0,0">
            <dxlc:LayoutGroup Orientation="Vertical">
                <dxlc:LayoutGroup Orientation="Horizontal">
                    <TextBlock  Text="{DynamicResource AddHardLockPluginReference}" VerticalAlignment="Center"/>
                    <dxe:ComboBoxEdit  Margin="-100,0,0,0"
                                                      VerticalAlignment="Top"
                                                      HorizontalAlignment="Stretch"
                                                      ItemsSource="{Binding ReferenceList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                       
                                                      DisplayMember="Reference" ValueMember="IdArticle"
                                                      AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True">

                        <!--SelectedItem="{Binding SelectedReference,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"-->
                        <dxe:ComboBoxEdit.SelectedItem>
                            <Binding Path="SelectedReference" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                <Binding.ValidationRules>
                                    <Validation:AddEditHardLockLicenseValidation FieldName="SelectedReference" ValidationStep="UpdatedValue"  
                                                                                                        ValidatesOnTargetUpdated="True"/>
                                </Binding.ValidationRules>
                            </Binding>
                        </dxe:ComboBoxEdit.SelectedItem>

                    </dxe:ComboBoxEdit>
                </dxlc:LayoutGroup>
                
                <dxlc:LayoutGroup 
                x:Name="GroupFamily" dxlc:LayoutControl.AllowHorizontalSizing="True"
                                                              Header="SupportedPlugins"
                                                              View="GroupBox" 
                                                              Padding="-10"
                                                              IsCollapsible="False" IsCollapsed="False"
                                                              Grid.Row="1">
                    <dxlc:LayoutGroup.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left">
                                                            <Run Text="{DynamicResource AddHardLockPluginSupportedPlugins}"></Run>
                                                            <Run Text=" : "></Run>
                                                            <Run Text="{Binding DataContext.SupportedPluginList.Count,Mode=OneWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}"></Run>
                            </TextBlock>
                        </DataTemplate>
                    </dxlc:LayoutGroup.HeaderTemplate>
                  
                    <dxg:GridControl x:Name="GridFamily" ShowBorder="True"  Height="548"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                                                 ItemsSource="{Binding SupportedPluginList, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                                 SelectedItem="{Binding SelectedPlugin,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">

                        <dxg:GridControl.Columns>
                            <dxg:GridColumn  Header="{DynamicResource GridColumnId}" FieldName="IdPlugin" FilterPopupMode="CheckedList" AllowEditing="False" Width="50" FixedWidth="True" ReadOnly="True"/>
                            <dxg:GridColumn  Header="{DynamicResource GridColumnName}" FieldName="Name" FilterPopupMode="CheckedList" AllowEditing="False" Width="*" FixedWidth="True" ReadOnly="True"/>
                            <!--<dxg:GridColumn  Width="*" FieldName="Name" FixedWidth="True" ReadOnly="True"/>-->
                            <dxg:GridColumn  Header=" " Width="45" FieldName="" FixedWidth="True">
                                <dxg:GridColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Name="deleteRowItem" ToolTip="{DynamicResource Delete}" Command="{Binding DataContext.DeleteSupportedPluginCommand,RelativeSource={RelativeSource AncestorType={x:Type dxwui:WinUIDialogWindow}}}"
                                                                           Visibility="{Binding Visible, UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">
                                            >
                                            <Button.ContentTemplate>
                                                <DataTemplate>
                                                    <Image Height="16" Width="16"  Style="{ DynamicResource DeleteStyle}" />
                                                </DataTemplate>
                                            </Button.ContentTemplate>
                                            <Button.Style>
                                                <Style TargetType="{x:Type Button}">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Cursor" Value="Hand"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DataTemplate>
                                </dxg:GridColumn.CellTemplate>
                            </dxg:GridColumn>
                        </dxg:GridControl.Columns>

                        <dxg:GridControl.View>
                            <dxg:TableView x:Name="FamiliesTableView"
                                                                       ShowGroupedColumns="True"
                                                                       HorizontalScrollbarVisibility="Hidden"
                                                                       VerticalScrollbarVisibility="Auto"
                                                                       ShowIndicator="False"
                                                                       ShowColumnHeaders="True"
                                                                       ShowGroupPanel="False" 
                                                                       AllowEditing="False"
                                                                       NavigationStyle="Row"
                                                                       ShowHorizontalLines="True" 
                                                                       ShowVerticalLines="True"
                                                                       ShowSearchPanelMode="Always"
                                                                       SearchPanelHorizontalAlignment="Stretch"
                                                                       AllowDragDrop="True"
                                                                       FocusedRow="{Binding Path=SelectedPlugin, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" >

                                <dxmvvm:Interaction.Behaviors>
                                    <dxmvvm:EventToCommand Command="{Binding CommandOnDragRecordOverSupportedPluginGrid}"
                                                                                       EventName="DragRecordOver" PassEventArgsToCommand="True">
                                    </dxmvvm:EventToCommand>
                                </dxmvvm:Interaction.Behaviors>

                            </dxg:TableView>
                        </dxg:GridControl.View>
                    </dxg:GridControl>
                </dxlc:LayoutGroup>
            </dxlc:LayoutGroup>
          
            
           

        </Grid>

        <Grid Grid.Row="2" Grid.Column="1" ShowGridLines="False" VerticalAlignment="Center" HorizontalAlignment="Center" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="3" Grid.Column="0"
                    Style="{DynamicResource Button16Style}"
                    HorizontalAlignment="Right"
                    Content="{DynamicResource AddCatalogueAcceptButton}"
                    Command="{Binding DataContext.AcceptButtonCommand,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}"
                    Height="30"
                    Width="100"
                    CommandParameter="{Binding ElementName=Compatibility}"/>

            <Button Grid.Row="3" Grid.Column="2"
                    Style="{DynamicResource Button16Style}"
                    HorizontalAlignment="Left"
                    Content="{DynamicResource AddCatalogueCancelButton}"
                    Command="{Binding CancelButtonCommand}"
                    Height="30"
                    Width="100" />
        </Grid>
    </Grid>
</dxwui:WinUIDialogWindow>
