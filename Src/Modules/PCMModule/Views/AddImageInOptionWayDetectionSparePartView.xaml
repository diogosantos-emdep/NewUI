<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.PCM.Views.AddImageInOptionWayDetectionSparePartView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.PCM.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:Converter="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             mc:Ignorable="d" 
             Style="{DynamicResource WinUIDialogWindowStyle}"
             Title="{Binding WindowHeader}"
             TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}">

    <dxwui:WinUIDialogWindow.Resources>
        <dxmvvm:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Converter:GroupboxBackroundColorConvertor x:Key="GroupboxBackroundColorConvertor" />
        <Converter:ForegroundColorConverter x:Key="ForegroundColorConverter" />
    </dxwui:WinUIDialogWindow.Resources>


    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding EscapeButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="270"/>
        </Grid.ColumnDefinitions>

        <!--File-->
        <TextBlock Grid.Row="0" Grid.Column="0"  Text="{DynamicResource File}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center"  />
        <dxe:TextEdit x:Name="FileTextEdit" Grid.Row="0" Grid.Column="2"
                      Text="{Binding ImageName}" VerticalContentAlignment="Center" NullText="{DynamicResource Name}">

            <!--<dxmvvm:Interaction.Behaviors>
                <dxmvvm:EventToCommand EventName="PreviewDragEnter"
                        Command="{Binding DataContext.PreviewDragEnterCommand, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}" 
                                    PassEventArgsToCommand="True"   /> 
                
                <dxmvvm:EventToCommand EventName="PreviewDragOver"
                        Command="{Binding DataContext.PreviewDragOverCommand, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}" 
                                    PassEventArgsToCommand="True"   />

                <dxmvvm:EventToCommand EventName="PreviewDrop"
                        Command="{Binding DataContext.PreviewDropCommand, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}" 
                                    PassEventArgsToCommand="True"   />
            </dxmvvm:Interaction.Behaviors>-->
            <!--CommandParameter="{Binding ElementName=FileTextEdit}-->
            <!--CommandParameter="{Binding ElementName=cmbSite, Path=SelectedIndexChanged}" UseDispatcher="True"-->
        </dxe:TextEdit>

        <!--ChooseFile-->
        <Button Grid.Row="2" Grid.Column="2" Content="{DynamicResource ChooseFile}" Width="115" HorizontalAlignment="Right" Command="{Binding ChooseFileActionCommand}"/>

        <!--Description-->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="{DynamicResource Description}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center" />
        <dxe:TextEdit Grid.Row="4" Grid.Column="2"  Text="{Binding Description}" NullText="{DynamicResource Description}"  VerticalContentAlignment="Top" TextWrapping="Wrap" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AcceptsReturn="True"/>

        <!--Checkbox--> <!--[rdixit][GEOS2-2694][01.08.2022]-->
        <!--<dxe:CheckEdit Grid.Row="6" Grid.Column="2" IsChecked="{Binding IsCheckedDefaultImage,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
                       IsReadOnly="{Binding IsImageReadOnly,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" VerticalAlignment="Center">

            <TextBlock Grid.Row="6" Grid.Column="2" Margin="5 0 0 0" Text="{DynamicResource DefaultPicture}" Style="{StaticResource TextBlock14Style}" VerticalAlignment="Center" />
        </dxe:CheckEdit>-->


        <Grid Grid.Row="8" ShowGridLines="False" Grid.Column="2" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button  HorizontalAlignment="Right"
                     Style="{DynamicResource Button16Style}"
                     VerticalAlignment="Top"
                     Content="{DynamicResource AddCatalogueAcceptButton}"
                     Command="{Binding AcceptImageActionCommand}"
                     Height="30"
                     Width="100" />

            <Button Grid.Row="10" Grid.Column="2"
                    Style="{DynamicResource Button16Style}"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Content="{DynamicResource AddCatalogueCancelButton}"
                    Command="{Binding CancelButtonCommand }"
                    Height="30"
                    Width="100" />
        </Grid>

    </Grid>
</dxwui:WinUIDialogWindow>
