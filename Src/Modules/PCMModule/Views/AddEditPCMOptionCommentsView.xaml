<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.PCM.Views.AddEditPCMOptionCommentsView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.PCM.Views"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             mc:Ignorable="d" 
             Title="{Binding WindowHeader,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
             Style="{DynamicResource WinUIDialogWindowStyle}"
             TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
             d:DesignHeight="300" d:DesignWidth="300">
    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>


    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="120"></RowDefinition>

            <RowDefinition Height="5"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="5"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="5"></ColumnDefinition>
            <ColumnDefinition Width="450"></ColumnDefinition>
            <ColumnDefinition Width="5"></ColumnDefinition>
        </Grid.ColumnDefinitions>


        <TextBox Grid.Row="0" Grid.Column="2" VerticalContentAlignment="Top" TextWrapping="Wrap"  VerticalAlignment="Stretch" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AcceptsReturn="True" 
                 Text="{Binding DataContext.NewItemComment,RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Grid}}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 >


        </TextBox>

        <StackPanel Grid.Row="2" 
                    HorizontalAlignment="Center"
                    VerticalAlignment="top"
                    Orientation="Horizontal" 
                    Width="290" Grid.Column="2">

            <Button Content="{DynamicResource Accept}"
                            Command="{Binding AcceptButtonCommand}"
                            Height="30"
                            Width="90"
                            Margin="30,0,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom" />

            <Button Content="{DynamicResource Cancel}"
                            Command="{Binding CancelButtonCommand}" 
                            Height="30"
                            Width="90"
                            Margin="60,0,0,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"/>
        </StackPanel>


    </Grid>

</dxwui:WinUIDialogWindow>
