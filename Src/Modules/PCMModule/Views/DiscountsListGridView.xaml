<UserControl x:Class="Emdep.Geos.Modules.PCM.Views.DiscountsListGridView"  
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.PCM.Views"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxn="http://schemas.devexpress.com/winfx/2008/xaml/navbar"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
             xmlns:ViewModel="clr-namespace:Emdep.Geos.Modules.PCM.ViewModels"
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
             mc:Ignorable="d" >
    <!--[rdixit][17.08.2022][GEOS2-3099]-->
    <Grid Background="{DynamicResource EmpDetailBackColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--<Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="{DynamicResource DiscountsList}" HorizontalAlignment="Left" Grid.Column="0" Style="{StaticResource LabelTitleStyle}" Grid.Row="0" VerticalAlignment="Center" />

        <Grid Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="AUTO"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <!-- Add Button -->
            <Button Content=" " Grid.Row="0" Grid.Column="1" Height="40" Width="40" HorizontalAlignment="Right"
                            Command="{Binding AddDiscountsListCommand}" CommandParameter="{Binding ElementName=ItemListTableView}" Margin="5,0,5,5"                            
                            IsEnabled="{Binding Source={x:Static GeosApplication:GeosApplication.Instance}, Path=IsPLMPermissionChange}">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonAdd}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{ DynamicResource PCMAddButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <!-- Refresh Button -->
            <Button Content=" " Grid.Row="0" Grid.Column="2" Height="40" Width="40" HorizontalAlignment="Right"
                            Command="{Binding RefreshDiscountsListCommand}" CommandParameter="{Binding ElementName=ItemListTableView}" Margin="0,0,5,5">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonRefresh}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{ DynamicResource PCMRefreshButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <!--Print Button-->
            <Button Content=" " Grid.Row="0" Grid.Column="3" Height="40" Width="40" HorizontalAlignment="Right"
                            Command="{Binding PrintDiscountsListCommand}" CommandParameter="{Binding ElementName=ItemListTableView}" Margin="0,0,5,5">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonPrint}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{DynamicResource PCMPrintButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <!-- Export to Excel -->
            <Button Content=" " Grid.Row="0" Grid.Column="4" Height="40" Width="40" HorizontalAlignment="Right"
                            Command="{Binding ExportDiscounts}" CommandParameter="{Binding ElementName=ItemListTableView}" Margin="0,0,5,5">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonExportToExcel}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{DynamicResource PCMExportToExcelButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </Grid>

        <dxg:GridControl x:Name="dlgrid" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                             ItemsSource="{Binding DiscountsList,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" SelectionMode="Row"
                             SelectedItem="{Binding SelectedDiscounts,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                             ImplyNullLikeEmptyStringWhenFiltering="True">

            <dxg:GridColumn Header="{DynamicResource GridName}" FieldName="Name" Width="200" ReadOnly="True" FixedWidth="False" FilterPopupMode="Default" />
            <dxg:GridColumn Header="{DynamicResource Description}" FieldName="Description" Width="550" AllowResizing="True" FixedWidth="False" ReadOnly="True"  FilterPopupMode="Default"/>
            <dxg:GridColumn Header="{DynamicResource DiscountColScope}" FieldName="Scope" Width="100" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList" IsSmart="True"/>
            <dxg:GridColumn Header="{DynamicResource DiscountColPlatform}" FieldName="Platform" Width="100"  FixedWidth="True" ReadOnly="True" FilterPopupMode="CheckedList" IsSmart="True"/>
            <dxg:GridColumn Header="{DynamicResource DiscountStartDate}" FieldName="StartDateNew" Width="100" ReadOnly="True" FixedWidth="True" FilterPopupMode="Default">
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text ="{Binding RowData.Row.StartDateNew,StringFormat=d, ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture},UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" VerticalAlignment="Center" Margin="5,0,0,0">
                        </TextBlock>
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
            </dxg:GridColumn>
            <dxg:GridColumn Header="{DynamicResource DiscountEndDate}" FieldName="EndDateNew" Width="100" ReadOnly="True" FixedWidth="True" FilterPopupMode="Default">
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text ="{Binding RowData.Row.EndDateNew,StringFormat=d, ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture},UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" VerticalAlignment="Center" Margin="5,0,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Value="True" Binding="{Binding RowData.Row.OverdueEndDate,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                                            <Setter Property="Foreground" Value="Red" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
            </dxg:GridColumn>
            <dxg:GridColumn Header="{DynamicResource DiscountValue}" FieldName="Value" Width="Auto" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList">
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <dxe:TextEdit Name="PART_Editor"
                                                      HorizontalAlignment="Right"
                                                      VerticalAlignment="Center"                                                  
                                                      ShowBorder="False" Mask="N1" MaskType="Numeric" 
                                                      Text="{Binding Value, Mode=TwoWay}"  MaskUseAsDisplayFormat="True" SelectAllOnGotFocus="True" SelectAllOnMouseUp="True">
                        </dxe:TextEdit>
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
                <!--<dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <dxe:TextEdit Name="PART_Editor"
                                                      HorizontalAlignment="Right"
                                                      VerticalAlignment="Center"                                                  
                                                      ShowBorder="False" Mask="N1" MaskType="Numeric" 
                                                      Text="{Binding Value, Mode=TwoWay}"  MaskUseAsDisplayFormat="True" IsReadOnly="{Binding RowData.Row.IsReadOnly, Mode=TwoWay}" SelectAllOnGotFocus="True" SelectAllOnMouseUp="True">
                        </dxe:TextEdit>
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>-->
            </dxg:GridColumn>
            <dxg:GridColumn Header="{DynamicResource DiscountColPlant}" FieldName="Plants" Width="90" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList" IsSmart="True"/>
            <dxg:GridColumn Header="{DynamicResource DiscountColGroup}" FieldName="Group" Width="100" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList" IsSmart="True"/>
            <dxg:GridColumn Header="{DynamicResource DiscountColRegion}" FieldName="Region" Width="100"  ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList" IsSmart="True"/>
            <dxg:GridColumn Header="{DynamicResource DiscountColCountry}" FieldName="Country" Width="100"  ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList" IsSmart="True"/>
            <dxg:GridColumn Header="{DynamicResource DiscountColPlant}" FieldName="Plant" Width="90" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList" IsSmart="True"/>
            <dxg:GridColumn Header="{DynamicResource DiscountColLastUpdated}" FieldName="LastUpdateNew" Width="100" ReadOnly="True"  FixedWidth="True" FilterPopupMode="Default">
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text ="{Binding RowData.Row.LastUpdateNew,StringFormat=d, ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture},UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" VerticalAlignment="Center" Margin="5,0,0,0">
                        </TextBlock>
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
            </dxg:GridColumn>
            <dxg:GridColumn Header="{DynamicResource DiscountInUse}" FieldName="InUse" Width="60" ReadOnly="True" FixedWidth="True" FilterPopupMode="CheckedList"/>

            <dxmvvm:Interaction.Behaviors>
                <UIHelper:GridSelectingBehavior />
            </dxmvvm:Interaction.Behaviors>


            <dxg:GridControl.TotalSummary>
                <dxg:GridSummaryItem x:Name="summary" FieldName="GridName" SummaryType="Count" DisplayFormat="Total Count : {0}" Visible="True" Alignment="Left"/>
            </dxg:GridControl.TotalSummary>

            <dxg:GridControl.View>
                <dxg:TableView x:Name="ItemListTableView"
                                   ShowGroupedColumns="True"  
                                   HorizontalScrollbarVisibility="Auto"  
                                   VerticalScrollbarVisibility="Visible" 
                                   ShowColumnHeaders="True"  
                                   ShowGroupPanel="False"  
                                   AllowEditing="True"  
                                   ShowHorizontalLines="True"  
                                   ShowVerticalLines="True"  
                                   ShowSearchPanelMode="Always" 
                                   SearchPanelHorizontalAlignment="Stretch" 
                                   EnableImmediatePosting="True"
                                   ImmediateUpdateRowPosition="True"
                                   AllowColumnMoving="True"
                                   NavigationStyle="Row"
                                   ShowFixedTotalSummary="True"
                                   ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"
                                   FocusedRow="{Binding SelectedDiscounts, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                   ShowFilterPopup="{DXEvent Handler='@args.PopupBaseEdit.PopupWidth=200'}" 
                                   IsColumnChooserVisible="{Binding IsDLColumnChooserVisible,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                                   AllowBestFit="True"
                                   AllowPerPixelScrolling="True"
                                   AutoMoveRowFocus="False"
                                   AllowScrollAnimation="True"
                                   ShowIndicator="True"
                                   AllowConditionalFormattingMenu="True"
                                   HorizontalContentAlignment="Center"
                                   UseLegacyFilterEditor="True"
                                   AllowConditionalFormattingManager="True"
                                   >
                    <dxg:TableView.Resources>
                        <DataTemplate x:Key="DiscountsListPrintHeaderTemplate">
                            <Grid Width="{Binding UsablePageWidth}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="130" />
                                </Grid.ColumnDefinitions>
                                <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" Height="50" />
                                <dxe:TextEdit Text="Discount Report" IsPrintingMode="True" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" />
                                <Image Source="/Emdep.Geos.Modules.PCM;component/Assets/Images/GeosReportLogo.png" Height="50" Width="80"  dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                            </Grid>
                        </DataTemplate>

                        <DataTemplate x:Key="DiscountsListPrintFooterTemplate">
                            <dxe:TextEdit HorizontalContentAlignment="Center" 
                                                  Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                                  dxp:ExportSettings.TargetType="PageNumber" 
                                                  dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                                  dxp:PageNumberExportSettings.Format="Page {0} of {1}"/>
                        </DataTemplate>

                    </dxg:TableView.Resources>

                    <dxmvvm:Interaction.Behaviors>

                        <dxmvvm:EventToCommand EventName="ShowFilterPopup"
                                               Command="{Binding DiscountShowFilterPopupCommand}"
                                               PassEventArgsToCommand="True"/>
                        <dxmvvm:EventToCommand Command="{Binding EditDiscountCommand}" 
                                                   EventName="MouseDoubleClick"
                                                   CommandParameter="{Binding ElementName=ItemListTableView}">
                            <dxmvvm:EventToCommand.EventArgsConverter>
                                <dx:EventArgsToDataRowConverter />
                            </dxmvvm:EventToCommand.EventArgsConverter>
                        </dxmvvm:EventToCommand>
                        <!--<dxmvvm:EventToCommand EventName="Loaded"
                                                                                     Command="{Binding DLGridControlLoadedCommand}"
                                                                                    PassEventArgsToCommand="True"
                                                                                     CommandParameter="{Binding ElementName=dlgrid}" >
                        </dxmvvm:EventToCommand>
                        <dxmvvm:EventToCommand EventName="ShownColumnChooser"
                                                                                 Command="{Binding DLItemListTableViewLoadedCommand}"
                                                                                 CommandParameter="{Binding ElementName=ItemListTableView}" >
                        </dxmvvm:EventToCommand>
                        <dxmvvm:EventToCommand EventName="Unloaded"
                                            Command="{Binding DLGridControlUnloadedCommand}"
                                            PassEventArgsToCommand="True"
                                            CommandParameter="{Binding ElementName=dlgrid}" >
                        </dxmvvm:EventToCommand>
                        <dxmvvm:EventToCommand SourceObject="{Binding ElementName=ItemListTableView, Path=FormatConditions}" 
                                                                                                   EventName="CollectionChanged" 
                                                                                                   Command="{Binding DLGridControlUnloadedCommand}"
                                                                                                   CommandParameter="{Binding ElementName=dlgrid}"/>-->
                    </dxmvvm:Interaction.Behaviors>

                </dxg:TableView>
            </dxg:GridControl.View>
        </dxg:GridControl>
    </Grid>
</UserControl>
