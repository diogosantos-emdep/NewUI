<dxwui:WinUIDialogWindow    x:Class="Emdep.Geos.Modules.PCM.Views.SynchronizationFailedView"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                            xmlns:local="clr-namespace:Emdep.Geos.Modules.PCM.Views"
                            xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
                            xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
                            mc:Ignorable="d" 
                            d:DesignHeight="300" d:DesignWidth="300">

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape"  Command="{Binding CancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <!--<Grid>
        <Border BorderBrush="Red" BorderThickness="5">-->

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="30" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="100" />
                    <RowDefinition Height="20" />

                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="500" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0">
        <Image Height="50" Width="50" Source="/Emdep.Geos.Modules.PCM;component/Assets/Images/warning256X256.png" />
                </TextBlock>

                <Grid Grid.Row="0" Grid.Column="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Synchronization Failed :" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" FontSize="20"></Label>

                    <Label Grid.Row="0" Grid.Column="1" Content="{Binding ErrorMessage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="20" Margin="10 0 0 0"></Label>
                </Grid>

                <!--<Label Grid.Row="0" Grid.Column="2" Content="Synchronization Failed :" HorizontalAlignment="Left" VerticalAlignment="Top" FontWeight="Bold" FontSize="20"></Label>

        <Label Grid.Row="0" Grid.Column="" Content="API Not responding !" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="20"></Label>-->

                <Label Grid.Row="2" Grid.Column="0" Content="Reference:" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="16"></Label>

                <Label Grid.Row="2" Grid.Column="2" Content="{Binding Reference, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Top"></Label>

        <dxe:TextEdit x:Name="Text_EditBox" Grid.Row="4" Grid.Column="2" Height="80" Width="500" FontSize="15" ScrollViewer.VerticalScrollBarVisibility="Visible"
                      TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Top" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AcceptsReturn="True"
                      VerticalContentAlignment="Top" Text="{Binding Details,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                </dxe:TextEdit>

        <Button x:Name="CopyButton" Grid.Row="4" Grid.Column="4" VerticalAlignment="Top" VerticalContentAlignment="Top" Height="40" Width="45"
                Command="{Binding CopyTextCommand}" CommandParameter="{Binding ElementName=Text_EditBox}" >
            <Button.ContentTemplate>
                <DataTemplate>
                    <Image Height="20" Width="20" Style="{ DynamicResource DuplicateBasePriceImageStyle}" ToolTip="Copy Reference">
                    </Image>
                </DataTemplate>
            </Button.ContentTemplate>
        </Button>


        <StackPanel Grid.Row="6" Grid.Column="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Orientation="Horizontal" 
                    Width="290" 
                    >

                    <Button Command="{Binding AcceptButtonCommand}"
                            CommandParameter="{Binding ElementName=CopyButton}"
                            Height="30"
                            Width="150"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom" 
                    Style="{DynamicResource Button16Style}">
                        <Button.ContentTemplate>
                            <DataTemplate>
                        <StackPanel Orientation="Horizontal" Height="24">
                            <Image Height="16" Width="16"  Style="{ DynamicResource SynchronizeStyle}" />
                            <TextBlock Text ="Synchronize"  VerticalAlignment="Center" Margin="5,0,0,0" ></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <Button Content="{DynamicResource Cancel}"
                            Command="{Binding CancelButtonCommand}" 
                            Height="30"
                            Width="120"
                            Margin="10,0,0,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                    Style="{DynamicResource Button16Style}"/>
                </StackPanel>
            </Grid>
        <!--</Border>
    </Grid>-->
</dxwui:WinUIDialogWindow>
