<Window x:Class="Emdep.Geos.Modules.SCM.Views.DocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.SCM.Views"
             xmlns:dxpdf="http://schemas.devexpress.com/winfx/2008/xaml/pdf" 
             Title="{Binding FileName,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
             xmlns:RichEdit="http://schemas.devexpress.com/winfx/2008/xaml/richedit"
             mc:Ignorable="d"
             WindowState="Maximized"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <ContentControl>
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="Content">
                        <Setter.Value>
                            <dxpdf:PdfViewerControl x:Name="PdfViewer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                                                DocumentSource="{Binding PdfDoc, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                                CommandBarStyle="Bars" />
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsPdf}" Value="False">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <Image Source="{Binding ConnectorAttachedImage}" Stretch="Uniform"/>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>

                    </Style.Triggers>

                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>

</Window>
