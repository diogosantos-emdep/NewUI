<UserControl x:Class="Workbench.Views.WorkshopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"                 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxwuin="http://schemas.devexpress.com/winfx/2008/xaml/windowsui/navigation"
             d:DesignHeight="300" d:DesignWidth="300">

    <!--<UserControl.Resources>
        <ResourceDictionary Source="/GeosWorkbench;component/Resources/Language.xaml" />
        </UserControl.Resources>-->
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <dxwui:PageAdornerControl Header="Workshop Assembly" Grid.Row="0" >
            <dxwui:PageAdornerControl.HeaderTemplate>
                <DataTemplate>
                    <Grid>


                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <ContentPresenter Content="{Binding}"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="1">

                            <dxwui:NavigationButton Style="{DynamicResource NavigationButtonWorkshop}"
                                                    Margin="10, 0, 0, 0"
                                                    Content="Harnesspart"
                                                    NavigateTo="Workbench.Views.HarnessPartsView">
                                
                            </dxwui:NavigationButton>

                            <dxwui:NavigationButton Style="{DynamicResource NavigationButtonWorkshop}"
                                                    Margin="10, 0, 0, 0"
                                                    Content="Design" 
                                                    NavigateTo="Workbench.Views.DesignView">
                                
                            </dxwui:NavigationButton>

                        </StackPanel>

                    </Grid>
                </DataTemplate>
            </dxwui:PageAdornerControl.HeaderTemplate>
        </dxwui:PageAdornerControl>

    </Grid>
</UserControl>