<UserControl
            x:Class="Workbench.Views.MainSplashScreenView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
            xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys"
            Width="800" Height="600">

    <UserControl.Resources>
        <ControlTemplate x:Key="{dxet:ProgressBarEditStyleThemeKey ResourceKey=ProgressBarMarqueeHorizontalAnimationElementTemplate, IsThemeIndependent=True}">
            <Border Style="{DynamicResource BorderBackgroundInner}"  />
        </ControlTemplate>
        <ControlTemplate x:Key="{dxet:ProgressBarEditThemeKey ResourceKey=PanelBackgroundHorizontalTemplate, IsThemeIndependent=True}" TargetType="{x:Type ContentControl}">
            <Border x:Name="Border" Style="{DynamicResource SplasherBackgroundColor}"  UseLayoutRounding="True" >
                <ContentPresenter x:Name="content" Margin="1" />
            </Border>
        </ControlTemplate>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot">
        <Grid x:Name="Splash" Width="498"  Height="368" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,100,0,0">
            <Grid x:Name="Back">
                <Grid.Effect>
                    <DropShadowEffect ShadowDepth="1" Direction="-90" BlurRadius="10" Opacity="0.25"/>
                </Grid.Effect>
                <Border Style="{DynamicResource BorderBackgroundOuter}" CornerRadius="3"  />
                <Border CornerRadius="2" Margin="1" />
            </Grid>
            <Grid x:Name="Content_Area" Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Width="475" Height="180" Style="{DynamicResource BorderBackgroundInner}">
                    <Grid Margin="0,15,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Image  Style="{DynamicResource SplashScreenWorkbenchStyle}" Width="350" Height="163"/>
                        <Label HorizontalAlignment="Right" VerticalAlignment="Bottom"
                               FontWeight="Bold" FontSize="22" Height="30" Width="150"
                               Content="{DynamicResource SplashScreenWorkbench}" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Margin="0,0,30,0" Style="{DynamicResource LableForegroundColor}" />
                    </Grid>
                </Border>
                <TextBlock x:Name="Info"  Margin="60,35,0,0" TextWrapping="Wrap" Text="{DynamicResource SplashScreenStarting}" Grid.Row="1" Style="{DynamicResource TextBlockForegroundColor}" />

                <dxe:ProgressBarEdit  Style="{DynamicResource ProgressBarEditForeground}"  Height="20" Width="355" Grid.Row="2" Margin="0,2,0,0" VerticalAlignment="Top" BorderBrush="White" >
                    <dxe:ProgressBarEdit.StyleSettings>
                        <dxe:ProgressBarMarqueeStyleSettings/>
                    </dxe:ProgressBarEdit.StyleSettings>
                </dxe:ProgressBarEdit>

                <Border Grid.Row="3" Height="80" Margin="-11,0,-11,-11" Style="{DynamicResource BorderBackgroundMainWindow}">
                    <DockPanel x:Name="Footer"  >
                        <TextBlock  x:Name="Footer_Text" TextWrapping="Wrap" Margin="20,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{DynamicResource SplashScreenCopyright}" Style="{DynamicResource TextBlockForegroundColor}" />
                        <Image x:Name="Logotype" DockPanel.Dock="Right" Style="{DynamicResource EmdepGeosLogo}"   Stretch="Fill" HorizontalAlignment="Right" UseLayoutRounding="True" Width="140"  Height="45" Margin="0,0,10,0"/>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>