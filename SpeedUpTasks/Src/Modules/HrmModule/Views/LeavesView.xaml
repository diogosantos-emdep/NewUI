<UserControl x:Class="Emdep.Geos.Modules.Hrm.Views.LeavesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Emdep.Geos.Modules.Hrm.Views"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"  
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:UIHelper="clr-namespace:Emdep.Geos.UI.Helper;assembly=Emdep.Geos.UI.Helper"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Content="{DynamicResource Leaves}" HorizontalAlignment="Left" Style="{StaticResource LabelTitleStyle}" Grid.Row="0" Grid.Column="0" />
        <Grid HorizontalAlignment="Right" Grid.Row="0" Grid.Column="1" Margin="0,5,0,0" >
            <Grid.RowDefinitions>
                <RowDefinition Height="AUTO"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="AUTO"/>
                <ColumnDefinition Width="AUTO" />
                <ColumnDefinition Width="AUTO" />
                <ColumnDefinition Width="AUTO" />
 
            </Grid.ColumnDefinitions>

            <!-- Refresh Leaves Grid-->

            <Button Visibility="Visible" Grid.Row="0" Grid.Column="1" Height="40" Width="40" HorizontalAlignment="Right" 
                                Command="{Binding RefreshButtonCommand}"
                                CommandParameter="{Binding ElementName=JobDescriptionTableView}"
                                Margin="0,0,5,5">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonRefresh}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{DynamicResource RefreshButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <!-- Print Leaves List-->
            <Button Visibility="Visible" Grid.Row="0" Grid.Column="2" Height="40" Width="40" HorizontalAlignment="Right" 
                        Command="{Binding PrintButtonCommand}"
                        CommandParameter="{Binding ElementName=LeavesTableView}" Margin="0,0,5,5">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonPrint}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{DynamicResource PrinterButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>

            <!-- Export Leaves List-->

            <Button Visibility="Visible" Grid.Row="0" Grid.Column="3" Height="40" Width="40" HorizontalAlignment="Right" 
                    Command="{Binding ExportButtonCommand}"
                    CommandParameter="{Binding ElementName=LeavesTableView}" Margin="0,0,5,5">
                <Button.ToolTip>
                    <TextBlock Text="{DynamicResource ButtonExportToExcel}"/>
                </Button.ToolTip>
                <Button.ContentTemplate>
                    <DataTemplate>
                        <Image Style="{DynamicResource ExcelExportButtonImageStyle}"  Height="24" Width="24" Margin="-10"/>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </Grid>
        <dxg:GridControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                         ItemsSource="{Binding Path=LeaveList,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" SelectionMode="Row"
                         SelectedItem="{Binding Path=SelectedLeave,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                         ImplyNullLikeEmptyStringWhenFiltering="True">
            <dxg:GridColumn Header="{DynamicResource LeaveName}" FieldName="Value" FilterPopupMode="CheckedList" MinWidth="100" Width="200" ReadOnly="True" FixedWidth="True" />
            <dxg:GridColumn Header="{DynamicResource LeaveAbbreviation}" FieldName="Abbreviation" FilterPopupMode="CheckedList" MinWidth="100" Width="150" ReadOnly="True" FixedWidth="True" />
            <dxg:GridColumn Header="{DynamicResource LeavePosition}" FieldName="Position" FilterPopupMode="CheckedList" MinWidth="80" Width="100" ReadOnly="True" FixedWidth="True" />
            <dxg:GridColumn Header="{DynamicResource LeaveHTMLColor}" FieldName="HtmlColor" FilterPopupMode="CheckedList" MinWidth="80" Width="150" ReadOnly="True" FixedWidth="True">
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <Rectangle Width="15"
                                                        Height="15"
                                                        Margin="3,3,6,3"
                                                        Fill="{Binding RowData.Row.HtmlColor}" />
                            <TextBlock Text="{Binding RowData.Row.HtmlColor}" 
                                       HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2,0,5,0">

                            </TextBlock>
                        </DockPanel>
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
            </dxg:GridColumn>
            <dxg:GridColumn Header="{DynamicResource LeaveInUse}" FieldName="InUse" FixedWidth="True" FilterPopupMode="CheckedList" MinWidth="60" Width="100" ReadOnly="True">
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" TextAlignment="Left" Margin="5,0,5,0">
                            <TextBlock.Style>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=RowData.Row.InUse}" Value="true">
                                            <Setter Property="TextBlock.Text" Value="Yes"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Path=RowData.Row.InUse}" Value="false">
                                            <Setter Property="TextBlock.Text" Value="No"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
                <dxg:GridColumn.PrintCellStyle>
                    <Style TargetType="dxe:BaseEdit" BasedOn="{StaticResource {dxgt:TableViewThemeKey ResourceKey=DefaultPrintCellStyle}}">
                        <Setter Property="dxp:ExportSettings.TargetType" Value="Text" />
                        <Setter Property="dxp:TextExportSettings.Text" Value="{Binding Path=RowData.Row.InUse}" >
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Path=RowData.Row.InUse}" Value="true">
                                <Setter Property="dxp:TextExportSettings.Text" Value="Yes"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Path=RowData.Row.InUse}" Value="false">
                                <Setter Property="dxp:TextExportSettings.Text" Value="No"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </dxg:GridColumn.PrintCellStyle>
                <dxg:GridColumn.EditSettings>
                    <dxe:TextEditSettings>
                        <dxe:TextEditSettings.DisplayTextConverter>
                            <dxmvvm:ObjectToObjectConverter>
                                <dxmvvm:MapItem Source="true" Target="Yes"/>
                                <dxmvvm:MapItem Source="false" Target="No"/>
                            </dxmvvm:ObjectToObjectConverter>
                        </dxe:TextEditSettings.DisplayTextConverter>
                    </dxe:TextEditSettings>
                </dxg:GridColumn.EditSettings>
            </dxg:GridColumn>
            <dxmvvm:Interaction.Behaviors>
                <UIHelper:GridSelectingBehavior />
            </dxmvvm:Interaction.Behaviors>

            <dxg:GridControl.TotalSummary>
                <dxg:GridSummaryItem x:Name="summary" FieldName="Name" SummaryType="Count" DisplayFormat="Total Count: {0}" Visible="True" Alignment="Left"/>
            </dxg:GridControl.TotalSummary>
            
        <dxg:GridControl.View>
                <dxg:TableView x:Name="LeavesTableView"
                                   NavigationStyle="Row"
                                   AllowHorizontalScrollingVirtualization="True" 
                                   RowDetailsVisibilityMode="Visible"
                                   ShowHorizontalLines="True"
                                   ShowSearchPanelMode="Always"
                                   ShowColumnHeaders="True"                                  
                                   VerticalScrollbarVisibility="Auto" 
                                   HorizontalScrollbarVisibility="Auto"
                                   AllowFixedColumnMenu="False"
                                   AutoWidth="False" 
                                   ShowFixedTotalSummary="True"   
                                   ShowSearchPanelCloseButton="True" 
                                   SearchPanelHorizontalAlignment="Stretch"   
                                   ColumnChooserColumnDisplayMode="ShowHiddenColumnsOnly"                             
                                   AllowGrouping="True"
                                   ShowGroupPanel="False"                             
                                   ShowIndicator="False"              
                                   FocusedRow="{Binding Path=SelectedLeave,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                  >
                    <dxg:TableView.Resources>
                        <DataTemplate x:Key="LeavesReportPrintHeaderTemplate">
                            <Grid Width="{Binding UsablePageWidth}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="130" />
                                </Grid.ColumnDefinitions>
                                <dxe:TextEdit Text=" " Width="{Binding UsablePageWidth}" Grid.ColumnSpan="3" IsPrintingMode="True" Background="#333E5E" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" Height="50" />
                                <dxe:TextEdit Text="Leaves Report" IsPrintingMode="True" HorizontalAlignment="Stretch" 
                                                      VerticalAlignment="Stretch" Padding="12" Foreground="#FFFFFFFF" Style="{DynamicResource TextEdit20Style}" FontWeight="Bold" />
                                <Image Source="/Emdep.Geos.Modules.Hrm;component/Assets/Images/GeosReportLogo.png" Height="50" Width="80"  dxp:ExportSettings.TargetType="Image" Grid.Column="2" />
                            </Grid>
                        </DataTemplate>

                        <DataTemplate x:Key="LeavesReportPrintFooterTemplate">
                            <dxe:TextEdit HorizontalContentAlignment="Center" 
                                                  Width="{Binding Path=UsablePageWidth, Mode=OneWay}" 
                                                  dxp:ExportSettings.TargetType="PageNumber" 
                                                  dxp:PageNumberExportSettings.Kind="NumberOfTotal" 
                                                  dxp:PageNumberExportSettings.Format="Page {0} of {1}"/>
                        </DataTemplate>
                    </dxg:TableView.Resources>
                </dxg:TableView>
        </dxg:GridControl.View> 
      </dxg:GridControl>      
    </Grid> 
</UserControl>
