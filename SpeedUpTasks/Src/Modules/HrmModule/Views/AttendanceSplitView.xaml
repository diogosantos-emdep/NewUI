<UserControl x:Class="Emdep.Geos.Modules.Hrm.Views.AttendanceSplitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" 
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:Validation="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
             xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:HrmApplication="clr-namespace:Emdep.Geos.Modules.Hrm" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:UIConverter ="clr-namespace:Emdep.Geos.UI.Converters;assembly=Emdep.Geos.UI.Converters"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <DataTemplate x:Key="headerTemplate" >
            <TextBlock Text="{Binding TaskOfferCode,UpdateSourceTrigger=PropertyChanged}" />
        </DataTemplate>

        <DataTemplate x:Key="FlowLayoutitemTemplate">
            <dx:DXTabControl MaxWidth="700"
                             ItemsSource="{Binding Tasks ,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             Name="DXTabControl2"
                             TabContentCacheMode="CacheTabsOnSelecting">
                <dx:DXTabControl.ItemContainerStyle>
                    <Style TargetType="dx:DXTabItem">
                        <Setter Property="Header" Value="{Binding Header,UpdateSourceTrigger=PropertyChanged}"/>
                        <Setter Property="CloseCommand" Value="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.CloseTaskCommand}" />
                        <Setter Property="CloseCommandParameter" Value="{Binding}" />
                    </Style>
                </dx:DXTabControl.ItemContainerStyle>
                <dx:DXTabItem>
                    <ScrollViewer ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <Grid Grid.Column="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="55"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>

                            <!--Employee-->
                            <Label Grid.Row="1" Grid.Column="0" Content="{DynamicResource Employee}" Height="30" FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,5,5,5"/>

                            <dxe:ComboBoxEdit Style="{DynamicResource ComboBoxEditStyle}"  Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" Height="30"  Margin="5,5,5,5" 
                                              ItemsSource="{Binding TaskEmployeeListFinal,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged }" 
                                              IsEnabled="{Binding IsEnabaledEmployee,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" 
                                              DisplayMember="FullName" ValueMember="IdEmployee"  IsTextEditable="False" ValidateOnTextInput="False"
                                              AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True" >

                                <dxe:ComboBoxEdit.SelectedIndex>
                                    <Binding Path="SelectedIndexForEmployee" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                        <Binding.ValidationRules >
                                            <Validation:AttendanceValidation FieldName="SelectedIndexForEmployee" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </dxe:ComboBoxEdit.SelectedIndex>

                                <!--<dxmvvm:Interaction.Behaviors>
                                    <dxmvvm:EventToCommand EventName="SelectedIndexChanged"
                                       Command="{Binding DataContext.SelectedIndexChangedCommand, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}"
                                       PassEventArgsToCommand="True"/>
                                </dxmvvm:Interaction.Behaviors>-->
                            </dxe:ComboBoxEdit>

                            <!--Shift-->
                            <Label Grid.Row="2" Grid.Column="0" Content="{DynamicResource AttendanceShift}" Height="30" FontSize="16" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,5,5,5"/>
                            <dxe:ComboBoxEdit Style="{DynamicResource ComboBoxEditStyle}"  Grid.Column="1" 
                                              Grid.Row="2" Grid.ColumnSpan="2" Height="30" Margin="5,5,5,5" IsEnabled="{Binding IsEnabledShift,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" 
                                              ItemsSource="{Binding EmployeeShiftList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                              DisplayMember="CompanyShift.Name" ValueMember="CompanyShift.IdCompanyShift"  IsTextEditable="False" ValidateOnTextInput="False"
                                              AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True"
                                              ApplyItemTemplateToSelectedItem="True">

                                <dxe:ComboBoxEdit.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Margin="5 0">
                                                <TextBlock.Style>
                                                    <Style>
                                                        <Setter Property="TextBlock.Text" Value="{Binding CompanyShift.Name}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CompanyShift.IdCompanyShift}" Value="0">
                                                                <Setter Property="TextBlock.Text" Value="---" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <!--<Image Margin="5 0" Height="16" Width="16" Source="{DynamicResource CoffeeCupImage}" 
                                                   VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <Image.Style>
                                                    <Style>
                                                        <Setter Property="Image.Visibility" Value="Visible" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IdCompanyShift}" Value="0">
                                                                <Setter Property="Image.Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>-->
                                            
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style>
                                                        <Setter Property="TextBlock.Text" Value="-"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CompanyShift.IdCompanyShift}" Value="0">
                                                                <Setter Property="TextBlock.Text" Value="{x:Null}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>

                                            <TextBlock Margin="5 0">
                                                <TextBlock.Style>
                                                    <Style>
                                                        <Setter Property="TextBlock.Text" Value="{Binding CompanyShift.CompanySchedule.Company.Alias}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CompanyShift.IdCompanyShift}" Value="0">
                                                                <Setter Property="TextBlock.Text" Value="{x:Null}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </dxe:ComboBoxEdit.ItemTemplate>

                                <!--<dxe:ComboBoxEdit.ItemContainerStyle>
                                    <Style TargetType="dxe:ComboBoxEditItem">
                                        <Setter Property="ToolTip">
                                            <Setter.Value>
                                                <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </dxe:ComboBoxEdit.ItemContainerStyle>-->

                                <!--<dxe:ComboBoxEdit.ToolTip>
                                    <TextBlock Text="{Binding TaskSelectedCompanyShift.Name}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </dxe:ComboBoxEdit.ToolTip>-->

                                <dxe:ComboBoxEdit.SelectedIndex>
                                    <Binding Path="SelectedIndexForCompanyShift" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                        <Binding.ValidationRules >
                                            <Validation:AttendanceValidation FieldName="SelectedIndexForCompanyShift" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </dxe:ComboBoxEdit.SelectedIndex>

                                <dxmvvm:Interaction.Behaviors>
                                    <dxmvvm:EventToCommand EventName="SelectedIndexChanged"
                                                           Command="{Binding DataContext.SelectedIndexChangedForCompanyShiftCommand, RelativeSource={RelativeSource AncestorType=dxwui:WinUIDialogWindow}}"
                                                           PassEventArgsToCommand="True"/>
                                </dxmvvm:Interaction.Behaviors>
                            </dxe:ComboBoxEdit>

                            <!--Start Time-->
                            <Label Content= "{DynamicResource AttendanceStartTime}" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="5 5 5 5" HorizontalAlignment="Left" Height="30" FontSize="16" />

                            <dxe:DateEdit  Grid.Row="3" Grid.Column="1" Margin="5 5 5 5" HorizontalAlignment="Stretch" VerticalAlignment="Center" AllowNullInput="True"
                                           IsReadOnly="{Binding Source={x:Static HrmApplication:HrmCommon.Instance}, Path=IsPermissionReadOnly,Mode=OneTime,UpdateSourceTrigger=PropertyChanged}">
                                <dxe:DateEdit.EditValue>
                                    <Binding Path="TaskStartDate" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay"
                                             ValidatesOnDataErrors="True">
                                        <Binding.ValidationRules>
                                            <Validation:AttendanceValidation FieldName="TaskStartDate" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </dxe:DateEdit.EditValue>

                                <dxmvvm:Interaction.Behaviors>
                                    <dxmvvm:EventToCommand EventName="EditValueChanging" Command="{Binding OnDateEditValueChangingCommand}" PassEventArgsToCommand="True"/>
                                </dxmvvm:Interaction.Behaviors>
                            </dxe:DateEdit>

                            <dxe:TextEdit Width="110" Mask="HH:mm" Margin="5"
                                          Grid.Row="3" Grid.Column="2" Height="25" 
                                          MaskType="DateTimeAdvancingCaret" DisplayFormatString="HH:mm"  >
                                <dxe:TextEdit.EditValue>
                                    <Binding Path="TaskStartTime" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                        <Binding.ValidationRules>
                                            <Validation:AttendanceValidation FieldName="TaskStartTime" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </dxe:TextEdit.EditValue>
                                <dxmvvm:Interaction.Behaviors>
                                    <dxmvvm:EventToCommand EventName="EditValueChanging" Command="{Binding OnTextEditValueChangingCommand}" PassEventArgsToCommand="True"/>
                                </dxmvvm:Interaction.Behaviors>
                            </dxe:TextEdit>

                            <!--End Time-->
                            <Label Content= "{DynamicResource AttendanceEndTime}" Grid.Row="4" Grid.Column="0"  HorizontalAlignment="Left" Margin="5 5 5 5" Height="30" FontSize="16" />

                            <dxe:DateEdit Grid.Row="4" Grid.Column="1"  ValidateOnEnterKeyPressed="True" Margin="5 5 5 5" HorizontalAlignment="Stretch" VerticalAlignment="Center"
                                          AllowNullInput="True" IsReadOnly="{Binding Source={x:Static HrmApplication:HrmCommon.Instance}, Path=IsPermissionReadOnly,Mode=OneTime,UpdateSourceTrigger=PropertyChanged}">
                                <dxe:DateEdit.EditValue>
                                    <Binding Path="TaskEndDate" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" ValidatesOnDataErrors="True">
                                        <Binding.ValidationRules>
                                            <Validation:AttendanceValidation FieldName="TaskEndDate" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </dxe:DateEdit.EditValue>
                                <dxmvvm:Interaction.Behaviors>
                                    <dxmvvm:EventToCommand EventName="EditValueChanging" Command="{Binding OnDateEditValueChangingCommand}" PassEventArgsToCommand="True"/>
                                </dxmvvm:Interaction.Behaviors>
                            </dxe:DateEdit>

                            <dxe:TextEdit Width="110" Mask="HH:mm" Margin="5"
                                          Grid.Row="4" Grid.Column="2" Height="25"
                                          MaskType="DateTimeAdvancingCaret" DisplayFormatString="HH:mm" >
                                <dxe:TextEdit.EditValue>
                                    <Binding Path="TaskEndTime" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                        <Binding.ValidationRules>
                                            <Validation:AttendanceValidation FieldName="TaskEndTime" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </dxe:TextEdit.EditValue>
                                <dxmvvm:Interaction.Behaviors>
                                    <dxmvvm:EventToCommand EventName="EditValueChanging" Command="{Binding OnTextEditValueChangingCommand}" PassEventArgsToCommand="True"/>
                                </dxmvvm:Interaction.Behaviors>
                            </dxe:TextEdit>

                            <Label Grid.Row="5" Grid.Column="0" Content="{DynamicResource Type}" Height="30" FontSize="16"
                                   HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,5,5,5"/>

                            <dxe:ComboBoxEdit Style="{DynamicResource ComboBoxEditStyle}"  Grid.Column="1" 
                                              Grid.Row="5" Grid.ColumnSpan="2" Height="30"  Margin="5,5,5,5"
                                              ItemsSource="{Binding Source={x:Static GeosApplication:GeosApplication.Instance}, Path=AttendanceTypeList}"
                                              DisplayMember="Value" ValueMember="IdLookupValue"  IsTextEditable="False" ValidateOnTextInput="False"
                                              AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True"
                                              IsReadOnly="{Binding Source={x:Static HrmApplication:HrmCommon.Instance}, Path=IsPermissionReadOnly,Mode=OneTime,UpdateSourceTrigger=PropertyChanged}">
                                <dxe:ComboBoxEdit.SelectedIndex>
                                    <Binding Path="SelectedIndexForAttendanceType" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                        <Binding.ValidationRules >
                                            <Validation:AttendanceValidation FieldName="SelectedIndexForAttendanceType" ValidationStep="UpdatedValue" ValidatesOnTargetUpdated="True"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </dxe:ComboBoxEdit.SelectedIndex>
                            </dxe:ComboBoxEdit>

                        </Grid>
                    </ScrollViewer>
                </dx:DXTabItem>

                <dx:DXTabControl.View>
                    <dx:TabControlScrollView x:Name="DXTabControl1"
                                             HideButtonShowMode="InAllTabs"
                                             NewButtonShowMode="NoWhere"
                                             NewTabCommand="{Binding AddNewTaskCommand}" />
                </dx:DXTabControl.View>
                <dxmvvm:Interaction.Behaviors>
                    <dxmvvm:EventToCommand Command="{Binding DataContext.SelectionChangedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           EventName="SelectionChanged"
                                           PassEventArgsToCommand="True" />
                </dxmvvm:Interaction.Behaviors>
            </dx:DXTabControl>
        </DataTemplate>

    </UserControl.Resources>

    <Grid >
        <Grid.RowDefinitions>
            <!--<RowDefinition Height="40" />-->
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <!--<Button Grid.Row="0" Height="40" Width="40" HorizontalAlignment="Right" VerticalAlignment="Top" 
                Visibility="{Binding AddNewTaskbtnVisibility,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay,Converter={StaticResource BoolToVis},FallbackValue=Hidden}"
                Command="{Binding AddNewTaskCommand}">
            <Button.ToolTip>
                <TextBlock Text="{DynamicResource ButtonSplit}" />
            </Button.ToolTip>
            <Button.ContentTemplate>
                <DataTemplate>
                    <Image Source="{DynamicResource SplitImageSource}"
                           Height="24" Width="24"
                           Stretch="None"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                </DataTemplate>
            </Button.ContentTemplate>
        </Button>-->
        <dxlc:FlowLayoutControl Grid.Row="0"
                                Margin="-10"
                                MaxWidth="1500"
                                AllowItemMoving="False"
                                AllowMaximizedElementMoving="False"
                                AnimateItemMoving="True"
                                BreakFlowToFit="False"
                                DragScrolling="False"
                                ItemsSource="{Binding Tasks}"
                                ItemTemplate="{StaticResource FlowLayoutitemTemplate}"
                                MaximizedElementPosition="Left"
                                Orientation="Horizontal"
                                ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                ShowLayerSeparators="True"
                                StretchContent="True">
        </dxlc:FlowLayoutControl>

        <!--ItemsSource="{Binding Tasks}" ItemTemplate="{StaticResource FlowLayoutitemTemplate}"-->
        <StackPanel Grid.Row="1"
                    Margin="0,10,0,0"
                    Width="290"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">

            <Button Height="30"
                    Width="100"
                    Margin="15,0,30,0"
                    VerticalAlignment="Bottom"
                    Command="{Binding SplitAttendanceViewAcceptButtonCommand}"
                    Content="{DynamicResource EditTargetAndForecastViewAccept}"
                    Style="{DynamicResource Button16Style}" />
            <Button Height="30"
                    Width="100"
                    Margin="30,0,0,0"
                    VerticalAlignment="Bottom"
                    Command="{Binding SplitAttendanceViewCancelButtonCommand}"
                    Content="{DynamicResource EditTargetAndForecastViewCancel}"
                    Style="{DynamicResource Button16Style}" />
        </StackPanel>
    </Grid>

</UserControl>
