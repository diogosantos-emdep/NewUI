<UserControl x:Class="Emdep.Geos.Modules.HarnessPart.Views.DataNav"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="21" d:DesignWidth="230" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors">
    <Grid Height="300">
        <dxe:ButtonEdit AllowDefaultButton="False" IsTextEditable="False" Focusable="False" HorizontalContentAlignment="Center"
                                    Text="{Binding NavigatorText, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,0,6,278">
            <dxe:ButtonEdit.Buttons>
                <dxe:ButtonInfo IsLeft="True" GlyphKind="First" Command="{Binding View.Commands.MoveFirstRow, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="MoveFirstRow" />
                <dxe:ButtonInfo IsLeft="True" GlyphKind="PrevPage" Command="{Binding View.Commands.MovePrevPage, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="MovePrevPage" />
                <dxe:ButtonInfo IsLeft="True" GlyphKind="Left" Command="{Binding View.Commands.MovePrevRow, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="MovePrevRow" />
                <dxe:ButtonInfo GlyphKind="Right" Command="{Binding View.Commands.MoveNextRow, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="MoveNextRow" />
                <dxe:ButtonInfo GlyphKind="NextPage" Command="{Binding View.Commands.MoveNextPage, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="MoveNextPage" />
                <dxe:ButtonInfo GlyphKind="Last" Command="{Binding View.Commands.MoveLastRow, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="MoveLastRow" />
                <!--<dxe:ButtonInfo GlyphKind="Plus" Command="{Binding AddNewRowCommand, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="AddNewRow" />-->
                <!--<dxe:ButtonInfo GlyphKind="Minus" Command="{Binding View.Commands.DeleteFocusedRow, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="DeleteFocusedRow" />
                <dxe:ButtonInfo GlyphKind="Up" Command="{Binding View.Commands.EditFocusedRow, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="EditFocusedRow" />
                <dxe:ButtonInfo GlyphKind="Apply" Command="{Binding View.Commands.EndEditFocusedRow, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="EndEditFocusedRow" />
                <dxe:ButtonInfo GlyphKind="Cancel" Command="{Binding View.Commands.CancelEditFocusedRow, RelativeSource={RelativeSource TemplatedParent}}" ToolTip="CancelEditFocusedRow" />-->
            </dxe:ButtonEdit.Buttons>
        </dxe:ButtonEdit>
    </Grid>
</UserControl>
