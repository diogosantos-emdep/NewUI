<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.Crm.Views.ModulesReportView"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                         xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" 
                         xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
                         xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
                         xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                         xmlns:GeosApplication="clr-namespace:Emdep.Geos.UI.Common;assembly=Emdep.Geos.UI.Common"
                         xmlns:Validation ="clr-namespace:Emdep.Geos.UI.Validations;assembly=Emdep.Geos.UI.Validations"
                         xmlns:local="clr-namespace:Emdep.Geos.Modules.Crm.Views"
                         xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                         xmlns:behaviors="clr-namespace:Emdep.Geos.UI.Behaviors;assembly=Emdep.Geos.UI.Common"
                         mc:Ignorable="d"
                         Height="300" Width="600"
                         x:Name="modulesReportView"
                         Title="{DynamicResource ModulesReportHeader}"
                         TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
                         Style="{DynamicResource WinUIDialogWindowStyle}">

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding ModulesReportCancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:SaveFileDialogService Title="Save Excel Report"/>
    </dxmvvm:Interaction.Behaviors>

    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <!--From date-->
        <Label Content= "{DynamicResource CommonFrom}" Style="{DynamicResource Label16Style}" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" />
        <dxe:DateEdit Grid.Row="0" Grid.Column="1" Style="{DynamicResource BaseUniversalStyle}" Height="30"
                      ShowClearButton="False"
                      Mask="{Binding CultureInfo.CurrentCulture.DateTimeFormat.YearMonthPattern}" 
                      MaskUseAsDisplayFormat="True"
                      HorizontalAlignment="Stretch"  VerticalAlignment="Top" IsTextEditable="False" 
                      HorizontalContentAlignment="Right" InvalidValueBehavior="AllowLeaveEditor">
            <dxe:DateEdit.EditValue>
                <Binding Path="DataContext.FromDate" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" ValidatesOnDataErrors="True" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <Binding.ValidationRules>
                        <Validation:ReportValidation FieldName="FromDate" ValidationStep="UpdatedValue"  />
                    </Binding.ValidationRules>
                </Binding>
            </dxe:DateEdit.EditValue>
        </dxe:DateEdit>

        <!--To date-->
        <Label Content= "{DynamicResource CommonTo}" Grid.Row="0" Grid.Column="3" HorizontalAlignment="Center" Style="{DynamicResource Label16Style}"/>
        <dxe:DateEdit Grid.Row="0" Grid.Column="4" Style="{DynamicResource BaseUniversalStyle}" Height="30"
                      ShowClearButton="False"
                      Mask="{Binding CultureInfo.CurrentCulture.DateTimeFormat.YearMonthPattern}" MaskUseAsDisplayFormat="True"
                      HorizontalAlignment="Stretch" VerticalAlignment="Top" IsTextEditable="False" 
                      HorizontalContentAlignment="Right" InvalidValueBehavior="AllowLeaveEditor"
                      ValidateOnEnterKeyPressed="True" ValidateOnTextInput="True">
            <dxe:DateEdit.EditValue>
                <Binding Path="DataContext.ToDate" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay" ValidatesOnDataErrors="True" >
                    <Binding.ValidationRules>
                        <Validation:ReportValidation FieldName="ToDate" ValidationStep="UpdatedValue" />
                    </Binding.ValidationRules>
                </Binding>
            </dxe:DateEdit.EditValue>
        </dxe:DateEdit>

        <!--Group-->
        <Label Content= "{DynamicResource CommonGroup}" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" Style="{DynamicResource Label16Style}"/>
        <dxe:ComboBoxEdit Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4" Height="30" 
                          ItemsSource="{Binding GroupList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                          DisplayMember="CustomerName" ShowSizeGrip="True"
                          AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True">
            <dxe:ComboBoxEdit.SelectedIndex>
                <Binding Path="DataContext.SelectedIndexGroup" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <!--ValidatesOnDataErrors="True"-->
                    <!--<Binding.ValidationRules>
                        <Validation:ReportValidation FieldName="SelectedIndexGroup" ValidationStep="UpdatedValue" />
                    </Binding.ValidationRules>-->
                </Binding>
            </dxe:ComboBoxEdit.SelectedIndex>
            <dxmvvm:Interaction.Behaviors>
                <dxmvvm:EventToCommand EventName="SelectedIndexChanged" Command="{Binding SelectedItemChangedCommand}" PassEventArgsToCommand="True"/>
            </dxmvvm:Interaction.Behaviors>
        </dxe:ComboBoxEdit>

        <!--Plant-->
        <Label Content= "{DynamicResource CommonPlant}" Grid.Row="2" Grid.Column="0" HorizontalAlignment="Left" Style="{DynamicResource Label16Style}"/>
        <dxe:ComboBoxEdit Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="4" Height="30" DisplayMember="Name"
                          ItemsSource="{Binding PlantList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                          ShowSizeGrip="True"  AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True">
            <dxe:ComboBoxEdit.SelectedIndex>
                <Binding Path="DataContext.SelectedIndexPlant" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <!--ValidatesOnDataErrors="True"-->
                    <!--<Binding.ValidationRules>
                        <Validation:ReportValidation FieldName="SelectedIndexPlant" ValidationStep="UpdatedValue"  />
                    </Binding.ValidationRules>-->
                </Binding>
            </dxe:ComboBoxEdit.SelectedIndex>
        </dxe:ComboBoxEdit>

        <!--Template-->
        <Label Content= "{DynamicResource CommonTemplate}" Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left" Style="{DynamicResource Label16Style}"/>
        <dxe:ComboBoxEdit Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="4" Height="30"
                          ItemsSource="{Binding Templates, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                          DisplayMember="Name" AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True">
            <dxe:ComboBoxEdit.SelectedIndex>
                <Binding Path="DataContext.SelectedIndexTemplate" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <!--ValidatesOnDataErrors="True"-->
                    <!--<Binding.ValidationRules>
                        <Validation:ReportValidation FieldName="SelectedIndexTemplate" ValidationStep="UpdatedValue" />
                    </Binding.ValidationRules>-->
                </Binding>
            </dxe:ComboBoxEdit.SelectedIndex>
            <dxmvvm:Interaction.Behaviors>
                <dxmvvm:EventToCommand EventName="SelectedIndexChanged" Command="{Binding SelectedItemTemplateChangedCommand}" PassEventArgsToCommand="True"/>
            </dxmvvm:Interaction.Behaviors>
        </dxe:ComboBoxEdit>

        <!--Type-->
        <Label Content= "{DynamicResource CommonType}" Grid.Row="4" Grid.Column="0" HorizontalAlignment="Left" Style="{DynamicResource Label16Style}"/>
        <dxe:ComboBoxEdit Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="4" Height="30"
                          ItemsSource="{Binding CpTypes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"           
                          DisplayMember="Name" 
                          IsTextEditable="False" ValidateOnTextInput="False" 
                          AutoComplete="True" ImmediatePopup="True" IncrementalFiltering="True">
            <dxe:ComboBoxEdit.SelectedIndex>
                <Binding Path="DataContext.SelectedIndexCpType" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                    <!--ValidatesOnDataErrors="True"-->
                    <!--<Binding.ValidationRules>
                        <Validation:ReportValidation FieldName="SelectedIndexCpType" ValidationStep="UpdatedValue" />
                    </Binding.ValidationRules>-->
                </Binding>
            </dxe:ComboBoxEdit.SelectedIndex>
            <!--<dxmvvm:Interaction.Behaviors>
                <dxmvvm:EventToCommand EventName="SelectedIndexChanged" Command="{Binding SelectedItemTemplateChangedCommand}" PassEventArgsToCommand="True"/>
            </dxmvvm:Interaction.Behaviors>-->
        </dxe:ComboBoxEdit>

        <!--Options-->
        <Label Content= "{DynamicResource CommonOptions}" Grid.Row="5" Grid.Column="0" HorizontalAlignment="Left" Style="{DynamicResource Label16Style}"/>
        <dxe:ComboBoxEdit Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="4" Height="30"
                          ItemsSource="{Binding Detections, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                          DisplayMember="Name" 
                          PopupFooterButtons="OkCancel" ShowSizeGrip="True"
                          IsTextEditable="False" ValidateOnTextInput="False" AutoComplete="True" 
                          ImmediatePopup="True" IncrementalFiltering="True"
                          EditValue="{Binding SelectedOptionList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
            <dxe:ComboBoxEdit.StyleSettings>
                <dxe:CheckedTokenComboBoxStyleSettings/>
            </dxe:ComboBoxEdit.StyleSettings>
            <!--<dxe:ComboBoxEdit.SelectedIndex>
                <Binding Path="DataContext.SelectedIndexOption" RelativeSource="{RelativeSource AncestorType=dxwui:WinUIDialogWindow}" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                </Binding>
            </dxe:ComboBoxEdit.SelectedIndex>-->
            <!--ValidatesOnDataErrors="True"-->
            <!--<Binding.ValidationRules>
                        <Validation:ReportValidation FieldName="SelectedIndexOption" ValidationStep="UpdatedValue" />
                    </Binding.ValidationRules>-->
            <!--<dxmvvm:Interaction.Triggers>
                <dxmvvm:EventToCommand EventName="PopupClosed"
                                               Command="{Binding OptionPopupClosedCommand}" 
                                               PassEventArgsToCommand="True"/>
            </dxmvvm:Interaction.Triggers>-->
        </dxe:ComboBoxEdit>

        <!--Buttons-->
        <Grid Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="5"
              HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button  Grid.Column="0"
                     Content="{DynamicResource CommonAccept}"
                     Command="{Binding ModulesReportAcceptButtonCommand}"
                     CommandParameter="{Binding ElementName=modulesReportView}"
                     Height="30"
                     Width="100"
                     Style="{DynamicResource Button16Style}"
                     VerticalAlignment="Bottom" />

            <Button Grid.Column="2" 
                    Content="{DynamicResource CommonCancel}"
                    Command="{Binding ModulesReportCancelButtonCommand}" 
                    Height="30"
                    Width="100"
                    Style="{DynamicResource Button16Style}"
                    VerticalAlignment="Bottom" />
        </Grid>

    </Grid>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="KeyDown">
            <behaviors:InvokeDelegateCommandAction Command="{Binding CommandTextInput}" CommandParameter="{Binding Path=InvokeParameter, RelativeSource={RelativeSource Self}}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
</dxwui:WinUIDialogWindow>
