<dxwui:WinUIDialogWindow x:Class="Emdep.Geos.Modules.Crm.Views.MoveCloseDateView"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
                         xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                         xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                         xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
                         xmlns:ViewModels="clr-namespace:Emdep.Geos.Modules.Crm.ViewModels"
                         xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
                         xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                         xmlns:behaviors="clr-namespace:Emdep.Geos.UI.Behaviors;assembly=Emdep.Geos.UI.Common"
                         Height="170" Width="300" 
                         Title="{DynamicResource MoveCloseDateTitle}"
                         TitleStyle="{DynamicResource WinUIDialogWindowTitleStyle}"
                         Style="{DynamicResource WinUIDialogWindowStyle}">

    <dxwui:WinUIDialogWindow.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding MoveCloseDateViewCancelButtonCommand}"/>
    </dxwui:WinUIDialogWindow.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <!--<RowDefinition Height="30" />-->
            <RowDefinition Height="50"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <!--<Label Content="{DynamicResource MoveCloseDateTitle}" Grid.Row="0"
               FontSize="28" 
			   FontWeight="Bold"
			   HorizontalAlignment="Left"
			   VerticalAlignment="Top"
			   FontFamily="Calibri" Margin="0,-25,0,0" />-->
        <StackPanel Orientation="Vertical" Grid.Row="0" >
            <Label Content="{DynamicResource MoveCloseDateNewClosingDate}" FontWeight="Bold" />
            <dxe:DateEdit Text="12/03/2016" HorizontalContentAlignment="Right" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsTextEditable="False"
                          Mask="{Binding CultureInfo.CurrentCulture.DateTimeFormat.YearMonthPattern}" MaskUseAsDisplayFormat="True"
                          EditValue="{Binding OfferCloseDate,Mode=TwoWay ,UpdateSourceTrigger=PropertyChanged}"
                          MinValue="{Binding OfferCloseDateMinValue}" InvalidValueBehavior="AllowLeaveEditor"/>
        </StackPanel>
        <DockPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Width="250" Height="30">
            <Button HorizontalAlignment="Left" Width="100" Content="{DynamicResource MoveCloseDateAccept}"/>
            <Button HorizontalAlignment="Right" Width="100" Content="{DynamicResource MoveCloseDateCancel}" Command="{Binding MoveCloseDateViewCancelButtonCommand}" />
        </DockPanel>
    </Grid>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="KeyDown">
            <behaviors:InvokeDelegateCommandAction Command="{Binding CommandTextInput}" CommandParameter="{Binding Path=InvokeParameter, RelativeSource={RelativeSource Self}}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
</dxwui:WinUIDialogWindow>
